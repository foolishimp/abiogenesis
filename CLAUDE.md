# CLAUDE.md - ABIogenesis

## Authority Lookup

Read current authority from its owning surfaces:

```text
specification/GOALS.md
specification/INTENT.md
specification/PRODUCT.md
specification/requirements/
build_tenants/abiogenesis/typescript/design/
.ai-workspace/tickets/active/
```

`PRODUCT.md` is the sole complete Product-definition surface. GOALS selects the
epic wave. Active tickets carry detailed execution authority. This bootstrap
file does not record current tickets, waves, gates, candidates, or status.

## Stable Architecture

- GTL.TypeScript is the only program language.
- The GTL validator checks whole-Program law without lowering.
- GraphFunction is a named callable graph-template contract, not the whole
  Program.
- HoG directly traverses the admitted Program.
- ABG owns event admission and Event Calculus runtime truth.
- Replay reconstructs and projects that truth.
- Product, install, workspace, catalog, and implementation owners supply their
  declared authority boundaries.
- SDK and CLI are thin projections, not controllers.
- `F_D`, `F_P`, and `F_H` retain distinct semantic authority.

Do not introduce a compiled plan, generated executable Program, hidden default,
feature runner, Public controller, second runtime, rival catalog, private event
writer, compatibility Product, or required process-local runtime authority.

## Delivery Discipline

### Map-First Reference Frames

- Resolve the exact STDO basis and companion Product composition from
  `stdo_abiogenesis.json`. Load the corresponding STDO Representation map and
  released Axiom Indexer from `.genesis/development-products/`, the current
  source-linked `a_c.ABI` map, and `ABI5_PROJECT_REFERENCE_FRAME_BASIS.md`
  before broad source acquisition. Verify source freshness with the released
  tools before using either map.
- Show each selected frame URI, purpose, subject, source routes, effects,
  evidence, stops, and return relation.
- Use separate bounded Worker and Reviewer contexts for materially distinct
  frames. Workers and Reviewers return to the Executive; they do not assign one
  another.
- Re-enter exact Source STDO or ABI authority whenever the map is stale,
  conflicting, unresolved, or insufficient. The map and joined prompt are read
  models, not authority.
- Keep stable `WorkspaceBinding` authority distinct from mutable worksite
  observations. Require owner effect, successor observation, ABG admission,
  events, and fresh replay before treating a physical change as runtime truth.

- Implement only work selected by GOALS and the active ticket set.
- Reuse conforming implementation; do not preserve invalid authority wiring.
- Recover rejected code selectively, never through whole-branch adoption.
- Worker-owned HOW proceeds when Product, requirements, and accepted design
  already determine the outcome.
- Freeze one exact candidate after mechanical readiness checks.
- Assurance reviews the frozen subject for counterexamples and drift.
- Escalate only for Product, requirement, exact Public-family, or materially
  different authority changes.

<!-- STDO_BOOTSTRAP_START -->
## STDO Bootstrap

This scope is routed by an STDO Product Definition Overlay.

Before constitutional work:

1. Resolve the applicable `stdo_<label>.json` for the requested Product scope.
2. Use `constitution.stdo.basis`, not its mutable selector, as the operative basis.
3. Resolve and verify that exact installed release through the STDO toolchain manager.
4. Load the Product Definition's declared bootstrap entrypoint, then exact owning standards as needed.
5. Resolve the applicable accepted Project Reference-Frame Basis or its declared composition.
6. Bind the exact outcome, basis and existing work grant. Use direct Writer entry when the accepted frame configuration permits it and one capable context suffices; use Executive or the declared project equivalent for material coordination. Activate only applicable dependency-ready evaluations, preserving unknown applicability and required independence. Executive does not mutate files or Git; a separate Writer activation binds those effects.
7. Fail closed when the Product Definition, frame basis, subject, authority, or activation is missing, ambiguous, stale, or outside the governed scope.

Mutable methodology source, another installed version, a cache entry, and this
bootstrap cannot replace the exact basis selected by the Product Definition.
A prompt, summary, symbolic map, or prior result may route attention but cannot
replace current source authority or a closed frame result.
<!-- STDO_BOOTSTRAP_END -->
