# ABIogenesis Goals

## Authority

`GOALS.md` is the epic layer. It selects the current delivery wave and states
its Product-level outcome. It does not duplicate ticket execution detail or
redefine Intent, Product, requirements, design, qualification, or release
truth.

```text
Goals -> Intent -> Product -> Requirements -> Design -> Code
  -> Events -> Projection -> Delta -> Scenarios -> Gap -> Reprice
```

`specification/PRODUCT.md` remains the complete ABIogenesis 5.0 Product
definition. T-287 is the sole active delivery ticket.

## Current Goal

| Goal ID | Goal | Success signal | Status |
|---|---|---|---|
| `GOAL-035` | Deliver one complete, source-independent ABIogenesis 5.0 Product for a trusted developer desktop as the direct feature-complete successor to the immutable 4.6 Product foundation. | All 16 selected feature families cross one installed vertical path; selected pre-RC scenarios, exact-candidate qualification, immutable RC, final tap, and post-publication install close without a rival Program, executor, controller, event authority, or release subject. | Active — Wave 1 is accepted; Wave 2 / `A5-F01` is active; only `ST-S01-ROOT/S1` is selected. |

## Current Basis

- Intent, Product, and requirements are accepted and unchanged.
- Immutable STDO `v2.2.2` governs ABIogenesis 5.0.
- The immutable `v4.6.0-rc.5` Product and released artifact are the semantic
  conservation reference. The current Git line's `v4.6.0-rc.3` ancestry is a
  genealogy fact, not a replacement semantic reference.
- Exact-pinned Effect `3.22.1` remains the internal functional-composition
  calculus within its accepted boundary.
- The TypeScript design basis indexed by
  `build_tenants/abiogenesis/typescript/design/README.md` governs realization.
- The stable entry for the selected S1 design is annotated Product tag
  `v5.0.0-dev.286`, commit
  `3014f12571c12f97f85dfe54ca4da28e7dfee3ea`, and tree
  `a399045de5d752b92c084b5b38b358aa2d1c63aa`. Commit
  `326c214c982d527c18d716e1db9637becadd5c71` changes commentary only.
- `A5-F12` and `ABG5-S04` remain planned 5.1 work and do not participate in
  5.0 realization, qualification, or release.

## Product Scope And Conservation

The fixed 5.0 Product scope is:

- feature families `A5-F01` through `A5-F11` and `A5-F13` through `A5-F17`;
- scenarios `ABG5-S01`, `ABG5-S02`, `ABG5-S03`, `ABG5-S05`, `ABG5-S06`, and
  release lifecycle `ABG5-S07`;
- the forty 4.6 traversal-conservation rows; and
- the shape-preserving fibre-substitution differential.

Counts of operations, definitions, capabilities, files, tests, reviews, or
commits are derived projections. They do not change Product scope or
substitute for delivered behavior.

## Feature-Wave Delivery

| Wave | Feature families | Product-level exit | State |
|---:|---|---|---|
| `1` | `A5-F10`, `A5-F02`, `A5-F03`, `A5-F04` | One installed runtime kernel proves event-authoritative truth, one canonical admitted Program, direct HoG traversal, deterministic fresh-process replay, and fail-closed probabilistic result admission. | Accepted functional substrate; integrated M5 qualification remains later work |
| `2` | `A5-F01`, `A5-F09`, `A5-F05`, `A5-F06`, plus the bounded early `A5-F17` consumer path | One source-independent installed Product path closes exact Product, Public, SDK, CLI, and remaining Wave 2 qualification truth without a rival authority. | Active — only `ST-S01-ROOT/S1` selected; S2-S4 and full Wave 2 exit unselected |
| `3` | `A5-F14`, `A5-F07`, `A5-F08` | Packed Hello World, live probabilistic proof, One Surface, and Consensus execute through the same installed Product path. | Pending Wave 2 |
| `4` | `A5-F13`, `A5-F17`, `A5-F11` | Native and bounded host projections, an independent downstream Product, and self-conformance use the exact installed candidate and Public contracts. | Pending Wave 3 |
| `5` | `A5-F15`, `A5-F16` | One exact candidate passes qualification and produces an immutable RC, governed final tap, stable package/tag, checksums, and fresh post-publication install. | Pending Wave 4 |

The earlier `ST-1`, `ST-2A`, `ST-2B`, `ST-3`, and `ST-4` development MVP is
accepted historical input at checkpoint
`a2d62e674f36d76d56d32f7e020eea4e7cfde763`, tree
`1af1f5e31acd2f8292550314ad8907c3c6c2ff68`. It is not the current selected
increment, Wave 2 completion, a root/scenario subject, or release evidence.

## Current Selection

The current re-entry is `goal_reprice` plus a bounded `design_reframe`.
Product and requirements remain fixed. Wave 2 / `A5-F01` selects exactly
`ST-S01-ROOT/S1`.

S1's exact outcome is one independently frozen
`s1_development_candidate` whose prebuilt package artifact:

- contains one canonical, digest-bound Product-owned ABI 5 root-binding carrier and one
  exact owner-bound module publication;
- passes Product verification from explicit external artifact and assessor
  basis identities;
- reproduces the same verification and private-receipt coordinates from a
  fresh offline package-manager extraction whose complete installed bytes
  equal the tarball; and
- yields one immutable, receipt-fed declaration selection before any Product
  runtime operation.

S1 returns only a closed `S1DevelopmentGateResult` pass/refusal for that exact
development subject. Its proof is source-blind, content-addressed, externally
frozen, and read-only during each Product verification cut.

S1 does not satisfy constitutional R1 or any of R2-R10. It does not prove
callable membership, Program validation, graph materialization, an all-`F_D`
traversal, runtime execution, ABG admission, replay, CLI equality,
`ABI5-ROOT-001`, `ABG5-S01`, any other scenario, pre-RC qualification, RC,
final tap, or release. `ST-S01-ROOT/S2` through `S4` and every later feature
increment remain `UNSELECTED`.

The exact HOW, entity schemas, authority equations, proof cuts, foundations,
falsifiers, file ownership, and delivery state machine are owned only by
`build_tenants/abiogenesis/typescript/design/T287_S1_ROOT_ARTIFACT_CARRIER_DESIGN.md`.

## Work Selection

- T-287 is the sole active execution authority under GOAL-035.
- Acceptance of one development gate does not redefine Product meaning or
  imply a later gate, scenario, qualification, or release result.
- Product, requirement, exact-family, or materially different authority
  changes require lawful re-entry.
- Rejected subjects remain archived evidence and gain no authority through
  later reuse.
- Assurance evaluates one frozen candidate for counterexamples; it does not
  create Product truth or a second implementation loop.

## Release Milestones

| Milestone | Exit | State |
|---|---|---|
| `M5` | Waves 1 through 4 place all 14 pre-qualification feature families, required installed negatives, selected scenarios, publication truth, and conservation evidence on one exact candidate. | Active — `ST-S01-ROOT/S1` selected; no M5 closure claimed |
| `M6` | Wave 5 `A5-F15` accepts selected-method binding, self-conformance, selected pre-RC scenarios, and complete 4.6 conservation against one exact pre-RC candidate. | Pending M5 |
| `M7` | Wave 5 `A5-F16` accepts distinct immutable RC, installed-RC, final-tap, released Product, and post-publication install subjects as stable `5.0.0`. | Pending M6 |

`odd_glc` release, ABIogenesis 5.0.1 self-use, and the planned 5.1
observer/tuner Product remain successor work and do not gate ABIogenesis 5.0.

## Progress Reporting

Every status report states the active wave/feature/gate, exact accepted
behavior, accepted versus archived subject identity, remaining counterexamples,
and the precise candidate/evidence coordinates. Paperwork, code volume, test
count, commit count, review count, elapsed effort, or a green source-tree run
is not Product progress.
