# M04 Bootloader Derivation

**Status**: Completed
**Date**: 2026-04-24
**Purpose**: Derive the next TypeScript `M04-app-bootstrap`
bootloader/project-facing delivery boundary from the paused Python reference delivery
evidence without promoting installer script behavior or bootloader content
doctrine into the wrong module boundary.

## 1. Source Material

This boundary derives from:

- `build_tenants/abiogenesis/python/code/gen-install.py`
- `build_tenants/abiogenesis/python/code/gtl_spec/GTL_BOOTLOADER.md`
- `build_tenants/abiogenesis/python/code/gtl_spec/packages/project_package.py`
- `build_tenants/abiogenesis/python/test_env/tests/test_sandbox_install.py`
- `build_tenants/abiogenesis/typescript/design/ABG_COMMON_DELIVERY_LIBRARY_FIRST_SLICE_IACS.md`
- `build_tenants/abiogenesis/typescript/design/M04_INSTALL_BOOTSTRAP_FIRST_SLICE_IACS.md`
- `.ai-workspace/tickets/completed/T-019-realize-typescript-m04-install-bootstrap-under-package-first-installed-runtime-law.md`
- `.ai-workspace/tickets/completed/T-028-realize-a-tenant-local-abg-common-delivery-library-for-installed-root-plans-verification-and-instruction-file-injection.md`

## 2. Position

The next TypeScript `M04` wave does not start from Python regex helpers,
shell-oriented installer flow, or project-package doctrine.

It starts from the paused Python reference design truths:

- bootloader delivery is explicit delivery truth
- bootloader content is delivered and injected idempotently into instruction
  files
- instruction-file mutation is project-facing delivery behavior, not kernel
  or constitutional semantics
- bootloader verification is explicit and typed

## 3. Preserved Boundary Truth

The next TypeScript `M04` slice preserves these truths from the Python line:

- one explicit bootloader document is delivered into the installed root
- instruction-file injection uses explicit markers and is idempotent
- delivery verification proves the bootloader document and instruction-file
  updates are present
- bootloader delivery remains below runtime semantics

## 4. Demoted Python Delivery Detail

The TypeScript line intentionally demotes these Python-shaped details to
delivery binding or deferred concern:

- Python installer file layout
- project-package ontology inside the bootloader source
- Python-specific `.genesis/docs/` install tree
- live sandbox execution and archive capture

## 5. First TypeScript M04 Bootloader Target

The first TypeScript bootloader slice should realize only:

- one admitted public bootloader delivery request carrier
- one bounded delivery plan for the bootloader document
- one bounded instruction-file injection route using the shared delivery
  library
- one closed public bootloader delivery outcome family

This first slice should **not** widen into:

- install root planning
- package-manager execution
- runtime execution or sandbox proof
- public asset registry semantics

## 6. Required Next Assets

Before bootloader implementation starts, this derivation must be followed by:

- the `M04` bootloader first-slice IACS
- the `M04` bootloader structural carrier diagram in Mermaid UML
- the `M04` strict-lane expansion

Only then is the next `M04` bootloader wave ready for implementation.
