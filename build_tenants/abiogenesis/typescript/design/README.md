# ABIogenesis TypeScript Design

## Current Boundary

The ABIogenesis 5.0 Product, requirements, direct-GTL M3 architecture, S03,
S05, and Wave 1 functional interface remain accepted. GOALS and T-287 select
Wave 2 `W2-05` whole-family static constructability for the Program-only
odd_glc Hello sentinel. At banked commit `e7e252ed`, legacy/current behavior and
callability evidence exists for 37/56 definitions: 35 active definitions and
two release snapshots that intentionally return their frozen qualification
refusals. Conformance to the new Section 5.6.2C algebra is 0/56 until each
binding is reconciled, migrated, or exactly proven conforming; 19/56 remain
absent and non-callable. The two callable release snapshots are also contract-
final held because their current success result schemas are `v.never()` and
their bindings use null resource/receipt contracts. The held
`project.read#release_evidence` definition additionally lacks the accepted
authenticated originating snapshot request, same-member owner outcome, and
owner-issued publication-receipt relation. The
exact `bf193b3d` code subject is rejected evidence and does not promote a 39/56
ledger. The dirty five-member interaction experiment is distinct rejected
evidence. Neither advances the accepted counts, conformance axis, or story
points.

The 18 operation identities and 56 definition keys are static external
coordinates and exports, not 56 semantic implementations. The operative shared
binding algebra is exactly three module-static higher-order combinators over
imported typed owner functions: one static owner shell, one exact-prefix read
specialization, and one exact-prefix transition specialization. Every export is
constructed at module initialization as one real `ExactDefinitionCallable`
Effect closure. Public admits and selects one exact installed binding, calls it
once, and projects structurally; Product, GTL, HoG, ABG, Validator, and
Implementation owners retain all semantic and runtime decisions.

Each static shell receives its exact owner-authored strict Valibot resource-
assertion and receipt contracts with the fixed packet and typed owner import.
Those module-static contracts are not runtime callbacks or another
combinator. They preserve exact `TResources` and `TResourceReceipt` generics;
the same exact receipt type indexes the singular
`DefinitionExecutionFault<K,TResourceReceipt>` branch. Its three closed
variants carry no receipt before acquisition/append, carry the latest exact
owner- or ABG-issued receipt after separately durable work, or conserve an
already-issued exact receipt when enclosing output/receipt validation later
fails. Owner semantic refusal remains an owner output. The canonical host
transports the variant and receipt losslessly; it does not roll back, inspect a
store tail, synthesize a generic receipt, or use a side channel. The shell
performs structural admission only, while resource meaning, authorization, and
currentness remain owner law. The verified manifest and dependency lock must
resolve each declared module/export/member coordinate to one callable. That
installed static resolution is required; semantic string dispatch, ambient
lookup, and invocation-selected handlers remain prohibited.

Constitution Section 16.11 closes the cross-family owner-input authority
design. One pure Product exact-set resolver supplies installed-definition and
Product-publication equality, actual WorkspaceBinding/ProductSet/ProductInstall/lock bodies,
the accepted invocation-authority actor/attribution coordinates, complete
family/catalog identity, and exact grants from one immutable direct owner-output
input set. That input set directly reuses the
complete actual owner-issued `IntrinsicPublicFunctionFamily` body/coordinate,
the actual `ProductPublicContractCatalog` body/coordinate, and the complete
existing digest-valid `VerifiedProductArtifact`. The family uses its accepted
18-operation/56-member operation/member digest projection. The actual catalog
digest omits only top-level `catalogDigest`; the artifact's existing PFC-F08A
`CompleteDefinitionContractCoordinateMap`, not the catalog, supplies the 56
definition-contract coordinate sets. No second 56-map coordinate or recursive
body-digest law exists. The resolver selects K directly from that verified map
and joins the accepted definition/execution-binding specification to verified
native declaration evidence and the unique admitted Product. No intermediate
installed-definition snapshot or coordinate exists. These are adjacent resolver
relations, never intrinsic definition fields, and require no ambient/source read. The resolver
derives install coordinates only through `productInstallCoordinate`, rebuilds
the ProductSet in its declared order, and validates WorkspaceBinding through its
candidate projection while carrying `admissionEventRef` separately from the
owner digest preimage. No actor,
attribution, or InstalledProduct body is invented.

One parameterized ABG projector declares and receives the complete PFC-F04-
admitted invocation and exact current-K grant bodies, the full validated prefix,
and actual non-materialization `PublicOperationAdmissionBasis`. Except for run
invoke, it also receives the exact event id and returns those bodies unchanged
with the unique full actual `public_operation_admitted` event.
The coordinate/body prefix pair must pass the existing durable-coordinate
validator and exact canonical-event-line UTF-8 length/digest relation before
selection; J05/L11 provenance does not replace that pure equality check.
For the two `run.invoke` variants, event id and source are null; the projector
instead returns the complete accepted `EffectfulPublicInvocationTruth`
`available` value for the PFC invocation ref. That truth is historical only.
L35/L37 reuse the existing transient `PreparedProductRunInvocation` to construct
the distinct Program/policy root grants and Product invocation, and their
shared-authority repair removes the current `authorityMatches` equality/subset
between that root set and the exact PFC/L66 current-K grant set. The domains
join only through their actual actor, WorkspaceBinding, Program, and session-
policy relations. L38 retains the full J03 artifact-truth and J04 CatalogView/
application bodies beside the prepared output, passes them directly to
`admitExactInvocation`, and never reconstructs them from a digest. L69 plus
L38's repeated held-prefix/availability check admits the actual Product-owner
pair. The two continuation and five interaction-response variants separately
carry the complete accepted root `InvocationAdmission`, require
`projectExactInvocationAdmissionAtPrefix` to return that body canonically from
the supplied prefix, and join it to the distinct current PFC-F04 invocation
through workspace and actor while the basis binds current invocation ref/request
digest. Root catalog/program fields and the Program-declared singleton source
grant validate against the root projection and source event. The exact current-K
grant set stays independently PFC/L66/Metadata-bound and need not contain that
root capability. Their
source remains in the current PFC basis/request-digest domain, never the root
Product-owner invocation domain. The bounded result-assessment ingress binds
the full PFC invocation digest; continuation/respond and witness variants bind
the separately verified exact-operation coordinate plus request digest. None of
those digest domains is equated.
Their existing live source events are not yet prefix-projectable before owner
work: continuation response/resume and witness admission currently append the
source and semantic event in one transaction. The repaired HOW splits those
existing candidates into two guarded one-row transactions. The first admits
only `public_operation_admitted` and yields P1 for J05/L11/L67; after L68, the
second consumes exactly P1 and appends only the existing semantic event. It
reuses `prepareContinuationPublicOperation`, the response/resume preparations,
and witness `publicOperationCandidate`/`prepareSemanticEvent`; witness semantic
projection retains the original P0 events. The existing F_H invocation-fact
projection recognizes a fully validated source-only P1 as pending prior
admission and upgrades it when the adjacent owner event arrives. E33 still has
exact `L55 -> L69 -> L56` ordering. No payload variant, root-contract digest,
event kind/fluent, owner meaning, port, combinator, or leaf is added.
Historical projection proves
prefix consistency only. The exact held-store/expected-prefix guard immediately
before each effect or append alone proves live currentness. Their exact-equality
product is transient `DefinitionInvocationAuthority<K>` inside the owner kernel,
never a Public or persisted carrier. The sole existing `CapabilityGrant` body
covers the complete 18-operation union; the two-factor carrier uses a separately
named six-operation/18-key consumer subset. It requires
`grantSet == requiredGrantSubset ==` exact
`MetadataOf<K>.capabilityRefs`, rejects every surplus grant, and binds
`familyRef`, `familyDigest`, the accepted family member projection, and the
complete `PublicContractCatalogCoordinate` into Product resolution identity.
The raw runtime-schema-bearing family body is validated transiently, never
serialized into that identity. Family-
specific result, witness, interaction, run, and materialization fields remain
outside it. The D11-specific grant/authority bodies are superseded; only pure
Product assessment, ABG historical projection/candidate/admission, the live
held-store guard, Section 16.10 typed substrate faults, and shared E10
`assessment_evidence` specialization are retained.

The only bounded new coordinate/body relation in Constitution 5.6.2C is the
Product-owned materialization-authority candidate. Installed-definition
resolution directly reuses the actual family definition/execution-binding
specification, verified PFC-F08A K slot and native declaration evidence, and
unique admitted Product; no intermediate snapshot or coordinate exists. The 16
non-materialization consumers reuse the actual
`PublicOperationAdmissionBasis` body without assigning it a fictional RD
identity. The `result.assess#assess` binding first supplies its missing source through one
exact nine-key event-candidate projection on the existing full-invocation-digest
`public_operation_admitted` variant and existing expected-prefix transaction.
Its event contains only typed ingress/binding fields; the later `assessed` event
alone carries owner meaning. It adds no payload variant or root-contract digest
change. For the two materializers, one strict payload/admission variant of the
existing `public_operation_admitted` event embeds the Product candidate's full
admitted invocation and complete grant bodies with its ref/digest before effect;
successor-prefix replay recovers it exactly. The closed twelve-key payload and
workspace event envelope are constructed only from that candidate, with empty
causation fixed explicitly. Existing
`admitNonEmptyRuntimeEventTransactionAtDurablePrefix` plus `admitRuntimeEvent`
performs the held-prefix check, admission, and non-null successor relation.
The later Product result retains the separate existing artifact carrier, but
live artifact admission/truth is still closed to Product install/workspace bind.
The L62/L63 binding remains C2-pending on a bounded EVENTS-032 ABG HOW extension
that admits the full owner result against the held predecessor using its
manifest ref/digest. This repair claims no post-effect materialization
callability; the result cannot substitute for the ingress candidate and no
top-level result coordinate is added. The shared-carrier addition itself adds no
event kind, Event Calculus meaning, registry, controller, callback,
generic JSON bag, source read, runtime, or second authority. L67 remains pure:
`L66 + complete PFC-F04 admitted invocation -> Product candidate -> L69 ->
existing non-empty expected-prefix ABG transaction -> successor prefix -> J05
-> L11 -> L67`, with no L70. The active selected factor
basis is exactly 69 leaves, `L01..L69`.

The added payload variant changes `ROOT_EVENT_CONTRACT_DIGEST`. Durable-prefix
coordinates, reopen authorities/handoffs, event-resource receipts, replay
semantic-view identities, downstream packed identities, and stored restart/
reopen proof carrying the former digest must be reissued from lawful sources or
migrated under separate authority; they cannot be silently restamped. Existing
event bytes, event ids, kind roster, and Event Calculus meaning remain conserved
when their inputs are unchanged.
The continuation/respond/witness split separately invalidates their compound-
transaction shape, new P1 receipt, pending-prefix replay, retry/duplicate/
interleaving/failure proof, packed identity, and installed binding proof. The
cone is global because `projectEffectfulPublicInvocationTruthAtPrefix` folds the
changed F_H facts before selecting a queried ref: its available/duplicate/
invalid-history contract, Product-install/workspace-bind E24/L22 and E25/L27
through `admitArtifact`, run admission, continuation preparation, installed/
Public consumers, and cross-operation pending-P1 falsifiers must be regenerated.
An exact P1 makes its own ref duplicate and an unrelated fresh ref available;
malformed pending truth invalidates every query, without creating a pending lock
or owner meaning. Its existing variants do not change the root-contract digest.
With no intervening append, both existing events, ids, ordinals, causal relation,
and final raw-log prefix stay byte-identical; post-ingress failure carries P1
under Section 16.10.
The exact ordered E31-E35/E37-E38 trace rows change only to expose that fixed
preparation -> first L69 -> typed-ingress transaction -> successor order before
J05/L11/L67. The L01-L69 identifier basis and owner leaves are unchanged, and
E33 retains the later exact `L55 -> L69 -> L56` subsequence.
The run grant-domain repair separately invalidates `authorityMatches`, Product
run preparation/admission grant proof, the L67 availability/L68 join proof, and
E29/E30 packed bindings. It changes neither the availability projector nor run
event bytes/order or the L35/L37/L38 leaf meanings.

E33 ordering is singular: L53 historical projection, Product/environment join,
L54 decision, and L55 candidate construction may run against the immutable
prefix; L69 then checks the held store exactly after L55 and immediately before
L56 admission/effect. A stale store refuses at L69 before effect.

Product resolution, ABG historical projection, and transient composition each
have a closed refusal union. Candidate-side failures before invocation admission
map only to the existing indexed admission refusal. After invocation admission,
invariant, substrate, or held-store failures map only to the receipt-appropriate
Section 16.10 `DefinitionExecutionFault`. Only a family owner reached after
successful composition can return its exact owner refusal; no infrastructure
failure is relabelled as owner `basis_mismatch`.

Foundation/Technology disposition is exact. `catalog_reuse` covers
`canonicalJson`, `compareUnicodeCodeUnits`, `sha256Canonical`, `deepFreeze`,
`product/exact_match.ts`, the actual family definition/execution-binding,
catalog/verified-artifact/PFC-F08A/native-evidence laws, the named Product
environment/install verifiers and coordinate projections, PFC-F04 invocation
admission plus its self-contained identity equations, ABG validated-prefix,
`projectExactInvocationAdmissionAtPrefix`, replay, and grant callables, the
existing continuation/respond/witness source and semantic candidate
constructors, and the existing non-empty durable-prefix transaction, event
admission, held-store guard, effectful-invocation availability projector, and
transient Product run preparation. That reuse includes run no-prior-admission
truth plus later owner admission and the complete seven-key continuation root
admission while keeping current-K and Program/root grant domains distinct, plus
Valibot `1.4.2` and Effect `3.22.1`.
`catalog_extension` applies to the sole `CapabilityGrant` body and its full
operation index, the run preparation `authorityMatches` grant-domain repair, the
split of the three existing compound admission callables, and the pending-source
branch of the existing F_H prior-admission projection.
`catalog_addition_proposal` covers the nine-key result-assessment
event-candidate projection, the minimal Product
materialization candidate plus closed existing-event payload/projection
relation, one immutable-input Product resolver, one immutable-prefix ABG
projector, and one pure transient equality/refusal mapper. `FS-18`/`FS-19`
select this lowest-cost reuse: no new dependency, duplicated project/library
callable, schema or digest engine, grant family, event kind/fold, registry,
store, framework, persistence layer, or runtime is introduced.

Constitution Section 16.12 closes the local `run.continue` design re-entry.
One owner-local `RunContinuation` kernel consumes Section 16.11's transient
`DefinitionInvocationAuthority<K>` and one pure branch-neutral ABG frontier
projected from the same immutable prefix. The existing
`RUN_OPERATION_CONTRACTS.continue.current_intent` and `.selected_action`
packets fix two irreducible leaves at module initialization, and each binding
uses existing `bindExactPrefixTransition`; the kernel is not a fourth
combinator. The frontier carries only exact common replay-derived Run,
Continuation, invocation, execution-basis, prefix, and cursor-lineage truth.
Branch fields are disjoint rather than nullable slots in a universal bag.

The Product-owned plain immutable `RunContinuationDecisionPort` projects only
the existing member-indexed outcome union. `current_intent` consumes the exact
replay-derived admitted intent, admitted continuation input, and expected basis
and admits no new intent. `selected_action` consumes one exact admitted selected
`NextActionProjection`, exact same-basis or covering-reprice truth, and the
matching Product-issued `ProgramGraphFunctionMembership`; ABG then admits
exactly one new ConstructionIntent before the owner kernel invokes only the
projection-declared target. These are exactly `continueExecution` for the
current intent and `evaluateNext -> admitConstructionIntent ->
invokeGraphFunction` for the selected action. Both branches preserve Section 16.10 receipts
across every separately durable child transaction. No runtime mode, registry,
raw-event fold, Public sequencing, new event meaning, nullable authority bag,
or caller-selected topology is admitted.

Constitution Section 16.13 closes the local release-family design. The pure,
eventless `release_evidence` read uses `bindStaticOwner`, never an ABG-prefix
read, and projects only from one authenticated originating snapshot
invocation/request, its exact same-member owner outcome, and its exact owner-
issued publication receipt bound to the requested cut and manifest. Authentic
refusal plus an exact no-publication receipt yields `not_ready`; absence cannot
mint refusal. `published_rc` and `tapped_release` retain separate nominal
success, refusal, resource, and publication-receipt contracts and separate
pre-RC versus accepted-RC/installed-RC/final-delta currentness and commit laws.
The published-RC refusal algebra is exactly six common causes plus
byte-currentness `bytes_mismatch` and publication-effect
`publication_failure`; Wave 2 currently reaches the common six. The
tapped-release algebra is those eight plus its four accepted-RC/install/delta/
gate causes; Wave 2 currently reaches the common six plus accepted-RC, install,
and delta causes, nine total.

The live non-generic `ReleaseSnapshotRefusal` is narrowed by member/code aliases,
and each two-argument `AdmittedPublicInvocation` uses its exact
`{operationId, memberKey}` object and JSON-record-compatible request.
The current shared twelve-code schema and member/code-independent operation-
result type are migration inputs: contract-final code wires distinct strict
eight- and twelve-code refusal schemas plus exact member-dependent construction
and port types to the corresponding owner packets, with no shared admission
fallback.

Each member receipt is a separate `publicationDisposition` union. Its
`not_published` arm carries only exact origin/request, narrowed same-member
refusal, requested cut/manifest, truthful nullable acquired resources and
empty-or-nonempty exact residue/compensation plus provenance, with empty
publication outputs. Its `committed` arm alone requires exact member success,
non-null resources, full member qualification/artifact/currentness, and exact
corresponding request/result/receipt cut/artifact/manifest/provenance relations.
Section 16.10 still transports the exact receipt unchanged after commit and
after output/receipt-validation faults.
Current `v.never()` and null-resource forms are migration inputs. All success
and publication remain unreachable in Wave 2 and owned by Wave 5.

Constitution Sections 5.6.2, 5.6.2A, 5.6.2B, and 16.6 retain historical
classification evidence only. Their `PublicExecutionCandidate`, topology-arm,
generic resource-provider, and C1F/D6 advancement design is superseded and is
non-operative implementation authority. Sections 5.6.2C and 16.7, as amended
by receipt-conservation record 16.10, cross-family record 16.11,
run-continuation record 16.12, and release-family record 16.13, are the
operative Wave 2 binding law. The
accepted census, owner classifications, indexed outcomes, Effect `3.22.1`
composition, HoG/ABG durability boundaries, and legacy deletion target remain
conserved without turning `E`, `L`, `J`, `R`, `X`, or `T0-T9` proof maps into
runtime types or implementation budgets.

Constitution Section 5.6.5A activates Proof-frame `SP-05` and `SP-06` for
W2-05. Each changed common combinator and irreducible owner kernel/seam receives
one deep review. Fixed equivalent packets receive exhaustive mechanical
schema, digest, coordinate, manifest, installed-resolution, and legacy-
exclusion checks across all 56 coordinates, including all three fault variants
and lossless receipt transport. Review reuse requires exact byte identity and
unchanged governing contracts, including the same receipt/currentness and
fault-boundary law. This compresses repeated reasoning without sampling,
reduced assurance, code acceptance, or partial points.

`A5-F12` and `ABG5-S04` are deferred to planned 5.1 work. Their frozen design
remains non-operative future input under backlogged T-268.

The current design basis is:

- [T-287 Graph Catalog Contraction Accepted Design](./T287_GRAPH_CATALOG_CONTRACTION_ACCEPTED_DESIGN.md),
  which supersedes every catalog, catalog-view, catalog-application,
  registry-entry, and catalog Event Calculus relation in the terminal-
  quiescence design while preserving its conforming execution relations;
- [T-287 Terminal-Quiescence Owner Network Accepted Design](./T287_TERMINAL_QUIESCENCE_OWNER_NETWORK_ACCEPTED_DESIGN.md),
  the exact accepted A5-F10 runtime-truth implementation composition;
- [ABIogenesis 5.0 Realization Constitution](./ABI5_REALIZATION_CONSTITUTION.md),
  the sole local STDO 2.2.2 disambiguation for AI-authored design, coding,
  review, proof, lawful technology, common libraries, and entity lifecycle
  truth;
- accepted M03 and M05 Sections 1 through 12 at S03 candidate `8865ccff`;
- [M05 S05 Consensus Global-To-Local Design](./M05_S05_CONSENSUS_GLOBAL_TO_LOCAL_DESIGN.md);
  the accepted S05 realization basis;
- [ADR-045 Global Design Constraints Survive Local Projection](./adrs/ADR-045-global-design-constraints-survive-local-projection.md)
  as global-to-local rationale; and
- accepted S06 predecessor designs `4f80f84a`, `2bb7b594`, and `aa0daa62` as
  immutable evidence refined by exact accepted Gate 1 subject `3f80ba23` and
  the explicit current Product/catalog supersessions above.

M05 Section 13 remains superseded by the accepted S05 design. The frozen
[S04 observer/tuner design](./M05_S04_OBSERVER_TUNER_GLOBAL_TO_LOCAL_DESIGN.md)
and [ADR-047](./adrs/ADR-047-reflective-optimization-is-gtl-over-replay.md)
are planned 5.1 input, not 5.0 design authority. Completed T-272 and T-286
remain evidence only.

## Governing Truth

Read in this order:

1. specification/GOALS.md
2. specification/INTENT.md
3. specification/PRODUCT.md
4. specification/requirements/
5. accepted functional designs in the current design basis above
6. ABI5_REALIZATION_CONSTITUTION.md for local realization and review
7. exact Gate 1 object `3f80ba23` for the Wave 2 structural owner map, as
   reconciled by current Product requirements and T-287
8. the active ticket selected by GOALS

Specification defines WHAT. Accepted design defines HOW for S06. The deferred
S04 subject defines no 5.0 implementation or review authority.

## Historical Evidence

Other files in this directory and `build_tenants/common/design` are donor or
historical evidence unless GOALS, the active ticket, or the current design
basis explicitly consumes them. They may supply retained behavior, test ideas,
and native-carrier evidence only through an active owner.

They do not define current modules, interfaces, sequencing, public operations,
or implementation authority. A maintained historical file list is
intentionally omitted because it would create a second stale design-status
projection; Git and the T-284 correction vector preserve that inventory.

## Implementation Gate

T-287 currently selects the bounded `W2-05` static-binding `design_reframe`.
The next implementation cut first installs the singular receipt-indexed fault
ABI and lossless host transport while the already-authorized `run.invoke`
bounded repair and twelve-key sentinel proof continue. It retains the shared
three-combinator foundation, then implements the one Product definition/grant
immutable-input resolver, one ABG immutable-prefix historical projector,
the complete Product family/catalog/verified-artifact/PFC-F08A and direct
environment/install input relations, complete ABG invocation/grant/event/basis
bodies, the distinct Product materialization candidate/ingress,
transient equality/refusal composition, and exact held-store guard;
splits the existing continuation/respond/witness typed-ingress rows from their
semantic-event transactions with pending-P1 replay; after that it implements
result assessment, six witness packets, and one E33
kernel/five packets in that order. Materialization owner mechanics may proceed
independently, but both bindings wait for the shared carrier;
implements the ratified `run.continue` kernel through the two static packets and
two irreducible leaves; proves both separately durable child-append topologies
under Section 16.10; atomically removes the legacy current-intent-only
`public/operations.ts::applyRunContinue`, caller `continuationAuthority`, and
Public-owned continuation rehydration path when the two native bindings replace
them; binds held `release_evidence` only through the accepted pure Product
origin/outcome/member-receipt relation using `bindStaticOwner`, never an ABG
prefix; independently migrates the exact `published_rc` and `tapped_release`
future result/refusal/resource/receipt contracts while retaining refusal-only
Wave 2 behavior and Wave 5 publication authority; and only then proves 56/56
packed loadability plus legacy exclusion.

Parallel work obeys the operative dependency law. Each lane binds exact base
commit/tree, digests every consumed interface, owns one equivalence seam, and
has exclusive writable paths. Shared fault/host/combinator surfaces; grant/
resolver/projector/composition surfaces; materialization/persistManifest;
`run.invoke` kernel/packets; and generated/index/schema/manifest/Public/packed-
integration/legacy-deletion surfaces are single-writer. Overlapping paths,
seams, dependencies, or invalidation cones cannot run together; pre-carrier-
freeze run/materialization work is owner-local only; upstream changes stop and
rebase dependents with renewed digests. Reviewers use frozen bytes and disjoint
equivalence units, shared-unit verdicts gate dependents, and one final integration
review covers joins, the exact 56-coordinate census, packed resolution/
projection equality, and legacy exclusion. Numbered order controls dependency,
acceptance, and merge priority, not blanket execution serialization.
No dirty rejected interaction closure, per-key wrapper, fourth combinator,
generic receipt, side channel, enclosing transaction, stub, runtime registry,
old C1F/D6 topology-arm carrier, ABG prefix release read, generic release
kernel, or duplicate release framework may cross this gate.
