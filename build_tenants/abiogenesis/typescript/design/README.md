# ABIogenesis TypeScript Design

## Current Method And Work Selection

The sole exact method selection is
`repo://abiogenesis/stdo_abiogenesis.json#/constitution/stdo/basis`; its
composition owns the release-matched Axiom Indexer and STDO Representation
Development Products. The RC6 migration and all current work are bounded by
[GOALS](../../../../specification/GOALS.md#current-selection) and
[T-287](../../../../.ai-workspace/tickets/active/T-287-deliver-abiogenesis-5-feature-waves.md#stdo-rc6-migration).
Wave 2 remains `HOLD`. Bounded C0 correction-01 is completed and Executive-accepted
after independent review returned `satisfied`.
[Current Selection](../../../../specification/GOALS.md#current-selection),
[T-287 W2-BL-C0](../../../../.ai-workspace/tickets/active/T-287-deliver-abiogenesis-5-feature-waves.md#w2-bl-c0),
and its [corrective record](../../../../.ai-workspace/comments/codex/20260907T033506Z_W2_BL_C0_POST_PUBLICATION/correction-01/execution.md)
own the exact accepted subject, review and evidence limits. Parent subjects and
exercised proof remain immutable history. Local context is regenerated from
these settled owners; no next implementation is selected.

Recorded accepted hashes identify their original exact subjects. The completed
RC6 migration changed method/frame routes only. C0's correction changes only
the public-surface HOW paragraph to keep the physical writer internal to the
guarded owner; it does not relabel frozen prior bytes. C1/C2/C3 semantic HOW and historical acceptance remain
unchanged; no new qualification is claimed.

The accepted [native event-contract compatibility](T287_NATIVE_EVENT_CONTRACT_COMPATIBILITY_DESIGN.md)
and [interrupted event-resource recovery](T287_INTERRUPTED_EVENT_RESOURCE_RECOVERY_DESIGN.md)
supplements preserve exact profile ancestry, physical acquisition and witnessed
stop ownership. [Checkpoint11](../../../../.ai-workspace/comments/codex/20260911_ABG5_EXECUTIVE_CHECKPOINT_11/checkpoint.md)
records their application and separate implementation/runtime limits.
The accepted [native liveness conservation](T287_NATIVE_LIVENESS_CONSERVATION_DESIGN.md)
binds the same current profile to native probe, lease, interruption and
replay-derived disposition law.

## Pre-Baseline Boundary (Historical; No Current Grant)

The ABIogenesis 5.0 Product and requirements remain fixed. Wave 1 is accepted;
Wave 2 is current; and the accepted `ST-S01-ROOT/S1` v4 design remains
implementation authority. Its bounded Cut-A convergence amendment was
independently accepted and delivered at `9bb230efaa5a1db06c7932a1204a5d477ead5e0f`.

The prior exact C0/C1/C2 worksite-root design was accepted after two independent
`GO` reviews with P0/P1/P2 all zero and one retained C2 trusted-desktop P3.
Two later independent Stage 1 source reviews returned `HOLD`. C1 and C2 remain
accepted and immutable; a bounded C0 atomic-window reframe is now frozen for
independent review and records a distinct C0 trusted-desktop P3. The exact
19-path canonical-root C0/C1/C2 I/E selection is retained, but its nine Stage 1
source paths are frozen on `HOLD`, owner-unity repair remains pending, and Stage
2 is blocked. The design carries the existing full `WorkspaceAuthorityBasis`
alongside `WorkspaceBinding`, authenticates both against the exact-prefix
environment before dispatch/effect, resolves authored worksite subjects and
snapshot sources beneath `WorkspaceAuthorityBasis.canonicalRoot`, and retains
`WorkspaceBinding.roots.productRoot == ProductInstall.installedRoot` as the
immutable Program-owner identity with zero Product delta. Owner unity requires
the Program/GraphFunction publication and selected implementation binding to
resolve through that one unique install; their publication digests are separate
and need not be equal. C3 inherits exact C1
tasks unchanged and refuses mixed bases before branch dispatch. The selection
remains bounded to exactly 19 existing realization/evidence paths. Product/
requirements/Public/events/GTL/C3 topology, package
metadata, indexes, generator source, unspecified paths, odd_glc/live/model,
artifact acceptance, package publication, version, qualification, release, and
Git effects remain unselected.

S1 is one non-closing development gate over an exact prebuilt
`s1_development_candidate`. Its claim ends at packed Product verification,
fresh installed-byte reproduction, private preimage/receipt convergence, and
one lexical root declaration selection. It has no Product runtime, R1-R10,
scenario, pre-RC qualification, RC, final-tap, or release claim.

The accepted v4 basis was authored from detached stable commit
`326c214c982d527c18d716e1db9637becadd5c71`, tree
`518d37bda358f85e07ed263293909285694b5cac`, independently accepted as exact
commit `701f6c018257d271465860ecb097b44381d614d0`, and delivered before the
reviewed repo-local execution-selection basis at `bc3a9377b926`. The bounded
carrier amendment is authored from exact dependency-closure delivery and
amendment-construction basis `ece6597ed8846323ccab3d9a5736ecfa03f74bb3`, tree
`31c9557db076ce5348184f10c9e4c8fb4d9ec92d`.

## Frozen C0 Candidate And Accepted C1/C2 Worksite-Root Design

[ABIogenesis Project Reference-Frame
Basis](./ABI5_PROJECT_REFERENCE_FRAME_BASIS.md) is the selected structured-prose
extension for this work. The Definition-selected immutable STDO owns the generic
frame method, conditionally applicable Executive/Worker/Reviewer engagement,
and generic specialist families.
The ABI basis adds only ABI/GTL/HoG/ABG and mutable-worksite causal relations.
It is context and evaluation structure, not a frame runtime or Product
authority.

[T-287 Wave 2 odd_glc Typed-Workspace Admission And Functional Traversal
Design](./T287_W2_ODD_GLC_TYPED_WORKSPACE_ADMISSION_DESIGN.md) has an accepted
R0/R1 force rank and roadmap. Its broad R2/R3 questions remain proposed,
unratified, and non-executable as HOW; only the bounded children explicitly
accepted or selected below carry authority.

The prior accepted C0 bytes and the still-accepted C1/C2 designs are identified
below. The current C0 path contains a frozen atomic-window review candidate;
that candidate has no acceptance effect. The prior accepted relations and exact
coordinates remain predecessor evidence.

| Design subject | Frozen SHA-256 and status |
|---|---|
| C0 mutable-worksite causality | Correction-01 accepted and closed after independent `satisfied` and Executive `accept`. [Current disposition](../../../../.ai-workspace/comments/codex/20260907T033506Z_W2_BL_C0_POST_PUBLICATION/correction-01/execution.md) owns exact identities and grants. Original HOW `1bceff03c850a28801a40ee267ed0e386173b1c50f3c9bb29cbab6556345ca06`, immutable [subject](../../../../.ai-workspace/comments/codex/20260907T033506Z_W2_BL_C0_POST_PUBLICATION/subject.json), earlier atomic-window and accepted predecessor bytes retain their historical dispositions; their exercised evidence does not establish clean corrective closure. |
| C1 live-LLM construction | Accepted `61589c4428c6d26f7e8063d40c9d6e653897df8a5bbfaf8d0fb048ce8cce3c61` |
| C2 command execution | Historical accepted bytes `8dbe22c8bcc2bcedc0e77a5f03a2fde1805d408e555e83bde342b9bb1d2a2301`; semantic HOW conserved by the method-header-only RC6 successor |

The complete frozen review subject also contained GOALS at SHA-256
`69fbc1b91cd511839308c110648a4eb4154a1aa0bc1b8d95d2819bbaf768dfa5`,
T-287 at `aa3863cddd4efabdd89c5bebadca307b2daa794adc1b071b32b1557574ba39f6`,
and this README at
`027883ffd5b1edee36f1709b35b774735b22f88fd124f2d83b2d600fc1d83829`.
Those three hashes identify the prior accepted review preimage. The later
authority projection changed only those three surfaces. The bounded reframe
changed C0 into the candidate recorded above and left C1/C2 bytes unchanged at
that time. RC6 migration preserves those identities as history and changes only
the C2 method header, not its accepted runtime relation.

[T-287 W2-R3-C0 Mutable-Worksite Causality
Design](./T287_W2_R3_C0_MUTABLE_WORKSITE_CAUSALITY_DESIGN.md) is now the frozen
atomic-window review candidate. Its earliest accepted predecessor bytes had SHA-256
`ccb19745eadca757db028cf0c5ded5d92d3507f65f0ba7189958984117ed475a`;
the later accepted worksite-root bytes had SHA-256
`427ced47826e95ef0262ca0d4797eaf144b07d14e30f8e4ace1de6b585864b84`.
The candidate retains those root semantics, makes its request carry full
authority basis and binding, resolves subjects beneath the canonical worksite,
protects installed/runtime roots, and requires zero Product delta. It clarifies
that substitutions visible at final pre-effect validation refuse and are then
followed by one same-directory atomic namespace publication or replacement.
Atomic means no partial successor visibility, not expected-inode CAS. An
ungoverned external or same-user target or parent mutation after final
validation and before the publication syscall is the distinct C0 atomic-window
P3/nonclaim. Prior `W2-R3-C0-I/E` proves one owner-scoped atomic file
replacement from stable
`WorkspaceBinding` plus current observation through receipt, exact-prefix ABG
admission, Event Calculus currentness, and fresh replay. It does not ratify the
parent R2/R3 roadmap.

[T-287 W2-R3-C1 Live-LLM Worksite Construction
Design](./T287_W2_R3_C1_LIVE_LLM_WORKSITE_CONSTRUCTION_DESIGN.md) is the
accepted reframed `W2-R3-C1-D` over predecessor bytes at SHA-256
`7db51dc50720bc36e729f4b17968c2e9c01d1f88ba673b10a2e165a3a31f98e1`. It
composes one caller-authored prompt through one governed `F_P`, validates a raw
worker-result carrier, deterministically wraps it with task authority, and
passes the admitted join result into one zero-binding `workflow.C`
vector-application child whose fan-out enters the exact C0 and reducer
children. Any declared command/probe execution and mechanical-observation
admission is a separate ABI C2 invocation through its `worker_executes`
Worker/helper and ordinary ABG admission. After replay, odd_glc owns only
interpretation of admitted observations, Reviewer return, and Executive
disposition through separate invocations; it does not execute those
commands/probes or synthesize observations.
The accepted design additionally carries and exact-prefix authenticates full `A`/`W`
in its task, copies full `A` into every C0 request, and derives each request's
W identity/digest only from full task `W`. Same-basis C3 nesting is
unchanged; mixed-basis branches refuse before dispatch. Its named realization
and proof surfaces remain bounded predecessor input and
its Product/Worker boundary is not widened by C2 or accepted C3. Accepted C3
I/E's graph-call C1 root child-closure declaration is the bounded
definition/digest amendment stated below, not a change to C1 meaning.

[T-287 W2-R3-C2 Worksite Command Execution
Design](./T287_W2_R3_C2_WORKSITE_COMMAND_EXECUTION_DESIGN.md) is the current
accepted reframed design. Its previously accepted two-manifest HOW at SHA-256
`f4eafb82961beb6ebe47fa54eb91dc7deb470c8fb106609f7d3d12ced76d074b` after two
independent `GO` verdicts with P0/P1/P2 all zero and P3=1 as the explicit
trusted-desktop nonclaim remains predecessor law except for root semantics
superseded by the accepted current design. That design carries full `A`/`W`, re-observes protected source
truth beneath `A.canonicalRoot` into the snapshot and after Worker return, and
requires the Program-owner installed payload to remain byte-exact. Its former
five-path I/E selection is historical. The current exact 19-path staged I/E
supersedes it. Artifact acceptance, package publication, qualification,
odd_glc/live-model use, version, RC, tap, and release remain unselected.

The retained two-manifest design conserves C2 execution-observation meaning and the exact
owner-derived C1-root or accepted C3-reducer source set while separating two
canonical sibling manifests under one attempt root. Public `task.json` is the
sole authoritative Product-task carrier, contains exact canonical task bytes
followed by one LF, and remains exactly described by the unchanged public
12-key helper plan's task-manifest path/digest/length. Occurrence-bound private
`launch.json` is retained only by the implementation-private launch plan. The
public tool command derives it structurally, never by parsing command text, and
exposes only
`node <installed ABI helper> --task <attemptRoot/launch.json>` to the Worker;
`--task` is a compatibility flag.

The host publishes and reobserves `task.json` first and `launch.json` last as
the readiness marker. Host, helper, and completion independently derive and
revalidate both distinct canonical single-link manifests plus exact result and
sandbox siblings. Partial publication fails closed without dispatch or retry.
The helper validates the bare task, private envelope, occurrence, attempt,
helper/package, and locus joins and requires result/sandbox absence before any
protected-O1, snapshot/result, probe, task-module, or declared-command work.
Successful result publication requires non-swallowed cleanup and final
single-link `result.json`; completion checks it again. Neither private launch
field nor artifact/sandbox path appears in Worker CLI or prompt prose. The
retained two-manifest relation adds no further private-launch field to Product
contracts; the root reframe does change the canonical C0/C1/C2 task carrier
shapes and digests by adding full `A`. Fully recomputed coherent same-user substitution
remains an explicit trusted-desktop P3/nonclaim. No odd_glc or live-model
consumption is authorized.

That C2 task/launch/derived-sibling P3 is distinct from the C0 atomic-window P3
above. C2 retains the exact installed helper beneath
`W.roots.toolchainRoot` and its helper/package/locus joins; the helper is not
rebased beneath the Program-owner `productRoot`.

## Pre-Baseline Canonical-Root I/E Territory (Historical; No Current Grant)

Stage 1 retains exactly these nine production paths. Their current bytes are
frozen on `HOLD` after two independent source reviews and may not change during
the C0 design reframe:

1. `code/src/product/worksite_effect.ts`
2. `code/src/product/worksite_operations.ts`
3. `code/src/product/worksite_construction.ts`
4. `code/src/product/worksite_command_execution.ts`
5. `code/src/product/worksite_branch_construction.ts`
6. `code/src/abg/execution_basis.ts`
7. `code/src/implementation/worksite_file_replace.ts`
8. `code/src/implementation/worksite_command_execution.ts`
9. `code/src/implementation/worksite_command_helper.ts`

Only after independent acceptance of the C0 candidate, a separately activated
owner-unity repair, and a new independent Stage 1 source `GO` may Stage 2 change
the eight support/evidence paths and refresh the two generated outputs:

1. `test_env/support/root-installed-environment.mjs`
2. `test_env/support/root-cli-environment.mjs`
3. `test_env/tests/t287-worksite-file-replace-owner.test.mjs`
4. `test_env/tests/t287-post-binding-worksite-write.test.mjs`
5. `test_env/tests/t287-worksite-construction.test.mjs`
6. `test_env/tests/t287-live-worksite-construction.test.mjs`
7. `test_env/tests/t287-worksite-command-execution.test.mjs`
8. `test_env/tests/t287-worksite-branch-construction.test.mjs`
9. `product-toolchain-manifest.json`
10. `contracts/capabilities/capability-definition-graph.json`

Owner unity means one unique admitted Product installation owns the selected
Program/GraphFunction publication and implementation binding. Their separate
publication digests need not be equal. Stage 2 remains blocked; if later
activated, it freezes one exact source/evidence/generated subject for
independent review and the ordinary package-verification gate. Product,
requirements, Public, event kinds, GTL, C3 topology, `package.json`, indexes,
generator source, design bytes, and every unspecified path are excluded. So
are odd_glc/live/model use, artifact acceptance, package publication, version,
qualification, RC, tap, release, and Git effects. The two selected branch
construction paths may carry inherited A/W propagation and regression proof;
they may not change accepted C3 topology or meaning.

The downstream reacquisition gate is prospective and unselected. Only after
the selected realization/evidence subject and one exact installed artifact are
independently accepted may a later Executive select the unchanged
`basic-cli` run first and, only after its complete admitted result and fresh
replay pass, the frozen `js-tenant-test` run. Each invocation projects full
`A`/`W` from the exact-prefix environment, resolves authored targets including
`package.json` beneath `A.canonicalRoot`, proves
`A.canonicalRoot != W.roots.productRoot == I_owner.installedRoot`, and leaves
the complete installed Product tree path-, topology-, and byte-exact. ABI C2's
Worker/helper plus ABG admission is the sole declared-command/probe execution
and mechanical-observation lane. Fixture roots, odd shims, scenario-specific
ABI branches, host command/probe execution, and synthesized observations are
prohibited. odd_glc only interprets admitted observations and owns later
Reviewer/Executive disposition. Historical `basic-cli` evidence earns no
`1/7` scenario credit.

The occurrence is an exact eight-key authority-free `LeafExecutionOccurrence`,
with C2 `programLocusRef`
`node://abiogenesis/worksite/command-execution/fp@5`, `executionAuthority: null`,
the retained scalar constraints, and no extra key. Host and helper independently
recompute `occurrenceDigest`, the exact
`worksite-command-attempt://abiogenesis/<lower-case digest body>` from
`sha256Canonical({cCallRef,runId,graphCallId,frameId,taskOrdinal,attempt})`, and
`attemptDigest = sha256Canonical({ attemptRef })` before effects. Current
Product construction, observation admission, completion, and `exactExchange`
also refuse a fully rehashed current observation carrying the immediately
preceding 12-key one-argument `--task <task.json>` plan. Historical validity is
available only under its exact immutable predecessor Product/artifact.

Prior accepted amended C2 design SHA-256
`f7075c286d33e6b95ae5a3481e5900e45052f65f8d434f6ee09039d29d7a12fe`,
earlier accepted design SHA-256
`7f69c4c0c5e027f4025c0f3885042c65a668b4089eb139d278c0cdbc78e4f570`,
and their installed coordinates remain predecessor evidence. A post-review live
transport check found one response-schema defect in the prior installed
package subject. The bounded semantic/carrier predecessor is
independently accepted at artifact SHA-256
`7272967ec7bed96612768b3ae7fe5e34382eca1127ce69a3e69a6fba9c119df1`,
manifest-file SHA-256
`c1dca319e995733db0debc68c7fe176433e7747ebbb6c11d9d3396d0d23fbdbd`,
Product-content digest
`a0236266e8c9d8c6654d33b8767ac4c21b1192c42f45b9bda17d6ae58afa3076`,
and C2-publication digest
`f7cb3acd366932f80881c24f6628f9462f0bf127ee726c2c0fedd7490506493b`;
independent final review returned `GO`, P0/P1/P2 all zero for that bounded
subject. Later live evidence exposed a P1 in its transport relay. The accepted
relay-repaired predecessor is independently accepted at artifact SHA-256
`d5278229916565777e4fd3e0c61d6000825ca944998f765dbf112584f36b3726`,
manifest-file SHA-256
`7f3be93f8a9c05ab60219637dcc2e9d7be073ad558dae2278f4a78410eb7c680`,
Product-content digest
`4c7c8509cb98f04a017ae79d2fe1b5500a9fc61bf8bd8c4883557a4a347bdc61`,
and C2-publication digest
`24336ffee58eedc639df04f8a95d6c5fffbf9207737a9026b0e46dc4d8c494bd`.
Independent review returned `GO`, P0/P1/P2 all zero on that exact subject. It
satisfies the historical relay gate but does not prove the current root design.
The fifth retained basic-cli run over historical artifact SHA-256
`e0bd55d90b2a2005c2021ba75f127a6a0402a7675994a757dd350b76e16cd449`
showed the permissive helper perform commands and write caller-moved
artifact/sandbox paths before Product returned `transport_identity_mismatch`.
That diagnostic evidence motivated the accepted two-manifest design. Current
authority selects only the accepted three-child worksite-root design's staged
19-path I/E.

[T-287 W2-R3-C3 Branch-Construction Aggregate Accepted
Design](./T287_W2_R3_C3_BRANCH_CONSTRUCTION_AGGREGATE_DESIGN.md) is the accepted
HOW authority for completed and qualified `W2-R3-C3-I/E`. It declares one generic ordered branch DAG
whose members are exact C1 tasks, validates dependencies and disjoint target
allocation before effects, serially traverses existing C1 children through
`C.batch` with explicit outer branch-vector/output-vector carriers and
`workflow.C`, marks its planning `F_D` non-result-bearing, preserves partial-
stop truth, and deterministically flattens a complete authenticated result
vector solely through its reducer into the existing C1 result contract for an
explicit C3-reducer source join into C2. Accepted I/E also adds and publishes
the graph-call-scoped C1 root `abg.child_closure_contract`; that bounded change
alters the C1 root GraphFunction and containing-publication digests and requires
direct C1 regression plus nested C3 proof. Independent review accepted the
exact design subject with P0/P1/P2 all zero. Its exact design SHA-256 is
`6da224353368e1ba9e2fc5651640ece743f6b528e98bbd9ccb58b8624a96e2a8`;
final evidence-test SHA-256 is
`5c04ad0bc0dc7b8caff32f5242d49040c7c2a5fd9acea88ce643265849a657c9`;
manifest, Product-content, Catalog, and C1/C3-publication digests are
`b7749e6d20a6c278ce5a42b4be728b86e15b5efbe56e732820cb92836f481140`,
`a0ff7fd2298698868d340146c7a739599ceda16a87ae07108112cf7493f30126`,
`d036a63e7e163246f750e94b0923305412be66f2848e4c5272f55040a8926b36`,
and `72a5e998592913bd3142e7a6d3064190f3799d0eb58c94343f39a10cd2c6d8fe`.
Independent source/evidence and exact-artifact reviews returned `GO`,
P0/P1/P2 all zero. Historical qualified artifact SHA-256
`e0bd55d90b2a2005c2021ba75f127a6a0402a7675994a757dd350b76e16cd449`
remains qualification evidence, not the subject of current I/E review.
Product goal closure remains open pending odd consumption.

Accepted Executive direction requires Wave 2 to force-rank the installed Public
family against the generic odd_glc contract class rather than pursue 56/56
callable closure. Except for accepted `W2-R3-C0-I/E`, the bounded C1
predecessor, prior accepted C2 semantics, accepted C3 I/E, and the current
frozen C0 candidate, accepted C1/C2 worksite-root HOW, and retained selected I/E,
R2/R3 remains unselected and must close:

- the existing URI-binding admission engine over Product-instantiated,
  GTL-defined types, with neutral fixture types in ABI acceptance and `A/B/C`
  instantiated only at the downstream odd_glc Product boundary;
- typed workspace admission through Catalog/View, `node_type` and `overlay`
  application, Program conformance, and exact GraphFunction/owner closure;
- a non-circular pre-binding admission-authority network;
- named and hierarchical odd_glc overlays as the higher-order reference-frame
  composition, with no ABI `ReferenceFrame` entity;
- one-start direct HoG traversal with ABG-owned evidence, continuation,
  terminal result, and replay;
- stable `WorkspaceBinding` authority plus mutable-worksite observation,
  owner-effect, successor-observation, ABG-admission, and fresh-replay
  causality; and
- an exact use-case force rank of `18` direct, `8` transitive, `15` negative-
  only, and `15` out-of-scope definitions.

`W2-R3-C0-I` is deliberately post-binding. It reuses an admitted call basis and
does not claim pre-binding authority, declaration application, URI admission,
`run.invoke#start`, complete traversal, or broad R4. Those relations remain in
the proposed parent roadmap and require their own accepted child before code.
The accepted C1 child is also post-binding and uses only
`run.invoke#invoke`; it does not close those residual relations.

The S1 evidence-DAG reframe remains a separate proof-HOW prerequisite. It does
not define typed workspace admission or functional traversal and cannot claim
Wave 2 Product progress merely by accepting or implementing its representation.

## Governing Truth

Read in this order:

1. `specification/GOALS.md`
2. `specification/INTENT.md`
3. `specification/PRODUCT.md`
4. applicable `specification/requirements/`
5. accepted functional designs indexed below
6. `ABI5_REALIZATION_CONSTITUTION.md`
7. the active ticket selected by GOALS

Specification defines WHAT. Accepted design defines HOW. Tickets order work.
Commentary, generated views, proof records, and rejected subjects are evidence.

## S1 v4 Accepted Design And Bounded Carrier Amendment

[T-287 ST-S01-ROOT/S1 v4 Artifact Carrier Design](./T287_S1_ROOT_ARTIFACT_CARRIER_DESIGN.md)
contains the complete accepted S1 v4 HOW and its accepted bounded amendment.
The amendment closes only the internal I1/I2 carrier handshake: the
driver retains the complete successful Cut-A output, projects its verification/
root-receipt pair into Cut B, and never supplies or convergence-compares the
Cut-A packed-snapshot coordinate there. That file alone owns:

- the one authored root carrier and acyclic identity relation;
- the narrow package carrier port and private interior;
- packed archive and raw installed-tree observation;
- static/runtime module closure and OCI confinement;
- proof identities, cut order, closed result union, and falsifiers;
- the strict Cut-A-to-Cut-B convergence projection and evidence-retention law;
- implementation file order; and
- the four-state delivery/archive protocol.

The accepted scope authorizes only ordered S1 implementation and proof. The
amendment grants the exact adapter handshake needed before E00. It changes no
Product or requirement, Public/runtime surface, carrier method count, result
disposition, or one-port boundary; it does not select S2 or any runtime/root/
release claim. It is retained predecessor law; the current worksite-root design
and I/E selection authorizes neither the adapter nor E00.

## Retained Accepted Basis

- [ABIogenesis 5.0 Realization Constitution](./ABI5_REALIZATION_CONSTITUTION.md)
  remains retained ABI-specific realization and predecessor evidence. The
  selected project frame basis imports its ABIogenesis/GTL/HoG/ABG atlas and
  instantiates the Definition-selected immutable STDO. Section 16.13 records
  the RC6 method reconciliation. Historical Section 16.12 selected only the
  then-current C0 mutable-worksite realization; earlier Product-specific
  relations remain predecessor evidence unless explicitly retained or
  superseded. Prior accepted exact C2 design SHA-256
  `f4eafb82961beb6ebe47fa54eb91dc7deb470c8fb106609f7d3d12ced76d074b` is a later
  bounded child under the same fixed Product/requirement authority. Its
  two-manifest law is retained, while the accepted current C2 design supersedes
  its worksite-root interpretation. The old exact 19-path staged I/E is retained
  predecessor scope, not current work authority.
- [T-287 Graph Catalog Contraction Accepted Design](./T287_GRAPH_CATALOG_CONTRACTION_ACCEPTED_DESIGN.md)
  remains the accepted one-Catalog realization. Catalog is outside S1.
- [T-287 Terminal-Quiescence Owner Network Accepted Design](./T287_TERMINAL_QUIESCENCE_OWNER_NETWORK_ACCEPTED_DESIGN.md)
  remains the accepted A5-F10 runtime-truth composition. Runtime is outside S1.
- Accepted M03 and M05 Sections 1 through 12 at S03 candidate `8865ccff` and
  [M05 S05 Consensus Global-To-Local Design](./M05_S05_CONSENSUS_GLOBAL_TO_LOCAL_DESIGN.md)
  remain predecessor design.
- [ADR-045 Global Design Constraints Survive Local Projection](./adrs/ADR-045-global-design-constraints-survive-local-projection.md)
  remains global-to-local rationale.

`A5-F12`, `ABG5-S04`, the S04 observer/tuner design, and ADR-047 remain planned
5.1 input, not ABIogenesis 5.0 implementation authority.

## Supersession

The accepted S1 v4 design supersedes prior development readings that:

- select the cumulative `ST-1` through `ST-4` MVP, C2A, W2-05, a 56-callable
  gate, or a horizontal sentinel as the next increment;
- treat an S1 development subject as `ABI5-ROOT-001`, `ABG5-S01`, a
  qualification subject, or a release subject;
- derive root selection from constants, fixture data, source/private imports,
  a caller receipt, a broad eager Product export, or a parallel selector;
- use system `tar`, `node:child_process` in Product verification, a rival
  installed verifier, ambient modules, or self-reported effect counters; or
- import Product resolve/install, Workspace, Catalog/View, Program/GF/
  `ExecutionBasis`, HoG, ABG, replay, SDK/CLI, or governor evaluation into S1.

The design does not supersede Product's root table, final one-family contract,
R1-R10 terminal predicate, accepted owner/runtime laws, or any scenario or
release requirement.

Rejected docs commits `6720c0d9`, `87d369d3`, `1e1e61af`, and `0d3c869f`
remain immutable evidence only. Their prose, ancestry, and implementation maps
are not carried forward by default.

## Document Ownership

- GOALS states the current WHAT and nonclaims.
- T-287 records current frame, order, gates, and status.
- This README routes design authority and supersession.
- The dedicated S1 design owns accepted v4 HOW; its amendment clauses own HOW
  only after exact independent acceptance and delivery.
- The typed-workspace roadmap owns the accepted R0/R1 force rank and records
  the proposed broad R2/R3 design questions. It remains unratified as HOW, does
  not supersede the accepted S1 design, and grants no implementation through the
  current bounded worksite-root I/E selection.
- Prior accepted C0 bytes own the exact post-binding file-replacement,
  authority-basis/root, ABG-admission, and replay relation. The frozen current
  C0 candidate clarifies only atomic namespace semantics and the distinct C0
  trusted-desktop P3 pending independent review.
- The accepted reframed C1 design owns only the generic raw-live-`F_P` result,
  Product wrapping, pure-child failure contract, and ordered C0-child
  composition plus exact authority-basis propagation through fresh replay.
  Declared command/probe execution and mechanical-observation admission belong
  only to the separate ABI C2 Worker/helper and ABG lane. odd_glc owns only
  post-replay interpretation, Reviewer return, and Executive disposition. C1
  remains the bounded construction predecessor. The prior accepted C2 sibling
  owns only exact snapshot command and probe observation through
  `worker_executes` and replay; its
  semantic/carrier and relay-repaired installed coordinates remain predecessor
  evidence. Prior accepted amended C2 design SHA-256
  `f7075c286d33e6b95ae5a3481e5900e45052f65f8d434f6ee09039d29d7a12fe`
  remains historical HOW. Prior accepted exact C2 design SHA-256
  `f4eafb82961beb6ebe47fa54eb91dc7deb470c8fb106609f7d3d12ced76d074b` owns only the
  distinct bare-task/private-launch manifest relation, one-argument
  Worker/helper locus, helper-derived private paths, completion currentness, and
  bounded falsifiers. It received two independent `GO` verdicts with P0/P1/P2
  all zero and P3=1 as the explicit trusted-desktop nonclaim. Its former
  `W2-R3-C2-I/E` five-path selection is historical. The
  accepted C3 child
  owns only ordered
  branch-DAG admission, serial traversal of exact C1 children, authenticated
  fan-in, flat construction-result reduction, and the explicit C3-to-C2 source
  relation. C3 I/E is completed and independently qualified. Product goal
  closure still requires downstream odd consumption evidence. The selected
  realization/evidence territory remains only the exact staged 19 paths listed
  above. Stage 1 is frozen on `HOLD` pending accepted C0 clarification and a
  separately activated owner-unity repair; Stage 2 is blocked. Product,
  requirements, Public, event kinds, GTL, C3 topology, `package.json`, indexes,
  generator source, unspecified paths, artifact acceptance, package
  publication, qualification, odd_glc/live-model use, version, RC, tap,
  release, and Git effects remain unselected.
- Git retains all other historical and donor evidence without making it current.
