# T-287 Declared Testing-Capability Reuse

## Status And Authority

One-pass design accepted by Executive after `/root/testing_reuse_review`
returned satisfied with no material findings or structural blocker in the
single independent design review. The reviewed substantive design digest is
`ed8ca92c894d91ec9a5eeceb04f2d977d84aa00c24f0f0d08f64deb457810012`;
`ABG5_TESTING_REUSE_IMPLEMENT_01` separately grants implementation. Intake is
`design_reframe` within the existing STDO environment Product,
F02/F04/F07/F10/F17; no upstream contradiction was found. No implementation or
native success is claimed. Whole-cycle deadline: **2026-09-16T19:40:31Z**,
including design, one review, implementation, verification and return. There
are no repeated local review cycles. First-cycle proof excludes live paid-LLM
campaigns and broad qualification. The frozen 1B correction is unchanged.

The subject is the live dirty ABIogenesis workspace at HEAD
`adaeede022a105cbc358799029d4e063cfa55426`, not HEAD alone. The
[activation](../../../../.ai-workspace/comments/codex/20260916_ABG5_TESTING_REUSE/activation.md)
and handoff bind this subject and the write grant. Authority is
`PRODUCT.md#stdo-governed-run-environment`, `REQ-L-GTL3-CONTEXT-008`, and
`REQ-R-ABG3-INSTRUCTION-ASSEMBLY-001..020`. Existing environment-pilot, D1,
C1 and C2 designs retain their unaffected contracts. This amendment covers
STDO declaration/assembly at the existing C1/C2 leaves and their exact exchange
identity checks; it does not change command, effect or acceptance meaning.

Operative method: `stdo://releases/v2.5.0-rc.7/`, manifest
`1f56029380604b0879fe322047fa8b38060297ba86b54bc8db8450d01ec034ae`, with
the exact nine-member Representation and seven-member Axiom installs selected
at `4824d5a05619e6957110b7ac97464b42ac84c096`. Executive verified these
before activation. The pilot's old RC6 wording remains historical.

## Reuse Decision

Required capability: give an actor the currently permitted checking operations,
current workspace and bounded proof task; let it select an existing operation
under declared policy; obtain actual evidence through native execution/replay.

Reuse `SemanticWorksiteBasis.commands`, `SemanticWorksiteDesign.commandRefs`,
worksite preparation and C2. No `TestingTool`, testing registry, new command
schema, new GraphFunction, controller or generic role engine is needed.
Another checker is another command declaration, not an ABG branch.

Bounded discovery inspected the live semantic-stage, native assembly,
preparation and C2 owners and focused tests at this source cut. C2 already owns
argv/cwd/environment, timeouts, reports, effect limits, observations and replay;
Node's existing test runner and tenant build/check commands supply checking.
A bespoke runner would reproduce these mechanics without their native joins.
External foundation replacement is not applicable to this bounded connection:
another checker cannot remove the missing ABI assembly relation. Predecessor
C2 evidence supports reuse only at its accepted scope. This is non-exhaustive,
not an all-tools superiority claim. A material command requirement outside the
existing carrier invalidates this selection.

STDO owns reuse/proof law. Use exact RC7
`STDO_REFERENCE_FRAME_BASELINE.md#derived-generic-specialist-frame-set`
(Reuse/Foundation and Proof), `#derived-product-testing-frame-set`, and
`DESIGN_MODULE_METHOD.md#capability-and-foundation-selection-stdo-up-023`.
RC7 has no dedicated Reuse/Proof index: supply those exact Context source spans,
not the T009 complete-update frame. `a_c` retrieves declared available indexes;
it does not apply axioms, choose a checker or accept a result.

## Existing Chain And The Missing Relation

1. Native semantic assembly already renders observed targets, declared
   commands, predicates and write territories. GTL `worksite_design` permits
   candidate `commandRefs`; the assessor independently judges the candidate.
2. `product/semantic_stage.ts::deriveSemanticWorksiteConstructionConfiguration`
   requires satisfied Design, sufficient dependencies, paired implementation/
   verifier obligations, and unique existing commands. It projects the exact
   selected command declarations in selected order.
3. `abg/semantic_stage.ts::projectSemanticWorksitePreparation` authenticates
   input, current authority/binding and physical observations. Existing GTL
   bridge -> C1 -> retention/preparation -> C2 carries the selected operation.
4. The existing C2 helper runs the manifest; its compact acknowledgment joins
   the actual full artifact, not a claimed test pass.
5. `projectSemanticEvidenceInput` joins admitted bridge, C1 reducer and C2
   results **in the same invocation**, then supplies actual output/artifacts
   to later native application assessment. Preserve
   `selectedSemanticPredecessor`/`sameInvocation` unchanged.

The gap is environment coverage. `gtl/stdo_run_environment.ts` currently
accepts only semantic author/assessor loci; C1 and C2 prepare separate native
worker requests, and `actor_process.ts` reconstructs semantic assembly only.
Separate STDO Design and standalone C2 invocations cannot satisfy the current
evidence bridge. Retain **one composed lifecycle invocation** through Design,
C1, C2 and assessment. Do not add a private cross-invocation conversion.

## Minimal Declaration And Assembly Amendment

Extend the existing role alternative only for the already published C1
`candidate` leaf (`constructor`) and C2 `command-execution` leaf
(`command_executor`). Existing semantic `author`/`assessor` meanings stay fixed.
Match exact admitted GraphFunction, locus, implementation and contracts, never
ticket names, checker names, prompt phrases or desired answers. Every F_P leaf
in an adopting Program still needs exactly one supported binding. Missing,
duplicate, mismatched and unsupported coverage refuses. Revision/forward/
recovery and other leaf classes are not newly supported. Non-adopters retain
their exact existing behavior.

Reuse frame refs, identified policy text, access refs and Context bindings.
Policy tells the semantic Worker to consider available checking operations and
the bounded claim/population, and to report insufficiency. It does not prescribe
a command id or answer. Author remains tool-free; C1 remains construction; C2
remains exact one-helper-call execution; evaluator retains semantic judgment.

Factor the common admitted-prefix/execution/graph/CCall/cursor authentication
already used by semantic assembly into the existing execution-basis owner.
Semantic authentication retains its lifecycle/source checks on that common
relation. Do not copy it into a second authenticator, registry or dispatcher.

The C1/C2 variant carries a subordinate immutable native-assembly basis on the
existing leaf occurrence: selected publication/graph, admitted execution,
CCall/cursor and predecessor prefix. It is a read model, not a new runtime
entity or grant. HoG carries it; ABG authenticates it; the existing
`instruction_assembly.ts` produces its existing plan/envelope/manifest/request.

Existing owner request construction supplies the base task renderer. Native
assembly includes the exact task text/schema/helper command, role/policy and
admitted STDO source/access content. Required content stays intact; unavailable
or mismatched content refuses. Existing declared access and transport bounds
apply, with no truncation or hidden defaults. Semantic policy interpretation
stays with actors. Stored declarations/content reproduce historical prompts.

Preparation and `actor_process.ts` must produce/reconstruct the **same final
request**. Do not append a prompt only at transport time. For C2, construct
the pure helper plan, validate assembly, then publish task/launch manifests and
dispatch. Missing binding/access refuses before dependent effects.

The exact exchange seams are material:

- C1 `task.promptDigest` continues to identify the original construction-task
  section. Adopting completion verifies the authenticated full assembled
  request and actual full prompt digest, not equality to unwrapped task text.
  Non-adopting C1 retains its existing equality.
- C2 task/attempt/helper and exact semantic tool-input identities stay fixed.
  Completion compares the final assembled request's plan/renderer/prompt
  identities and unchanged helper input. Its authority-free eight-coordinate
  helper occurrence is explicitly projected from the internal occurrence:
  validate the one declared assembly companion before projection and reject
  unknown extra fields. `executionAuthority` remains null. No assembly basis
  becomes a helper grant.

Use existing `actor_transport_binding_admitted.instructionAssembly` and replay
owner-fact projection. No new event kind, fluent, ledger, public operation or
replay API. Saved request/manifest disagreement refuses. Existing recovery is
not expanded; unsupported adopting recovery remains a truthful refusal.

## One End-To-End Walkthrough And Proof Population

Use a small ordinary consumer declaration over existing published lifecycle/
C1/C2 owners in an isolated proof workspace. Bind source task, independently
specified observations, two existing checker alternatives, executable/argv/cwd,
reports, dependencies, allowed writes and finite population prospectively.
Node's test runner over a declared verifier is sufficient; no new protocol.

Existing public admission supplies the actual source handoff and observed
worksite. Native Design author/assessor receive exact RC7 frame/policy and
current implementation/verifier/dependency bytes plus command alternatives.
A satisfied candidate selects A. The bridge rechecks physical currentness;
HoG traverses C1 and C2. C1 writes only declared targets; C2 executes A through
the existing helper and protected snapshot. ABG admits actual result/exchange
evidence. In the same invocation, the existing evidence bridge supplies it to
application assessment; declared consequence closes or truthfully blocks.
Fresh result/replay reproduces selection, command, observation, manifests and
disposition.

The bounded discriminators are:

- Missing command, implementation/capability, or required environment role/
  access binding: owning admission/preparation/assembly refuses before C2
  manifest publication or execution. An unknown selected command cannot run.
- Changed selection A -> B in a new exact invocation: native context, accepted
  Design projection, preparation and actual result all identify B, and A does
  not run. Old-digest argv changes or reusing A's evidence as B refuses. Prior
  evidence is immutable. A changed relevant file similarly needs its current
  owner observation or produces stale-basis refusal.
- Non-adopting control and fresh-process replay: original C1/C2 behavior is
  conserved; recorded policy/context/results reconstruct with access-tool and
  source roots unavailable. Tampered manifest/request support refuses.

First-cycle integration may use explicitly identified worker transport doubles
for candidate responses/helper invocation, but must traverse real GTL/HoG/ABG,
execute the actual selected checker, and read a fresh durable prefix in another
process. Fixtures never write ABG events or substitute successful results.
This demonstrates mechanics, not live LLM policy comprehension.

Live LLM UAT remains explicit and unexecuted, for a later bounded activation:
ordinary supplied information must suffice to select/reuse the checker and
assess actual evidence. No paid-model call or long existing pilot campaign is
selected in this first implementation cycle.

## Proposed Implementation Territory And Stops

After the single review, Executive may grant the following paths relative to
`build_tenants/abiogenesis/typescript/`; this is not a current write grant:

- `code/src/gtl/stdo_run_environment.ts`, `code/src/validator/validation.ts`:
  supported role coverage and matching diagnostics.
- `code/src/abg/execution_basis.ts`, `code/src/abg/semantic_stage.ts`:
  one shared native authentication relation with semantic checks conserved.
- `code/src/abg/instruction_assembly.ts`, `code/src/abg/index.ts`: the existing
  assembly owner's additional declared variants/exports.
- `code/src/implementation/contracts.ts`, `code/src/hog/ccall_lifecycle.ts`:
  subordinate basis transport, no new dispatch authority.
- `code/src/implementation/worksite_construction.ts`,
  `code/src/implementation/worksite_command_execution.ts`,
  `code/src/abg/actor_process.ts`: pre-effect final request preparation and
  rederivation, exact complete-exchange identity.
- New `test_env/tests/t287-declared-testing-capability.test.mjs`, optionally
  new `test_env/support/declared-testing-capability.mjs` for bounded fixture
  composition; existing `test_env/tests/t287-stdo-run-environment.test.mjs`,
  `test_env/tests/t287-worksite-construction.test.mjs` and
  `test_env/tests/t287-worksite-command-execution.test.mjs` for conservation.

Use tenant-root `npm run build` and `node --test` in an isolated candidate
copy: the normal build cleans generated output and must not overwrite the
canonical dirty tree. No dependency, lockfile, version, STDO, sibling,
retained-run or generic generator change. Another required production path
returns as an exact dependency instead of silently widening the grant.

Keep four claims separate: declared coverage/refusal; exact complete exchange;
real composed execution/evidence/fresh replay; live LLM usability (open).
Local assembly checks do not prove composition; doubles do not prove UAT.

Frames are RC7 Worker plus Design, Reuse/Foundation, Owner, Public Boundary
and Proof, conjoined with the local
`ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-end-to-end-interface-integration`.
One independent Reviewer returns this design evaluation to Executive.

Stop at the deadline, wider authority, loss of same-invocation or exact request
identity, or inability to fit the coherent increment. Return the exact completed
subset and dependency; do not claim disconnected execution as completion,
weaken the oracle or launch another review campaign. T-287 remains the schedule.
