# T-287 D4/D5: Native Assessment And Sole Qualification Verdict

**Status:** Accepted HOW. Implementation and execution require their separate
subject and grant. This design changes no WHAT and grants no runtime effect.
**Owner:** Validator.Conformance / AF-22 and existing native proof owners.
**Acceptance and exact source basis:** [Application record](../../../../.ai-workspace/comments/codex/20260911_ABG5_ACCEPTED_HOW_APPLICATION_18/accepted-subjects-and-reviews.md).

## 1. Product frame and exact gap

The selected fifteen-family Product / GOAL-035 / T287 and exact STDO 2.5.1 RC1 remain. Historical RC6 or fixed16 headings do not select additional 5.0 gates. F11 evaluates the exact candidate without exemption; F15 preserves one qualification family; F10 owns causal admission/replay; full F17/S06 remains mandatory behavioral coverage. F12/S04 stays 5.1. [SELF-CONFORMANCE](../../../../specification/requirements/product/REQ-P-SELF-CONFORMANCE.md), [QUAL-057/064A–C](../../../../specification/requirements/product/REQ-P-QUAL.md), the accepted [F11 HOW](T287_D4_SELF_CONFORMANCE_DESIGN.md) and [D6 HOW](T287_D6_RC6_RELEASE_IDENTITY_DESIGN.md) govern. The 2026-09-22 owner-authorized contraction reprices proof granularity while conserving these Product outcomes.

Product/law/rule catalogs, Programs, contracts, Context and AssetSurface declarations are immutable definitions. Assessment tasks and proof resources are supplied data until authenticated. J/O results and executable observations are immutable evidence; ABG alone admits their occurrences and causal state. Native qualification projections and published schemas are read models. No catalog, boolean, citation, digest, test count or successful generic F_P call certifies the Product.

The [frozen source and diagnostic basis](../../../../.ai-workspace/comments/codex/20260911_D4_D5_NATIVE_QUALIFICATION_HOW/revision-03/basis.json)
records predecessor implementation gaps and observations. Those records are
historical evidence, not a second current implementation-status surface.
This HOW supplies their native realization relation under the accepted
two-subject D6 law; schemas and published catalogs derive from that current
law, never a third qualification kind or a spelling alias.

## 2. Closed domain carriers and declared work

One authoritative `ExactCandidateQualification<K>` defines addressable `basis` and `verdict` projections. Subject kind is exactly `pre_rc_candidate | installed_rc`. The basis retains exact source inventory, Product/manifest/artifact/toolchain, native install/W, tenant manifest, QualificationLawBasis and frozen gate inventory. Its prospective release coordinate implements accepted D6 namespace/profile/subtree/version-line/ordinal/claim-byte law. It excludes future verdicts, its output snapshot and future human acceptance. A development version lacking that prospective identity remains non-green.

The following domain carriers have strict raw guards, constructive functions, native types and generated schemas from one owner. Canonical identities exclude their own identity fields; byte digests, typed-value digests and ABG result-body digests remain distinct.

| Carrier | Meaning and mandatory binding |
|---|---|
| `QualificationAssessmentTask` | Exact basis/law/catalog/inventory and selected rule/surface/claim pairs; Context members and source spans; AssetSurface output contract, renderer, proof obligations and authority slots; published assessment-role/independence requirement; criterion roster; actual prior evidence coordinates and explicit residuals. No result disposition is supplied as an expected answer. |
| `QualificationJudgment` | Native F_P assessment candidate with one declared role: catalog fidelity/coverage, inventory classification/coverage, tenant realization, rule applicability/sufficiency, or behavioral coverage and execution sufficiency. Each selected criterion has `satisfied | falsified | indeterminate`, source-grounded reasons, exact covered subjects/evidence and residuals. The native constructor, not the model, attaches input, producer, actor and transport coordinates. |
| `QualificationConstructionProvenance` | Closed `native_construction | external_construction` arms. Native construction cites actual producing occurrences. External construction binds the exact source inventory/candidate and relevant external STDO activation, author attribution, preimage, delta and closure records, their authority/identity correspondence and scope; it claims no historical ABG author event. Section 3 defines sufficient attribution and the conditional present F_H acknowledgment. |
| `QualificationOwnerRuling` | Typed response to an exact declared F_H request: authority, basis/law, governed decision slot, reason and retained obligations. This includes a scoped present external-attribution acknowledgment when needed; it is not retrospective construction authorization, a semantic assessment substitute or a MUST waiver. |
| `QualificationEvidenceSelection` | Requested evidence coordinates and one immutable pre-execution selection key from section 3: J task/assessment-or-peer slot, O request/decision slot, or AF22 episode/reducer slot, with exact role and coverage. Actual native invocation/Program/locus joins authenticate that key; a result or CCall ID cannot create a new slot. |

Reuse `ContextDeclaration` member/span identity and `SemanticAssetSurface` authority/output/proof roles. F11 publishes its own qualification domain contracts and renderers; it does not masquerade as a D1 worksite lifecycle. The existing lifecycle validator deliberately binds worksite construction/command roles and is not widened merely to fit this domain. The published role binding names the qualification role, allowed actual actor/profile and renderer, required independence relation and source authority. The admitted task selects that binding; it cannot author a new role or grant. Runtime actor/transport and invocation/grant facts must match it.

Publish qualification-bound GraphFunctions through the existing SYSTEM-owned conformance publication and normal Catalog/Program membership: `qualification_assess` (F_P), `qualification_ruling` (F_H), the retained `self_conformance` (F_D), and `exact_candidate_qualification` (F_D, the declared AF22 instance). Names below describe contracts, not new Public operations. Every function has exact input/output/raw-worker or interaction contracts, implementation/interaction bindings, contribution owner rows, reciprocal Program membership and meaningful step/closure predicates. Public `conformance.evaluate` remains its existing `gtl_program` member; invoke/start and result/replay carry these ordinary GraphFunctions. The 18/56 roster does not change.

GTL declares assessment work and dependencies. A bounded assessment may cover several explicitly listed rule/surface pairs with one valid same-basis J. Where context size requires partitioning, the declared tasks bind disjoint or explicitly shared coverage and a finite complete union; no prompt truncation, path heuristic or runner-created hidden partition changes scope. A root may compose declared children through the existing graph/C carriers. F_H is selected only for an applicable reserved decision, not universally inserted before every J. HoG traverses; the evaluator does not launch actors, select reviewers, retry or repair.

F_P uses the existing prepared-worker request, actor/process transport and probabilistic result admission. The qualification renderer binds the exact admitted task and full required source/evidence sections to its instruction/raw-output contracts. The response schema cannot encode a preselected green outcome. Source text is quoted data, not instruction authority. Raw result validation, the exact request/observed artifact relation and the constructive domain result relation all apply. No change to generic transport or the generic probabilistic verifier is required.

## 3. One native proof owner

`abg/qualification_proof.ts` owns the qualification-specific native join. It exposes closed projections for the current consumer basis, admitted J/O, execution evidence and complete F11 assessment and selected AF22 verdict. Validator owns their domain meaning. This module delegates native truth to existing prefix, invocation, execution, CCall, actor-evidence, F_H and declaration owners; it is not another event store, admission authority, actor loop, universal proof interpreter or release verdict engine.

The same native functions are called at dispatch, Product result/judgment validation and historical replay. The dispatch entry requires the real current predecessor prefix and selected-no-evidence CCall supplied by the leaf owner. Replay uses the recorded exact prefix, not today's mutable workspace or an ambient Catalog. The existing F11 special occurrence passage is extended only for the declared qualification implementations; an arbitrary implementation cannot request a qualification authority tuple.

The closed proof-resource input carries the actual durable prefix, explicit evidence selections and supplied immutable Catalog/View declarations needed for historical owners. Assessment Context supplies the exact source/law/member bytes or their existing manifest-bound immutable asset preimages; byte count/digest and source-span checks bind them to the admitted task and frozen subject, not an arbitrary current path. Missing bytes or an unjoined freeze/source relation is incomplete, never an assumed identical source. Required material is retained in admitted inputs or exact immutable proof resources for a fresh process. These resources are evidence, not a current Catalog overlay, imported event store or new public read protocol.

For J, selected execution evidence or F11's result, the join requires:

1. A validated native prefix with exact store identity and historical prefix coordinate. Every cited event is in that prefix and causally precedes its consumer. A preserved prefix may have a separately authorized append; a copied log is evidence bytes, not the same admitted store.
2. Actual invocation/Run, execution basis, Program, GraphFunction, materialized locus and selected CCall/fibre. Resolve supplied immutable declaration proof through the existing historical install/W/lock/Catalog/View verifier and exact Program closure. Root membership and child inherited subsets remain distinct. The historical owners must exist before the originating invocation; current broad Catalog membership supplies no missing authority.
3. The exact implementation or interaction owner and input/raw/output contract. Reuse `projectOpenedCCallCarrierAtPrefix` and `projectAdmittedCCallStateAtPrefix`; do not count raw-kind event values. Verify the result-body/value digests separately, result/judgment admission and required route/closed child or root relation. A valid negative assessment can have native result `success` and judgment `advance`: those mean the assessment completed, not that its subject passed.
4. For F_P, the actual admitted request, actor invocation/profile binding, observed artifact and closed transport/result evidence. Derive the domain wrapper from the admitted task and raw assessment. The artifact's claimed actor name, schema success, matching prompt hash, or a generic F_P output is insufficient.
5. Exact subject/law/catalog, Context members, AssetSurface, rule/version, selected surface/claim, evidence role and required scope. A narrower judgment serves only its covered pairs. Applicability and independence follow the source-bound role policy, never a caller's `independent: true` or a renamed actor label. The assessor is an actual admitted assessment occurrence with its evaluation grant. Construction provenance has two closed arms:
   - `native_construction`: resolve the actual admitted construction producer(s), their scope and actor/profile attribution; apply the required author/assessor independence relation to those native occurrences.
   - `external_construction`: bind the exact frozen candidate/source inventory to the relevant external STDO activation, attributed authors, preimages, delta and closure evidence. Native admission of this Context proves which records were supplied, not that their author labels or authority claims are true. Establish their attribution and identity correspondence from the sufficient source-grounded construction/grant record chain under the trusted-developer-desktop boundary, then compare the actual admitted assessor against that established attribution. No historical native author occurrence is required or invented. Missing, conflicting, unjoined or merely relabeled attribution blocks the dependent claim.

For insufficient external attribution, the declared `qualification_ruling` F_H request may ask the actual authorized source/Product owner to acknowledge the exact candidate and external record-set identities, attributed authors/identity correspondence and evaluation scope. Its closed response is `acknowledged | disputed | insufficient`, with reasons and evidence. Reuse the exact native response/actor/capability relation below; the current responding owner must occupy the declared authority slot. `acknowledged` records that present attribution act only: it does not turn external construction into past ABG events, retroauthorize it, waive independence or assert that qualification passed. The required independence comparison still applies to the established identities; disagreement or insufficient evidence remains blocked. Sufficient existing attribution needs no extra O, and no universal acknowledgment prerequisite is introduced.

For O, reuse `projectFhContinuations` and its exact Public response operation/capability join. Authenticate the request's CCall, response contract, actual responding actor/grant, response bytes, scope and basis at its historical frontier. `responded` or `resolved` may establish the typed act when its declared contract permits; an open, abandoned, superseded or conflicting continuation cannot. Do not require a fabricated successful Run to recognize an actual human response. Possessing a generic response capability does not extend the declared Product-authority slot. O can accept lawful re-entry or a justified bounded exclusion where law permits; it cannot discharge an unsatisfied mandatory obligation.

Selection is explicit native provenance, not chronology or Program-wide J counting. The admitted task/Program fixes each key before the producing effect:

| Selection family | Key and eligible-producer cardinality |
|---|---|
| J | Exact subject basis and law, admitted assessment-task identity/digest, declared assessment or independent-peer slot, role binding and exact covered rule/surface/criterion set; bind Program/locus and task ordinal/source position where the declared composition distinguishes occurrences. Require one eligible actual producer per required slot. Distinct partitions and declared independent-peer slots legitimately produce several Js in one Program, including peers assessing the same criteria. Their finite coverage and independence obligations remain separate. |
| O | Exact basis/law, F_H request/continuation identity and digest, and declared decision slot/scope. Require the actual eligible response for that request; distinct scoped requests are not competing rulings merely because they share a Run. |
| AF22 | Exact basis/law and admitted qualification episode/Program's sole declared reducer slot. Require one eligible actual AF22 producer for that episode; assessment-slot multiplicity does not multiply verdict authority. |

Resolve each result reference through the exact native producing CCall. Parent foldbacks are aliases only when the existing child/result/route relations join them to that producer. Several authenticated wrappers of one producer count once. Two distinct eligible actual producers for the same immutable selection key refuse even with identical values. A result/CCall ID, arbitrary new task label, after-the-fact coverage narrowing or role/peer renaming cannot split a predeclared slot to launder competition. A lawful native retry/supersession relation determines eligibility without erasing prior failures; first/last selection or value deduplication does not. Other historical episodes are not silently made current by choosing a result ref; their reuse remains explicitly bound by admitted selection and the owning applicability relation.

Currentness is exact-basis and owner-specific, not “most recent reviewer.” Reuse an earlier admitted J/O when subject bytes, law, input sources, role, scope, authority and required independence still hold and no native correction/supersession invalidates it at the consumer frontier. A role/context switch alone does not discard valid evidence. Changed subject/law or an affected owner invalidator blocks dependent reuse; unchanged hashes alone do not establish its admission. The selection retains earlier failures and re-entry pressure. No projection upgrades a failed command, transport failure or partial construction into successful evidence.

## 4. F11 finite evaluation and semantic responsibility

Preserve and complete the existing finite checks. Catalog source locators/spans must bind the actual Definition-selected law and published manifest asset. Inventory coordinates must bind exact immutable subject bytes and their selected roots; duplicates, undeclared members, missing declared members and crossed Product/install/W/tenant bases refuse. Catalog fidelity examines the selected governing authority population, not merely the subset the catalog author chose to list. A frozen candidate catalog's row count and source population do not establish completeness. Regenerated catalog bytes change their published identity.

The finite rule/surface matrix records every catalog rule and every applicable subject member through explicit classified coverage. For each pair, native C checks presence, uniqueness, exact coordinates, role compatibility and evidence scope. J establishes meaning, sufficiency and justified applicability/inapplicability. Unknown applicability is not omitted. Grouped J may cover an explicit set; a catalog-wide green flag cannot. A source-grounded coverage/classification assessment plus the mechanical inventory comparison addresses omitted applicable surfaces; filenames and `complete_claim` do not.

Tenant conformance checks exact capability graph/Public catalog identities, unique declared claims, owned capability/contract references and claim/evidence coverage. Existing raw/whole-Program/conformance owners compute their relations. Actual realization and proof adequacy remain the declared semantic responsibility; export presence, passing a schema or a contract count cannot establish them. The complete 18/56 export surface remains a conservation check, not the F11 verdict.

An assessment-required finding is discharged only by its corresponding
successfully resolved, adequate assessment; never by removing the check.
Result findings retain source/rule/surface, native evidence, C/J/O provenance,
diagnostics and residuals. Aggregate `failed` if any applicable obligation is
falsified or a required identity relation fails; otherwise `blocked_incomplete`
if any required evidence/applicability is missing, indeterminate or accepted
for re-entry but unsatisfied; otherwise `passed` only when the complete
applicable matrix is discharged. Justified inapplicability is limited by its
actual law and scope. F11 as a whole is not excludable.

F11 output remains `qualificationVerdict: false`. Native completion of a non-green F11 evaluation is a truthful completed result. Publication or catalog generation does not require F11 already green; the candidate's immutable inputs are frozen first and evaluated next. Evidence, reviews and verdicts produced by that evaluation are external records, not recursive inputs that must certify themselves. Preexisting proof/qualification/release-claim bytes within the candidate remain evaluated subject members.

## 5. Coverage and sole qualification verdict

The published `QualificationCoverageCatalog` contains source references and
retained behavioral obligations. It has no execution ordinals, graph/leaf
bindings, per-behavior owning result or mechanism/unbound inventory. Grouping
is declaration data, not authority to remove an outcome. Existing source spans
and independent catalog/coverage J determine fidelity and applicability.

The actual malformed-GTL and native-runtime assessment leaves remain. The
former uses `rawAdmitValue` / `validateProgram` against declared cases; the
latter authenticates actual input/implementation/result/judgment, terminal and
replay relations, including atomic, flat and C.edge role/locus/step checks and
same-final-value negatives. They return computable evidence only. Their former
second owning-result leaves and two-child wrapper Programs are removed.

`execution_selection` uses the existing proof resource and actual Result owner,
resolves truthful foldback aliases, exact Program/invocation/locus and installed
subject/W, and retains original Result/J/evidence as immutable assessment
material. Known malformed/runtime assessments additionally retain their exact
assessment producer/currentness checks. A caller's material or green label is
not native evidence. Evidence may be reused across coverage claims; no native
producer is rerun merely to mint another claim identity.

F11 conserves its existing catalog, inventory, tenant, rule, source, authorship
and independence checks. For each source-linked retained behavior, its existing
F_P criteria must judge `behavioral_coverage`, with exact catalog/source bytes
and authenticated execution material in the task. Criterion evidence refs must
resolve to that material. Required execution, positive/nearest-negative scope,
case adequacy and release applicability remain semantic judgments; a citation
alone never supplies them. Inapplicable scope requires the existing reason,
source and retained-boundary evidence. Unknown, missing or residual coverage
remains blocked; a falsified criterion remains failed.

`self_conformance_selection` authenticates the single whole F11 Result through
existing producer, occurrence, input, contract, advance and current competitor
relations. Its compact disposition/Result/bypass projection is data, not a
new executable gate. AF22 consumes it directly, preserves red/blocked and
blocks development/non-prospective bases. Only an exact ready green with no
bypass can authorize existing AF25. No per-row vector or second reducer remains.
The current source increment does not manufacture J, current execution
coverage, a release-ready subject, QUAL056 recipe completion or human acceptance.

## 6. Concrete source cone and integration dependencies

Paths below are relative to the TypeScript tenant. These owners realize the accepted relation under a separately scoped implementation grant; shared ownership is retained rather than copied into fixture logic.

| Existing or new owner | Bounded realization |
|---|---|
| `code/src/validator/qualification_contracts.ts` (new) | One qualification basis/verdict, coverage, native selection/proof-resource and J/O domain contracts/guards/constructors, including immutable selection-slot keys and the native/external construction-provenance sum. Move/re-export existing shared basis/law definitions rather than duplicate them. Implement accepted two-subject/prospective RC schema. |
| `code/src/validator/self_conformance_contracts.ts`, `self_conformance.ts` | Reuse the common family; complete finite checks and consume resolved J/O. Preserve old meaningful failures and separate F11 result. |
| `code/src/abg/qualification_proof.ts` (new), `code/src/validator/self_conformance_basis.ts` | One native proof projection using existing owners; replace the root/first-publication-row special case with exact declared owner selection. The old resolver delegates, not forks the proof relation. |
| `code/src/validator/qualification.ts` (new) | Domain assessment construction, exact task/evidence relation, coverage relation and sole AF22 verdict; no actor launch or admission. |
| `code/src/implementation/qualification.ts` (new), `code/src/implementation/self_conformance.ts` | Declared F_P preparation/completion and F_D owner seams. Domain rendering uses existing native request/transport machinery; no D1 envelope impersonation or transport fallback. |
| `code/src/validator/self_conformance_semantics.ts` | Exact new/old contract validators, task-aware worker contract selection, interaction response relation and step/closure predicates; dispatch and result validation reuse the native proof owner. |
| `code/src/gtl/self_conformance.ts` | Publish the domain carriers/assessment/F_H/F11/sole-verdict GraphFunctions and exact Programs, full reciprocal contributions, bindings and raw/output contracts. No new core GTL term. |
| `code/src/implementation/contracts.ts`, `code/src/implementation/leaf_invocation_port.ts` | Pass the current qualification occurrence to only its exact declared owners and verify their constructed result. Reuse generic F_P verification unchanged. No general new authority hook. |
| `code/src/validator/self_conformance_publication.ts`, `code/src/product/public_contract_publication.ts`, `code/src/{validator,abg,gtl}/index.ts` | Single native/schema asset publication and exports, including basis/verdict/coverage projections under the existing authoritative qualification schema asset. J/O are subordinate schemas and GTL contracts, not new Public members or a second qualification family; preserve 18/56 operation/member identities. |
| `scripts/generate-product-manifest.mjs`; new bounded `scripts/generate-qualification-rule-catalog.mjs`; `contracts/qualification/` and generated schema/read models | Reproduce current accepted source assets/spans and schemas from exact input law and types. These outputs are immutable publication data, not semantic certification or a rule interpreter. |

Unchanged reusable native owners include `abg/c_call.ts`, `event_prefix.ts`, `event_store.ts`, `invocation_admission.ts`, `invocation_execution_truth.ts`, `execution_basis.ts`, `environment_admission.ts`, `probabilistic_result.ts`, `actor_process.ts`, `fh_continuation_projection.ts`; Product's `reconstructHistoricalDeclarationCatalog`, `resolveExecutionDeclarationClosure` and exact contract selector; and R10's terminal projection. Reuse their actual joins. A needed change to any of these owners requires an exact additional scope return before editing, not a duplicate resolver hidden in qualification.

Integration carries the complete reviewed predecessor ancestry against exact
current canonical preimages, preserving all accepted owner relations at
overlapping paths. A shared ancestor or code equality does not establish an
integrated qualification subject. The [frozen source/integration record](../../../../.ai-workspace/comments/codex/20260911_D4_D5_NATIVE_QUALIFICATION_HOW/revision-03/T287_D4_D5_NATIVE_QUALIFICATION_SUPPLEMENT.md#6-concrete-source-cone-and-integration-dependencies)
retains the historical cuts, census, diagnostics and unintegrated state; this
design does not relabel those scopes. D6's accepted law/schema asset update,
complete independently assessed coverage, applicable failure/refusal qualification, F11 real-tree
and seed evidence, full S06 and one exact integrated D5 cut remain required.
Separately owned C2 continuation/refusal work has its own source cone. No
unrelated release code or Product work is selected here.

## 7. Finite discriminators and proof boundary

Every negative has a valid positive in the same claimed scope; an earlier blocked baseline gives no assurance credit to downstream mutations. Checks bind exact fixture and source/package/installed identity, native prefix, actor/profile, input/output contracts and oracle.

| Discriminator | Required observation |
|---|---|
| Legal bounded J/O -> F11 -> AF22 | Actual declared qualified assessment(s), any applicable actual F_H ruling, and same-basis finite checks produce their real native results; a complete genuinely green F11 coverage assessment yields exactly one native green/empty-bypass verdict and fresh typed result/replay equality. A smaller mechanism fixture is labeled as such, not full current-candidate qualification. |
| Seven F11 seeds | On separately identified exact mutant subjects: missing authority, broken traceability, unowned Public contract, design/code drift, malformed proof, ticket/closure mismatch, release identity mismatch each causes its owning source-linked non-green F11 finding and prevents release-eligible AF22. Restore the exact positive and re-establish it; seed names cannot be the actor's oracle. |
| Identity/admission | Wrong subject or law, changed catalog/source span, foreign install/W/Program, equal contract URI with different digest, forged result/value/event, future or unadmitted evidence and omitted native predecessor all refuse. A truthful failed command remains failed evidence. |
| Role/independence/scope | An ordinary successful F_P with no qualification role refuses; native or external self-review where independence is required, forged attribution, wrong actor/grant or identity correspondence, narrower or crossed rule/surface scope, unknown applicability and unauthorized O cannot discharge the obligation. Valid scoped/grouped J is reusable without a needless second semantic assessment. |
| Multiple legitimate J tasks in one Program | Two predeclared assessment partitions each produce one admitted J and cover the required union; separately, two declared independent-peer slots each produce their actual independent J over the same criteria. Neither positive is refused by Program-wide counting; duplicate or missing coverage remains governed by its declared policy. |
| Externally authored candidate, zero native author events | Exact external STDO activation/candidate/preimage/delta/closure evidence establishes the relevant attribution; an actual admitted independent assessor evaluates it without fabricated author events. Where that attribution is initially insufficient, an actual present scoped F_H acknowledgment can establish it, after which the independence check still applies. Wrong-candidate or merely self-labeled records, disputed/insufficient acknowledgment, forged authority or an assessor identified as a prohibited author stay blocked. |
| Currentness and ambiguity | Earlier still-valid evidence passes its exact reuse relation; changed basis or actual invalidator blocks it. Leaf plus authenticated foldbacks resolve one producer. Two distinct native eligible producers for one predeclared selection slot, including equal-valued ones, refuse; result-ID changes, after-the-fact task/peer splitting and raw event counts do not change that oracle. |
| Coverage and verdict | Missing/foreign/subset coverage, missing independent J, unadmitted or ambiguous execution/F11 producers, wrong Result digest and erased bypass refuse or remain non-green. AF22 conserves red and blocked; development/non-prospective basis cannot qualify. Two actual AF22 producers in the selected episode cannot mint a unique verdict. |
| Fresh reconstruction | Fresh supported installed result/replay reconstructs the same F11/verdict and retained failures from the real store plus exact immutable proof resources, without today's original source files, caller memory or event append on read. Historical prefix/identity and allowed later append are distinguished. Root/child contract ownership stays exact. |
| Actual clean-room semantic UAT | The fully integrated candidate is packed/installed source-blind and executes actual independent F_P assessments of the real full subject and all seven seeds, plus real reserved F_H acts where applicable. The complete independently assessed coverage includes full S06 and remaining Product obligations. Mechanical actors, supplied green JSON, hash checks or pure projector fixtures do not substitute. |

The earliest bounded installed discriminator follows lawful source and fixture readiness; exhaustive stable-owner permutations do not precede that composition test. Its first unexpected failure stops for the owning re-entry. Design acceptance alone does not close F11, D5, D6, an integrated candidate, semantic UAT, full S06 or release.

### Owned native runtime proof derivation

Native runtime assessment and F11 evidence consumers reuse immutable declaration, graph, admitted Result and completed source Run facts through the existing prefix derivation owner. Complete declaration/value equality is required for copied inputs; a claimed digest alone is never a reuse basis. Each current consumer still selects and authenticates every potentially eligible producer before deciding uniqueness. Ineligible calls are excluded using their admitted call/locus/result/implementation data before expensive reconstruction. A new eligible producer invalidates the previous unique selection. Historical assessment construction is reused only at its exact authenticated preparation cut; unrelated later activity does not require constructing it again. Changed Run membership, cold/copied/closed-owner coordinates and failed historical authentication retain their original reconstruction/refusal paths. These are realization rules, not additional qualification evidence, case adequacy, or completed qualification.

### Installed native proof owner handoff (OWNED-MODULE-CONTINUATION01)

The invoking ABG append/prefix owner retains its native proof operations across the installed implementation and declared judgment boundary. A separately loaded package module is not another owner of that session. The internal leaf passage carries a call-bound operation for the selected native assessment or owning-result realization; the judgment passage carries operations bound to that exact input/output/current-prefix evaluation. The selected installed implementation and semantic predicate still execute. Serializable native basis, Result bodies, declarations and Public contracts are unchanged.

Operations use the invoking owner's existing projectors and currentness/competitor predicates; they do not certify new evidence, confer admission, substitute another implementation, or share a registry by content equality. A different occurrence or input cannot reuse the leaf operation. Missing operations retain ordinary cold standalone reconstruction; copied, foreign, stale and closed-owner coordinates do not gain a live owner. Result cloning, foldback and successive prefixes preserve immutable facts through the existing derivation, while current invalidators remain current. Qualification schema construction may share its immutable recursive definition; accepted values and diagnostics are unchanged.

AF22 uses this same operation at realization, candidate validation and judgment. Its authenticated F11 relation and deterministic verdict belong to the exact immutable preparation cut and admitted input, retained by the existing native proof derivation. The current owner reidentifies that cut after Result cloning; it never authorizes a lookalike coordinate. A changed input/cut must establish the relation anew. Direct F11 and AF22 result projections, and native runtime source/assessment peers, exclude other admitted invocations before reconstructing their declarations; every eligible same-invocation competitor remains subject to the unchanged uniqueness check. AF25's AF22 projection reuses the same historical verdict relation and still checks current producers and terminal truth. Standalone, raw and closed-owner paths keep full cold authentication.
