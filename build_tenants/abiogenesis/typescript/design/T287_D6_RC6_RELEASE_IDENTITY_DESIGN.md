# T-287 D6 — RC6 Release Identity And Native Publication

Status: Executive-accepted release-only Product/requirement/HOW reconciliation; native implementation and qualification remain open. [Executive acceptance](../../../../.ai-workspace/comments/codex/20260911_D6_RC6_RELEASE_RECONCILIATION/application-03/executive-acceptance.md).

## 1. Frame, Authority And Conserved Scope

This `product_reprice` reconciles only ABIogenesis 5.0 release identity with the
selected immutable STDO `v2.5.0-rc.6` `RELEASE_METHOD.md`, especially Release
Identities, Phases, Immutable RC Publication, Exact-Cut Qualification and
Monotonic Version-Line Advancement. Its frame is the RC6 Derived Worker,
refined by ABI Product/Qualification/Release/Proof and
[F-WORKSITE-CAUSALITY](ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-worksite-causality).

The owning WHAT is Product F15/F16/S07/completion 9/13/14; QUAL-050,
057/057C, 067–070B; PUBLIC-CONTRACTS-006A; POLICY-059;
SELF-CONFORMANCE-008; and SCENARIOS-014 in this same candidate overlay.
Product fixed16, F12/S04 at 5.1, GOAL-035/T-287 D1–D6, all owning qualification
gates and the complete F17/S06 mandatory-application witness remain. Downstream
beta evaluation and a separate Product's release follow their own exact
development-Product selection and gates; neither substitutes for nor becomes
an extra ABIogenesis release gate.

A Source Project is mutable construction, not the released Product. A Release
Cut and its Product inventory are immutable. Installed Product and
WorkspaceBinding identities are native admitted facts. Qualification bases,
vectors, verdicts and F_H responses are immutable evidence whose admission and
causal/current relation ABG authenticates. Snapshot/acceptance manifests and
release evidence queries are subordinate projections. Git refs and artifact
writes are physical effects owned by the release Product function; none of
these categories creates another runtime, evaluator or event ledger.

## 2. Assigned Identity And One-Cut Lifecycle

| Coordinate | Exact rule |
|---|---|
| Project Release Namespace | `abiogenesis`, assigned prospectively by Executive under the Product owner's release direction; not inferred from folder/package spelling |
| Source carrier | `/Users/jim/src/apps/abiogenesis`, Project Subtree `.`, tracked `stdo_abiogenesis.json` source_project `./`; revalidate actual Git root/tree before a future effect |
| Ref profile | RC6 single-independently-released-project unqualified profile; immutable annotated `v5.0.0-rc.<n>`, mutable annotated selector `v5.0.0` |
| RC ordinal | Positive integer greater than every preserved published RC on this project/version line; no ordinal is allocated by this document |
| Product/package | Existing Product definition and package identity; each qualified artifact already carries its prospective `5.0.0-rc.<n>` identity and is not rebuilt or renamed at acceptance |
| Exact cut | Namespace/profile, immutable ref and annotated object, peeled commit, repository tree, subtree root/tree, exact Product member inventory/digest and release-claim bytes, artifact set, predecessor/successor dispositions |

The selector denotes the highest published RC ordinal, even while that RC is
unaccepted. It is discovery, not an exact qualification, dependency, evidence
or replay identity. Human acceptance and consumer adoption neither move it nor
create a `5.0.0` package. A convenience release branch, if explicitly selected,
tracks the same latest published commit. If the source gains another
independently released project or historical refs conflict with this profile,
publication stops for the RC6 namespace/profile re-entry; it does not rename or
rewrite historical cuts.

```text
exact pre_rc_candidate + law + complete owning-gate vector
  -> sole native C.of(AF-22) verdict
  -> AF-25/published_rc: unchanged RC + output-only snapshot + selector advance
  -> fresh installed_rc + law + complete applicable owning-gate vector
  -> sole native C.of(AF-22) verdict
  -> actual human Product-authority acceptance of the SAME unchanged RC
  -> AF-25/tapped_release: external same-RC acceptance/evidence addendum
  -> terminal post-publication same-Product installed proof
```

The qualification family has only `pre_rc_candidate | installed_rc` subjects.
There is no final-only delta, third qualification subject, second final cut,
retag or final-version assignment. Any byte capable of affecting the Product
or a release-scoped claim requires a higher RC and its applicable gates. The
complete vector and sole AF-22 reducer remain required; an affected-gate rerun
is not permission to omit another required gate or silently reuse its old basis.
Exact existing evidence is reusable only when its owning gate establishes its
applicability to the new subject.

The pre-RC basis binds prospective identity and already-built Product/claim
bytes, not nonexistent future tag objects or its own output snapshot. Actual
tag objects, publication receipts and the output snapshot become publication
evidence and bind the later installed-RC subject. Release notes and any claims
intended to belong to the cut are frozen qualifying inputs; future acceptance
or post-publication success is not falsely asserted in those bytes. Later
acceptance, review and install evidence lives outside the immutable inventory.
Acceptance is not a prerequisite to installed-RC qualification; that verdict
is evidence required before acceptance. Post-publication evidence cannot
retroauthorize either publication or acceptance.

## 3. Native Qualification And Human-Acceptance Joins

AF-25 receives exact refs/digests and supplied immutable proof resources, not
caller-authored green flags. Its Product owner reads a closed native prefix
through the existing ABG prefix/invocation/execution/result owners. It requires
one actual selected AF-22 producing CCall, exact declared qualification
implementation and historical installed declaration owner, invocation/Run,
Program/locus, input basis, result contract/value and successful judgment/route.
The admitted output must be the sole verdict over the exact subject, law and
complete vector with green disposition and empty bypass set. A wrapper or
foldback of the same admitted producer is provenance, not an additional
verdict; two competing admissible producers refuse rather than choosing first,
last or equal-valued output. A local hash over a claimed verdict proves no
admission. AF-25 does not rerun, reinterpret or reduce gate results.

The release owner consumes D5's one qualification projector/contract. D5 must
first supply the native AF-22 verdict relation required by QUAL-064A–C; its
absence is an implementation dependency and a typed release refusal, not an
invitation to put a replacement reducer in AF-25. Existing ABG
`event_prefix`, `invocation_execution_truth`, `execution_basis`, CCall and
replay owners retain their validation scopes. Historical declaration proof is
authenticated through the existing installed Product/Catalog/Program owners;
today's mutable source, broad Catalog lookup or a caller-supplied owner list
does not establish authority for the historical CCall.

The release-owned proof join is factored once and reused by both AF-25 members
and `project.read#release_evidence`. It composes those native owners and checks
release-domain equality; it does not copy their prefix, declaration, gate or
F_H algorithms. The local claimed `ReleaseQualificationVerdict` packet does not
remain a second independently authored qualification family.

Same-RC acceptance is an actual Product-authority human act after the required
qualification, independent reviews and operator checks. A release-owned closed
F_H request binds the complete RC identity, installed-RC basis/verdict/law and
required assessment evidence. Its closed response chooses `accept | withhold`
over that exact request digest and records the actual responding authority.
The declared GTL F_H boundary and existing `interaction.respond` admission
mechanism own this act; AF-25 does not solicit or synthesize it. The native
`projectFhContinuations` and `projectFhEffectfulPublicInvocationFacts` relations
authenticate the request/response contracts, CCall/basis, opening and response
events, Public response admission, actor/capability and causal order. Product
release policy then requires the actual human authority selected by the
release grant; an agent/reviewer decision, self-declared actor label or
unjoined approval receipt does not satisfy it on the trusted-desktop boundary.

An exact `responded` or `resolved` acceptance remains usable without inventing
a second Run closure requirement. A missing, withheld, abandoned, superseded,
foreign-subject or wrong-authority response cannot produce an accepted result.
The release function may report `withheld` through its refusal disposition;
there is no default approval. Reusing an existing valid same-RC acceptance is
evidence reuse, not a new human act or permission to change that RC.

## 4. AF-25 Members, Resources And Physical Boundaries

The existing `release.snapshot` members keep their public identities and
Product authority. Their owner contract packets change content digests when
their input/output/resource schemas change; the old second-cut packet is not
silently accepted as the new packet. The roster remains 18 operations/56
definitions. CLI and SDK serialize the same current DefinitionCall result;
there is no release-specific Public dispatcher, private helper result or new
Public member.

Both members declare module-static strict resource-assertion and receipt
contracts under the existing three-combinator DefinitionCall ABI. They read
the exact native evidence prefix with its real store/path/physical identity and
expected frontier, existing invocation actor/capability authority, selected
ProductSet/dependency lock/WorkspaceBinding and immutable artifact/declaration
proof. A supplied location is a locator, not proof of bytes or authority.
Evidence reads preserve the store identity and prefix; neither member appends
release events or imports another history. Evidence consumers use the native
owner projections, not a second release event parser.

`published_rc` additionally owns one explicit release-effect grant: exact
source carrier and clean qualification subject; prospective namespace/profile,
ordinal and immutable/selector/optional branch refs; remote identity and
expected ref objects; verified artifact sources; empty versioned snapshot
target; and bounded output/receipt locations outside the cut. No ambient
repository, current branch, default remote, broader filesystem root or mutable
selector can fill a missing resource. Artifact/package creation belongs to
candidate construction before qualification; publication only verifies and
materializes those exact qualified bytes. QUAL-051–056 snapshot inventory,
checksums, clean-source, identity, empty-target and non-bypass laws remain.

The native Product owner:

1. Authenticates qualification and the granted effect resources before effects,
   compares every qualifying Product/claim member, and revalidates namespace,
   profile, local/remote ref objects and greatest published ordinal.
2. Verifies the exact committed carrier and subtree against the qualified
   Product subject. It creates the annotated immutable RC and annotated selector
   over the same commit, and materializes the output snapshot without rebuilding.
   The grant lists every carrier/RC/release branch it permits to publish.
3. Publishes the selected ref set atomically where the transport supports it.
   Expected-object guards constrain mutable-ref advancement; immutable refs
   are create-only. With a non-atomic transport, the owner records each actual
   effect and does not report complete publication until exact reacquisition
   proves the whole required ref set and highest-ordinal selector relation.
4. Reacquires remote annotated objects, peeled commit/tree/subtree, artifact
   bytes, snapshot inventory and checksums. Only exact completed relations
   produce a complete publication result. A higher RC with an old selector is
   an invalid partial state, never permission to call the older RC latest.

`tapped_release` instead requires the exact published-RC receipt/artifact set,
native installed-RC qualification and actual human acceptance. It verifies
that those Product and claim bytes remain unchanged and writes one
content-addressed acceptance/evidence addendum outside the Product, snapshot
and release-claim inventory. It does not create/rewrite a tag, branch,
selector, package, snapshot or manifest in the cut. Its complete result reports
the same RC's existing cut/artifact/snapshot identities and exact addendum.
Neither member starts a hidden qualification or human-interaction loop.

## 5. Results, Partial Effects And Reacquisition

The owner output is a closed sum, transported without Public reinterpretation:

- `complete`: member-specific exact RC/artifact/snapshot identities, authorizing
  native basis/verdict/law, evidence/provenance and no unresolved effect;
  `tapped_release` also carries actual human acceptance and the external addendum.
- `refused`: failed exact relation, evidence and phase, with a receipt proving
  no publication/addendum effect in this invocation. Missing qualification,
  withheld acceptance, stale refs and wrong identity remain non-green.
- `incomplete_effect`: phase, actual known effects and their identities,
  explicitly unknown effects, exact predecessor/frontier, failed relation and
  residuals needed for reacquisition. It is a refusal-bearing owner outcome,
  never a complete or green release claim.

The strict owner refusal/receipt contract represents `incomplete_effect`; no
new generic outcome kind, runtime event or admission authority is required.
The result body digest and artifact/value digests remain distinct. Receipts
record only observed effects: per-path content/physical identity, tag/ref
objects and remote observation when known, or explicit uncertainty when not.
After any possible publication, later checksum/read/close/report failure must
retain that possibility and known effects; it must not return the effect-free
arm or lose earlier receipts. Evidence capture uses the declared output and
existing resource receipt, not a shadow success ledger.

There is no distributed all-or-nothing claim across Git, files and evidence
stores. Partial local/remote publication or an addendum write followed by a
fault stops the invocation. A later separately granted owner call first
reacquires exact existing state against the preserved receipt; it does not
blindly replay writes, force an immutable ref, downgrade the selector, delete
residue, fabricate a no-effect refusal or choose a new ordinal automatically.
Exact completed output may be recognized as existing evidence; a partial
snapshot cannot pass the empty-target creation gate by being called a retry.
Fresh-process reads reconstruct from immutable artifacts and native evidence,
without today's original workspace or a process-local success cache.

`project.read#release_evidence` remains one derived Product projection of
authenticated publication, installed qualification, actual acceptance and
post-publication evidence. It distinguishes published/unqualified, qualified/
unaccepted, accepted and incomplete effects without moving a selector or
creating qualification truth. Acceptance evidence is not consumer adoption.

## 6. Existing Reality And Minimal Later Realization Cone

Current `product/release_snapshot_operations.ts` uses local request-value
checks, includes the second-cut request, declares both success schemas as
`v.never()` and deliberately returns refusals. Its binding has null resource/
receipt contracts and rejects any success result. Current `release_evidence`
projects refusal to `not_ready`. The existing qualification basis schema still
lists three subject kinds; native overall verdict/vector reduction and the
release effect owner are not supplied by those files. This candidate removes
the governing contradiction; it does not implement or qualify these gaps.

The later isolated source cone is:

| Owning path, relative to `code/src` | Bounded change |
|---|---|
| `product/release_snapshot_operations.ts` | Same two members; new closed native proof/resource/result/refusal contracts and Product member semantics; remove old second-cut input, no compatibility reinterpretation |
| `product/release_snapshot_definition_bindings.ts` | Bind those exact static owner contracts through the existing ABI; preserve exact outputs and all receipts |
| `implementation/release_publication.ts` (new physical owner) | Explicit scoped Git/artifact/addendum effects and reacquisition only; no gate evaluator, CLI orchestration or event writer |
| `product/release_acceptance.ts`, `gtl/release_acceptance.ts` (new domain contracts/publication) | Closed same-RC F_H request/response and declared graph using existing HoG/ABG interaction semantics; no new Public operation or human-response runtime |
| `product/project_read_ports.ts`, `product/project_read_operation_contracts.ts`, `product/project_read_definition_bindings.ts` | Existing release-evidence member projects the owner's authenticated result/receipt and external addenda |
| `validator/self_conformance_contracts.ts` and the D5 qualification owner | Remove the third basis kind; share the one native AF-22 contract/projector. Completing the D5 vector/verdict is D5 work, not an AF-25-local substitute |

Module indexes, schema/publication generators and package manifests receive only
mechanical exports/digests for these owners under the later grant. Existing ABG
prefix/execution/CCall/replay/F_H and Product install/declaration/conformance
verification are reused, including exact historical scopes; no generic owner
is relaxed. If a necessary authenticated join or receipt cannot be represented
there, realization returns the exact missing relation before editing another
owner. No new runtime event or parallel release controller is selected.

## 7. Required Discriminators And Stop Boundary

The decisive installed witness uses one complete exact candidate and real
native AF-22 verdict, publishes one RC unchanged, fresh-installs and qualifies
that RC, records actual human acceptance through its declared F_H contract,
and reports that same RC through `tapped_release` and fresh release-evidence
reads. Remote tag/selector/artifact and native result/replay identities agree;
two fresh processes do not change history or Product bytes. Acceptance causes
no second artifact/cut/version or selector movement. A post-publication fresh
install is an addendum, not a retroactive prerequisite.

Required bounded negatives include wrong/forged/competing AF-22 producers;
wrong subject/law/vector or nonempty bypass; pre-RC verdict relabeled installed;
wrong or absent actual human authority, withheld/superseded/foreign acceptance;
same URI with changed digest; a qualifying byte change attempted on an existing
RC; stale ordinal/ref/selector; historical-profile collision; write failure
before effects and failure after each possible publication/addendum effect;
and missing/crossed partial receipt on fresh reacquisition. Equal-value
competing owners refuse; foldbacks of one owner do not manufacture ambiguity.
No negative may be credited without its valid positive owning baseline.

Qualification still includes every fixed16 gate, complete F17/S06, S01 R1–R10,
conservation, Consensus, F_H, Codex and self-conformance obligations. Mechanical
checks, this HOW, a source review, a bounded installed test or release tooling
presence cannot close D5/D6. This candidate returns for independent review;
it authorizes no implementation, publication, human acceptance or 5.0 closure.
