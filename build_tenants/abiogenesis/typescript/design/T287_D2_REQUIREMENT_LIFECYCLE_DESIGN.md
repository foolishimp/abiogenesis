# T-287 D2: Evidence-Bound Affected Lifecycle Successor

## Native phase-qualified intake and correction (NATIVE-D2-01)

Executive selection under T287/GOAL035 and T043 is a bounded design reframe of
this existing D2 relation. ABI's selected method is exact STDO 2.5.1 RC1; GLC
retains RC4. The historical design dispositions below remain historical. This
increment changes no Product family or Public operation and does not establish
installed qualification.

An ordinary caller supplies only the original public Run `{ref,digest}` and
its durable prefix to `program://odd-glc/native-semantic-revision/intake@5`.
The declared F_D intake uses existing ABG prefix, Run, execution-basis,
Result/J, workspace and native-observation owners to authenticate one eligible
cause and one successful advancing semantic predecessor from that Run. An
absent or ambiguous cause/parent refuses; chronological latest is not a
selector. The original admitted job, source, oracle and root remain exact.
Rejected candidate/assessment bytes are counterevidence, never the advancing
parent. Lifecycle declaration bytes must equal the selected publication's
lifecycle, even when its declaration reference has the same spelling.

When the installed setup changes W0 to W1, the ordinary caller first reads
`run_status.executionBasis` for the exact source Run, then submits the existing
`witness.admit/reprice` under W1. Its authority_basis subject and basis context
are that same authentic root; typed content/evidence retain the exact W0 -> W1
relation described below. Setup binding alone is not a cover. No diagnostic
child coordinates or inferred path correspondence are caller inputs.

Acquisition may add this root to its existing historical child/native-basis
cover set only after the unique eligible parent and cause both rehydrate through
the semantic-job ancestry owner to that source Run root. Every ancestry step
keeps its invocation, Program, root implementation set and historical W; the
original job/root and lifecycle remain separately authenticated. The unchanged
cover owner still requires actual witness and binding admissions, actor/current
authority, exact W evidence and event ordering. An unrelated root or matching
path does not suffice. Missing coverage and other first failed intake relations
use the existing semantic_stage_failure failureClass/diagnosticRef channel.
These diagnostic names confer no authority; current source invalidation and
physical context correspondence remain mandatory after coverage.

Before construction, a refused native assessment supplies the actual rejected
candidate and assessment; no future worksite, construction origin or target
selection is invented. After construction, an eligible failed command or
assessment must additionally resolve its real native construction source and
current observed context through the existing owners. A native observation
never becomes a fabricated C0/C1 result. Intake checks the current workspace
and admitted binding correspondence and observes current context; later
consumers use that one admitted acquisition and exact referenced leaves within
its basis, without repeating failure discovery.

A separately admitted preconstruction operational arm may use a lifecycle
author or assessor's genuine undispatched preparation failure. Existing CCall
outcome authentication retains the sole undispatched evidence, blocked J,
exact input, implementation, role/locus, frame and order; the diagnostic is the
existing typed native instruction-assembly refusal for that stage and role,
not arbitrary thrown text, dispatched actor failure or semantic falsification.
An author's input equals the unique earlier advancing assessed predecessor.
An assessor's input equals the unique earlier advancing authored Result of
that declared stage: its final asset retains exact candidate bytes, original
author provenance and assessment:null, with all prior assets satisfied. Both
retain the same source Run and original job/root/lifecycle. The operational
alternative is only that failed stage and role; all obligations survive and no
unsatisfied assessment is invented. Missing/ambiguous producer, crossed input,
wrong locus/order/root, stale context or absent binding cover refuses.

F_D derives the entry role from those authenticated facts, never actor prose.
The existing terminal-readable request's stage selectionChoice may include
entryRole: author or assessor; historical absence means author-first only.
Admission regenerates this role and the declared suffix projection checks it.
The raw selector gains no role choice. Assessment-first derivation preserves
the exact pending authored asset separately from unaffected accepted
preservedAssetRefs; it does not call that candidate accepted. Its whole
published suffix starts with the existing assessor, then follows ordinary
later stages/construction/C2/Evidence under HoG. A non-satisfied assessment
stops before effects; no caller stage loop, repeated author, local resume
controller or output salvage is introduced. This applies Product S03/S06 and
CONTINUATION-001/004/008/014 without a new Product or Public operation.

The existing F_P selection receives authenticated alternatives, complete
source, current context, obligations and counterevidence through a deterministic
decision view of the same admitted subject. The assembly owner renders each
distinct accepted/rejected asset, full assessment and observed byte body once;
local presentation references preserve repeated uses, not new authority.
Exact UTF-8 text replaces base64 display through the existing byte-identity and
round-trip check; non-UTF8 remains explicitly unavailable as text. An observed
byte carrier is reached only through its existing typed owner slot; opaque
application values, including carrier-shaped JSON, remain verbatim. An observed
native candidate file may present its exact candidate value once only when
strict JSON parsing proves that value equal; original byte/path/provenance
coordinates and full admitted carriers remain unchanged. Parent/cause C/J
outcomes, eligible stage/rubric/obligation/target domains, phase, historical versus
current observations and binding identities remain explicit. The plan/manifest
identify the decision view and its material references; they do not claim a
full-carrier echo. Ordinary source and task data remain available; separately
owned evaluator-only job data remains withheld from the selector, with its
omission explicit and the original job identity preserved. Declared STDO
policy/sourceContent remains unchanged. No
caller summary, truncation or failed transport output becomes selection truth.
Its semantic-refusal preconstruction
arm selects only an existing stage at or before the refused stage, with no
target references; the operational arm selects only its actual failed stage; postconstruction also admits construction repair and exact
historical Design paths. F_D request construction binds the admitted selection
and intake Results into the existing terminal-readable SemanticRevisionRequest.
Its `selectionChoice` contains the admitted `mode`, `selectedStageRef` and
owner-derived operational entry role when present, so
the caller can select the exact declared suffix from the serialized terminal
value and public Program data alone, even when several stages were eligible.
Request admission regenerates that projection; suffix derivation compares it
with the referenced admitted selection as well as checking the declared entry.
Changing the returned choice creates no selection authority. Historical requests
without this optional field retain their existing input behavior; a caller
cannot infer a missing choice through the new declaration lookup.
No caller extracts private leaf ids or writes a proof packet. Selection and
execution are two ordinary invocations: the second starts a declared whole
suffix. Its projection refuses a start whose entry differs from the admitted
mode/stage. HoG traverses the suffix; no caller stage loop is introduced.

Stage author/assessor presentation also conserves each typed byte body once:
observed file identities retain original encoding/digest/length and a verified
text view, without also displaying base64. Opaque application JSON is untouched.
Revision task metadata references the one task historical-assets array and,
only in the job view containing obligations.retainedTerms, its identical complete
retained-terms array through the existing local reference/digest form; full
admitted envelopes remain unchanged. Distinct accepted and rejected semantics,
full source, required inventory and role policy remain present. The original
assembly byte bound and required-content refusals still apply.

D2 retains unaffected accepted assets, original source and oracle, historical
counterevidence, grounded terms, outstanding obligations and binding versions.
The selected semantic stages use the existing D2 author/assessor owners.
Application correction uses native construction restricted to affected paths,
existing C2 over all declared target/dependency snapshots, then fresh Evidence.
An Evidence-only semantic re-entry retains its authenticated prior native
observation and re-evaluates that evidence without granting construction writes.
Old workspace semantic files remain historical when superseded by current
admitted D2 semantic assets; the native constructor receives that distinction.

Current execution limits are selected at the implementation intake boundary
and retained in its operating context. They are passed explicitly to the one
existing Product budget calculation for new revision author/assessor contracts
and construction readiness. Original job bytes and historical folds are not
enriched or reinterpreted; this also covers an old job lacking the optional
limits field. Changed selected controls refuse new construction preparation.

The exact native D2 graph factory owns its two retained-input edges. Existing
ABI contract, implementation and semantics owners authenticate that relation;
a marker alone is insufficient. STDO role admission recognizes only the exact
existing D2 selection/author/assessor bindings, using the existing assessor role
for selection and Program-scoped context declarations.

Source/component readiness is separate from a real correction. Successful
cases supply no invented failure. A budget-only retry is not consequential
application correction. The prospective bootstrap source additionally needs
its exact historical lifecycle selected unchanged; this increment neither
migrates old carriers nor admits a lifecycle-definition successor.


## Proposed generic-job binding conservation

Status: Executive-accepted at the ABI5_GENERIC_JOB_DESIGN_01 freeze after the same
single independent review as the [D1 generic-job arm](T287_D1_REQUIREMENT_LIFECYCLE_DESIGN.md#proposed-generic-job-binding-reframe)
closed satisfied. ABI5_GENERIC_JOB_IMPLEMENT_01 authorizes implementation;
this design acceptance does not establish runtime qualification.
Current ABI authority is RC7; GLC retains RC4. The inherited RC6 observations
below remain historical provenance, not this activation's selected method.

### Same job, immutable source, explicit binding successors

The canonical admitted job value and original root-input identity introduced by
D1 are conserved through D2. Source bytes, task, independent evaluation criteria
and owner scope are not rewritten in a revision envelope. Current worksite
observations, affected assets and authorized operating-basis transitions are
separate values joined through the existing admitted parent/cause/selection
relations. Neither equal source text nor equal file bytes permits borrowing
another job's results. No new job registry, current-pointer store or continuation
authority is introduced.

The live product/semantic_revision.ts merge currently keys bindings only by
obligationRef and rejects unequal values. That preserves old values but cannot
represent an assessed null-to-bound refinement or a later lawful binding
successor. Keep its refusal of conflicting history; replace only the job-bound
projection with two separate operations owned by Product and authenticated by
abg/semantic_revision.ts:

- retain the complete immutable term/obligation and binding-version history,
  deduplicating only an identical version ref with identical bytes; and
- derive the unique active version per obligation from the exact admitted
  predecessor/supersession chain using the same D1 projector.

Each version identifies the job, stable requirement and paired obligation,
ordinal, exact predecessor ref/digest, installed template/contracts, policy and
shape instances, and introducing assessed native asset. Initial null remains
an open version. A successor must name the current predecessor, advance once,
retain source grounding and pass the installed instance constraints and fresh
independent assessment. A skipped parent, fork, same-version changed bytes,
foreign source/job, or a numerically newer but unadmitted candidate refuses.
Legacy fixed bindings remain on their existing arm; no synthetic version is
used to weaken their equality.

Historical obligations remain visible even when their introducing asset is in
the invalidated suffix. Current construction uses only active admitted versions;
historical policy instances never become current merely because they are still
carried. A binding-version change invalidates dependent Design, preparation,
realization/proof interpretations and assessments through the declared dependency
closure. Unaffected assets/bindings remain exact and are not regenerated for
convenience. An unchanged implementation may be reused only where the existing
native preserved-result relation proves that reuse; this is not a digest-only
permission to reuse proof under changed meaning.

### Existing correction, not a new response/resume workflow

The selected demonstration is one within-job failed-verifier correction:
an actual admitted C2 or assessment failure supplies the cause; the existing
native selection/ruling chooses construction repair or stage revision under
declared policy; the corresponding existing D2 graph runs the affected suffix.
The host may submit the existing authorized correction request but may not
choose local stage calls, synthesize selection/evidence, or convert the failed
result into an advancing predecessor. No changed-greeting addendum or 5.1
human-response resume is required.

A construction-only defect with unchanged requirements, bindings and Design
does not mint a new binding version. The S/V/R law remains: selected writable
targets S are a subset of the current dependency/proof snapshot V, and R is
read-only. Preserve exact nonselected files, assets and binding versions;
re-observe every required dependency and obtain fresh C2/Evidence. If the
observed failure demonstrates that Design or binding meaning must change,
take the existing stage-revision route and independently assess that change
before constructing it. Unknown cause/dependency is a native block, not
permission to retry with a wider territory.

New assessed file paths use the same D1 target-formation and
[C0 parent prerequisite](T287_W2_R3_C0_MUTABLE_WORKSITE_CAUSALITY_DESIGN.md#proposed-assessed-file-parent-prerequisite);
they cannot be added by editing a carried target array. Existing directories
are retained; created-parent facts survive a later failure. An operating W/basis
transition remains available only through the already declared owner-witness
relation. It does not mutate the job's original input or silently cross
workspace authority.

Changed owner source, task or oracle is outside this same-job correction.
It requires a new ordinary authorized invocation/successor basis with a new
job identity and an explicit relationship to the prior subject where supported.
The old job and oracle remain immutable historical evidence. No automatic
cross-job asset/proof reuse is selected, and no new interactive response/resume
mechanism is needed: this repair must at least refuse in-place source/oracle
substitution. A later request requiring more successor semantics re-enters its
actual owner rather than overloading D2.

### Bounded implementation and proof

Proposed changes are product/semantic_revision.ts and abg/semantic_revision.ts,
with matching closed-contract/forwarding changes in gtl/semantic_revision.ts
and implementation/semantic_revision.ts only where required. Reuse the D1
projection, existing evidence/cause and preserved-result owners. No event-store,
traversal, scheduler or independent coverage store is selected.

Extend existing t287-d1-semantic-stage, d2-bounded-repair,
d2-evidence-owner and d2-binding-transition tests at the affected relation.
The cheap matrix covers initial-null to assessed-bound; valid successor;
duplicate same-version equality; changed duplicate; skipped/forked parent;
foreign-job/source/policy; stale current observation; preserved unaffected
assets; and selected-scope invalidation. A native failed-verifier correction
then proves actual affected writes, preserved nonselected bytes, new C2/Evidence
and fresh replay. Source/oracle mutation must refuse rather than pass by
rehashing an envelope. These are future checks, not completed evidence.

## Preserved predecessor design and bounded observations


The D1-CONTEXT-HOW-01 context/Consequence amendment is Executive-adopted under
[the bounded adoption record](../../../../.ai-workspace/comments/codex/20260913_ABG5_PLAN_EXECUTION/context-how-review-01/disposition.md).
Adoption covers the changed clauses, not every inherited candidate or an
implementation/qualification claim.

**Status:** Writer design candidate; Executive acceptance and implementation are separate.
**Owner:** ABG semantic lifecycle admission, with Product construction and GTL/HoG traversal owners.
**Re-entry:** `design_reframe`, T-287 D2 / GOAL-035; no requirement reprice.
**Frame:** [ABIogenesis project frame](ABI5_PROJECT_REFERENCE_FRAME_BASIS.md).
**Exact authority and observation:** [basis](../../../../.ai-workspace/comments/codex/20260909_D2_LIFECYCLE/basis.json).

## Product Outcome And Scope

The fixed 16 ABIogenesis 5.0 families remain selected; F12/S04 remains 5.1.
This component serves F02/F03/F04/F07/F10/F17: actual admitted execution or
assessment evidence can cause an appropriate bounded correction, preserve valid
work and outstanding obligations, and obtain fresh evidence on the same app.
It neither closes S06 nor implements all D3, proof-depth or release obligations.
D1 delivery/application/S06 remain open; its accepted source review is closed
and its current installed Run proceeds independently. W1/W2 remain banked.

Owning law is [proof carry-through](../../../../specification/requirements/abg/REQ-R-ABG3-REQUIREMENT-PROOF-CARRY-THROUGH.md)
001–015 and 022–030, [continuation](../../../../specification/requirements/abg/REQ-R-ABG3-CONTINUATION.md)
001–014, Product and the operative [Realization Constitution](ABI5_REALIZATION_CONSTITUTION.md).
The exact Definition selects STDO RC6; the independent GLC Product retains RC4.
The [D1 HOW](T287_D1_REQUIREMENT_LIFECYCLE_DESIGN.md#role-scoped-context-over-conserved-native-inputs)
owns the shared role-context projection. Its same-byte historical/current-binding
tail is not generic invalidation.

GTL declares stage dependencies, callable work and permitted traversal. HoG
traverses those declarations. ABG alone admits result, judgment, successor and
continuation truth. GLC supplies domain interpretation, rubrics and composition;
it cannot select runtime currentness or maintain a competing coverage ledger.
Every new value below is subordinate immutable declaration, evidence or derived
projection of these owners, not an independent runtime entity or store.

## Exact Existing Boundary

Candidate10 manifest `afdc1644c31a43098ccfd6a87f0c492b0086f033da1ca9a1061d3d03f36e6a86`
is the code observation, not a claim of D2 implementation. Paths below are under
`build_tenants/abiogenesis/typescript/code/src`:

| Existing native relation | Reuse and limit |
|---|---|
| `abg/semantic_stage.ts::projectSemanticEvidenceInput` | Authenticates bridge, C1/C0/C2 and stored snapshot bytes against retained source/Design. It does not decide affected re-entry. |
| `abg/c_call_outcome.ts::projectCCallOutcomeReceiptAtPrefix`, `admitCCallResult`, `admitCCallJudgment` | Own historical result/evidence and judgment truth. Preserve a failure as failure; neither its bytes nor a new digest make it an advancing semantic predecessor. |
| `product/semantic_stage.ts::deriveSemanticAsset`, `deriveSemanticAssessment`; `abg/instruction_assembly.ts::constructNativeInstructionAssembly` | Author/assessor computation is append-only for a new stage and one assessment. Existing-stage replacement and reassessment refuse. |
| `abg/semantic_stage.ts::semanticEnvelopeMatchesAdmittedPredecessor`, `semanticInputMatchesBasis` | The entire carried envelope and each asset must match native admitted history. A caller cannot splice retained assets into a revised envelope. |
| `hog/route_proposal.ts::proposeGraphSpanReentryRoute`; `abg/traversal_route.ts::admitTraversalTransition` | Existing declared `re_enter` and admission preserve source result/judgment, target/input and locus. They require a lawful advancing selection result, not advancement of the failed subject. |
| `abg/traversal_route.ts::rehydrateConstructionIntentForCursorAtPrefix`; `abg/invocation_admission.ts::admitExactInvocation` | Existing action/intent and invocation joins retain selected obligation/assets and exact authority. Durable gap-start re-entry additionally requires its actual gap-stop route and unchanged applicable start/install/binding coordinates. |
| `abg/continuation.ts::prepareContinuationPublicOperation`, `prepareFhInteractionResume` | Rehydrate a real held F_H continuation and its admitted response. No terminal/blocked Run is relabeled as held or gap-stopped. |
| `product/semantic_stage.ts::deriveSemanticWorksitePreparation`; existing C1/C0/C2 | Construct, replace and execute within current Product authority. D1's coordinate projection deliberately retains old preimage bytes, so it cannot refresh changed postimages for D2. |

The missing join is exact admitted counterevidence plus a permitted affected-work
selection to an ABG-admitted lifecycle successor and current worksite inputs.
The existing route, evidence and worksite owners remain authoritative.

## Select The Owning Re-entry

| Evidence and sufficient selection | Resulting work | Preserved meaning |
|---|---|---|
| Construction/compile/verifier-execution failure with an admitted determination that the governing stage remains applicable | `construction_repair`: repair selected realization/verifier artifacts using current postimages and the existing stage instructions. | All semantic assets remain valid; a failed candidate alone does not invalidate Design or requirements. |
| Admitted interpretation that actual evidence falsifies or leaves insufficient a stage's realization/proof instructions | `stage_revision`: re-author and independently assess the selected declared stage, then its affected declared dependents. | Source requirements stay fixed. Preserve prior assets/evidence as history; withhold reliance only where affected. |
| Ambiguous cause, dependency, scope, authority or affectedness | `blocked_incomplete` or an existing declared F_H decision path. | Neither silently rerun every stage nor assume that no stage is affected. |
| Actual owner changes governing requirement meaning | Separate owning requirement/Product re-entry first. | This component cannot manufacture the ruling or rewrite source meaning. |

A nonzero command result, transport failure, criterion label or supplied Boolean
cannot by itself select semantic invalidation. C computes exact known facts,
declared dependency closure, evidence roles and currentness. J establishes
source-grounded adequacy/affectedness where interpretation is required. O retains
reserved scope or meaning decisions. The selection carries the actual admitted
J/O coordinates and their subject, authority, scope and reason, not just citations
or self-authored premise flags. A fully mechanical classification may use only
its exact declared finite relation and sufficient admitted facts.

The two retained D1 Design proof limits are possible future triggers, not observed
C2 failures. The live result selects whether either is relevant. The first D2
witness may repair construction or revise any declared stage as warranted; native
code contains no Design, Requirements, Data Mapper or ACC-specific routing names.

## One Subordinate Successor Relation

Introduce a native D2 projection through ordinary declared GTL leaves and current
ABG admission. Its inputs are a closed `SemanticRevisionRequest` and a typed
`SemanticRevisionSelection`; these names denote subordinate contract values.

The request binds exactly one actual prior semantic result ref/digest and its
CCall/judgment/prefix, the unchanged source/lifecycle/publication identities, a
nonempty exact set of counterevidence coordinates, and current worksite authority.
Counterevidence identifies its producing CCall, execution basis, result/evidence
admission and declared role; physical residue names the actual owner receipt or
typed post-publication failure plus a fresh Product observation. Raw log paths,
worker prose or reread mutable files are not admitted evidence substitutes.

A selection has exactly one mode, selected declared stage ref when applicable,
selected obligation and target refs, reason/evidence refs, and the admitted
assessment or ruling establishing its scope. Required stage/target membership,
paired role/policy/shape joins and selection authority are checked before it can
be used. It is produced under an exact declared F_P/F_H result contract where
judgment is needed and admitted by the existing result/response owners. Its
successful admission means that the selection is lawful; it does not change the
failed subject's disposition. Missing selection realization is explicitly
unavailable until that declaration/implementation is installed.

`projectSemanticRevision` resolves these coordinates at the actual current native
prefix and reconstructs the selection and cause. It rejects foreign, missing,
stale, duplicate, spliced or out-of-scope evidence. A failed/blocked assessment
is admissible as counterevidence through its own outcome relation, never through
`semanticEnvelopeMatchesAdmittedPredecessor`'s advancing-history relation.
Existing public source-result admission also retains its own eligibility rules;
this component does not weaken them to transport a rejected result.

For `stage_revision`, let A be the selected stage and the transitive dependents
in the exact lifecycle's declared `predecessorStageRefs` relation, including any
consuming construction/evidence stage declared by that composition. Known
asset/obligation/target evidence dependencies contribute the affected evidence
set. Cycles, dangling dependencies, ambiguous membership or incomplete material
dependency declarations withhold the successor; computation does not guess from
text or file names. J/O establishes material scope when the declarations alone
cannot. For `construction_repair`, A is empty; affected artifact and execution
proof references still lose current reliance for the selected repair.

The result is one `SemanticRevisionEnvelope`: a closed native contract carrying
an immutable revision-basis coordinate and a derived active `SemanticStageEnvelope`
view. The basis identifies the parent result, cause, selection, computed affected
set, preserved set, current observation basis and selected work contract. Each
ref/digest is recomputed from canonical content, excluding its own identity.
These are proof of derivation, not caller-editable currentness flags. Native
history is resolved from its admitted references; do not duplicate whole history
into a writable ledger or accept a caller-supplied list as preservation proof.

The old D1 envelope contract remains an unchanged closed arm. D2 uses a distinct
closed revision input/output contract and wrapper arm; there is no optional
field/default upgrade of old evidence. One generic author/assessor/bridge
implementation may consume either declared arm, but preserves its arm and exact
revision basis through assembly, result admission, evidence projection and replay.
Distinct root input/output-kind declarations and existing root/workflow gates
remain intact; internal C<A,A> composition does not waive wire-contract direction.

The D2 active view retains exactly the unaffected current assets, in declared
stage order, and excludes affected stage results from current reliance. The four
original assets remain immutable native history even if one is superseded.
Replacing a stage creates a new asset/assessment occurrence; it never rewrites
the original ref, source, judgment or assessment. Fresh assessment is independent
under the existing actor/result contract. No historical failure gains pass status.

Obligation availability is the read-only union of original source bindings and
all admitted retained/discovered terms and bindings on this predecessor lineage.
The active stage view is not the obligation inventory. Removing an affected asset
from active reliance cannot erase the obligations it disclosed: those remain
pending with original role/policy/shape and source coordinates until lawfully
resolved. A new stage can add grounded obligations; it cannot replace this union
with a smaller set. Conflicting meaning is explicit pressure, not a changed
identity with silent retirement. This realizes conservation for the selected
successor, not general requirement completeness or proof-depth closure.

## Current Postimages, Repair And Fresh Evidence

The revision owner resolves the current admitted invocation/grant and Product
workspace observations for the same retained physical app. Reuse the existing
subject, territory, observation and construction-task constructors. Refresh bytes
from actual postimages under their real observation/receipt identities; do not
reuse D1's old-byte `projectSemanticWorksiteCoordinates` as a refresh operation.
A partial C0 commit remains real residue. Read and reconcile actual owner receipts
and current observations; never pretend rollback, atomic whole-app replacement
or runtime admission of an unadmitted physical commit.

For construction repair, preserve the prior selected semantic instructions and
map their targets to current observations by exact canonical root/path, role and
permitted territory. Translate target coordinates mechanically; do not edit the
historical Design asset. For stage revision, the newly assessed selected stage
names current typed targets. Both paths require paired selected obligations and
resolved non-null contracts/policy/shape, retain explicit snapshot dependencies,
and refuse broader target, command, write or capability authority. A changed
command/territory outside the selected authority requires its owning re-entry.
Every retained dependency outside the selected repair must match its last valid
admitted content basis. An unexplained change is additional pressure, not proof
that unaffected work survived; unknown affectedness withholds the dependent
claim. Selected changed bytes bind actual receipts/post-publication residue and
fresh observations, never a caller-digested postimage invented from the old task.
The same physical app does not imply the same stale observation or binding.

Native assembly projects the sufficient role-dependency closure of current
postimages, exact cause/selection, original source, conserved obligation union
and applicable retained predecessors under the D1 relation. The complete native
envelope and obligation lineage remain conserved outside that prompt view. Invalidated assets are labeled historical context, never
current proof. Oracle/evaluation-only information remains assessor-only; cause
feedback may explain failure without handing expected answers to authors.

The admitted revision bridge input/output supplies the new C1/C0/C2 lineage.
Fresh evidence projection follows that bridge and the actual current producer
chain; after writes it uses admitted receipts and stored snapshots, not a check
that old on-disk preimages remain unchanged. Fresh semantic evidence cannot reuse
invalidated verifier execution or the old failed assessment as its proof.
Unchanged evidence may be reused only when its exact declared dependencies and
currentness still hold. Final gap projection remains non-closing outside the
bounded repaired claim and retains all pending/discovered obligations.

## Traversal And Minimal Realization

A declared composition may obtain/admit selection, project the revision input,
execute the selected work, and assess fresh evidence through normal C/HoG/ABG.
It may use an existing declared `re_enter` relation within a live graph, or a
separately admitted successor Run with explicit parent/cause linkage when the
old Run is terminal. The latter is a new invocation, not reuse of its old
Continuation aggregate. Actual F_H holds and gap-stops use their existing exact
continuation/re-entry paths only when their conditions are present. Fresh next
work retains `evaluateNext -> admitConstructionIntent -> invokeGraphFunction`
where required by the selected composition; no Public shell or prompt chooses it.
The first bound composition must declare one of these paths; this HOW does not
claim the running D1 Program already declares a D2 branch.

| Owner territory | Minimum extension |
|---|---|
| New `product/semantic_revision.ts`; existing `product/semantic_stage.ts` | Strict request/selection/revision carriers, pure active-view/dependency/obligation conservation and current-target projection. Retain the old D1 arm. |
| New `abg/semantic_revision.ts`; existing `abg/semantic_stage.ts`, `abg/instruction_assembly.ts` | Actual cause/selection/historical-owner admission joins; regenerated successor, assembly and result; revision-aware bridge/evidence lineage. Read-only replay reconstruction uses the same relation. |
| `gtl/semantic_stage.ts`, `gtl/semantic_stage_identity.ts`, `gtl/semantic_stage_publication.ts`; `implementation/semantic_stage.ts` | Closed typed selection/revision contracts and ordinary leaf/publication bindings, with explicit root direction, owner, raw-result contract, child closure/failure and unchanged D1 identities. Reuse native author/assessor/bridge rather than a stage controller. |
| Existing `abg/c_call_outcome.ts` and native leaf occurrence/assembly interfaces | Only the required revision-basis/result-admission propagation. Preserve failure classes and producer authentication; no new event family. |
| Existing Product observation/C1/C0/C2, HoG route and ABG invocation/continuation owners | Reuse current APIs. Extend only a demonstrated missing coordinate; their authority/gates are not relaxed by this design. |
| GLC existing lifecycle declaration factory/HOW, under separate activation | Bind the selected domain decision contract and permitted correction composition. No GLC runtime, ledger or source rewriting. |

The new APIs above are required realization, not existing callable claims.
Exact dependency completeness, admitted selection, current postimages and a
bound correction composition are mandatory inputs; their absence returns typed
`blocked_incomplete`/`selection_required`/`evidence_unavailable`/`basis_mismatch`
or the existing owner refusal. Material governing ambiguity returns to its
actual owner. There is no new catalog, compiler, scheduler, process-local runtime
truth, automatic repair authority or general invalidation framework.

## Current Role Context And Ordinary Local Consequence

Apply D1's one finite role-dependency algebra to author, assessor, construction
and correction-selection contexts. Its selection does not change this design's
S/V/R worksite partition, observation origins, whole-binding cover, physical
guards or immutable historical source. R is read-only context even when its
content influences the result. Relevant current specification/design/code/test/
output observations use the existing owner-admitted carriers, not just the
selected write targets. Missing required read content is insufficiency; selecting
its context does not add it to S or to a command's write territory.

Compare each relied-upon result/assessment with the exact source, declaration,
obligation and observation basis on which it was admitted. Preserve its original
subject, role, scope, independence, evidence and owner ruling. The declared
dependency closure determines which support is affected by an observed change;
semantic materiality beyond that finite relation requires admitted J, and a
reserved change of meaning/scope requires O. A changed read-only dependency can
invalidate a claim even though S and W did not change. Conversely, unrelated
changed content does not reopen a still-supported stage. Unknown affectedness
withholds only the dependent use; it never proves either validity or invalidity.
No accepted J is recreated merely because the actor's conversational context
changed; author J cannot substitute for an independent evaluator.

The current `SemanticRevisionSelectionInput` carries only parent and causes;
`currentWorksite` first appears in the later revision request. Consequently the
existing selector does not yet receive newly observed file bodies. Where a
selection depends on those bytes, the bounded realization must carry the existing
`SemanticWorksiteBasis` through a strict selection-input arm and authenticate it
with the same native parent/cause/currentness relation before F_P dispatch.
Until that join is available, return insufficiency; a later projection or old
target names cannot retroactively supply the decision's missing context.

A fresh observed file is not automatically a lawful replacement for an admitted
dependency. Follow this HOW's existing origin/receipt and currentness relation;
an owner-admitted successor can supply current bytes, while unexplained physical
residue remains pressure. For a newly material observation, construct/admit the
separate revision/current input before its consumer; do not refresh frozen E_D,
an old stage envelope or recorded prompt in place. Later admission/replay uses
those recorded observation values at that cut. Physical changes to governing
source require the actual owning re-entry before changed meaning is relied on.

Local Consequence consumes the current admitted result/assessment, declared
next-work dependencies and actions, applicable observations/delta, retained
support, pending obligations and existing attempt/recursion limits. It is a
proposal in the existing traversal, not a new semantic authority. Its ordered
finite decision relation is:

| Admitted condition | Permitted proposal; withheld claim |
|---|---|
| Invalid source, authority, currentness or exhausted declared limit | Existing typed block/gap. Do not dispatch dependent work or invent a recovery or broader capability. |
| Required current-stage criteria satisfied, next-step prerequisites supported, and remaining pressure explicitly outside those dependencies under the admitted contract/evidence | Existing declared advance, carrying pressure and every unresolved obligation. No gap becomes satisfied and final lifecycle/application closure remains unchanged. |
| Actual affected support or failed realization, with a sufficient admitted selection and an available in-scope repair/re-entry declaration | Existing construction_repair or stage_revision and declared local route; preserve valid stages and invalidate only the justified dependent support. Prior failure remains failure. |
| Dependency sufficiency or scope/affectedness is unknown, and a matching declared selection/disambiguation F_P exists within limits | Propose that bounded evidence-producing work; its result is admitted and validated before any repair or target-use conclusion. Otherwise return selection_required/blocked_incomplete. |
| Required criterion remains falsified/indeterminate, or no lawful local next step exists | Typed block with actual cause, unmet obligation and owner handoff; no waived criterion, manufactured terminal success or automatic upstream A.0. |

The rows are evaluated in order over typed facts and admitted judgments, not
by matching words, severity labels or counts in pressure prose. A normal stage
continues through its existing exact satisfied-criterion judgment and HoG route;
ordinary wording or solution variation alone is not counterevidence. Every
declared rubric criterion remains required. An out-of-frame item may be carried
only when its noninterference is established by the declared dependency relation
or applicable admitted assessment, not by an author's unsupported label.

No new selection call is mandatory on an already-sufficient ordinary edge:
existing F_D checks consume the actual required-criterion judgment and declared
next step. Repair selection is different. The current native revision owner
accepts selection producers only in F_P/F_H, and the selected consumer declares
no deterministic policy supplying both repair mode and exact affected targets.
Do not infer that missing policy or add F_D to the regime allowlist. A future
F_D repair selection needs an exact finite case in the existing declaration
owner, sufficient admitted facts/J, native authentication of that selected
producer and regeneration of its typed output. Until that case is bound,
existing admitted F_P selection may be reused when applicable; otherwise return
selection_required or use its separately declared residual traversal. This is
a named realization/declaration gap, not new F_D availability or a mandatory
extra LLM at every edge. Reserved F_H authority remains distinct; the 5.1 human
response/resume path is not implemented here.

Ordinary advance/block continue to use existing judgment/outcome and route
owners. Repair proposals use the existing semantic revision selection/projection,
then `proposeGraphSpanReentryRoute`/`admitTraversalTransition` where the admitted
Program declares that route. A terminal prior Run instead needs a separately
admitted successor with the actual cause/parent and current authority; it is
never revived by a host loop. Keep `evaluateNext -> admitConstructionIntent ->
invokeGraphFunction` wherever the selected action requires it. Consequence
cannot bypass current-call, child-basis, effect, evidence or closure guards.
A missing local declaration yields a gap, not an implied routing capability.

### Bounded integrated discriminator

Use the prospectively selected witness and independent oracle, without changing
the active Run or expanding the application claim. First validate the changed
declarations/typed sufficiency and admission seams, then one installed thread:

- Sunny day: the exact selected role receives all required content and no
  unrelated worksite bodies; real work, independent evaluation and fresh
  result/replay retain the same bounded contract and native path.
- Mutable read dependency: a separately granted existing owner changes a
  declared relevant member outside S on the same app. The next admitted
  dependent input/prompt contains that actual successor, withholds affected old
  evidence, preserves unrelated valid evidence and leaves S/write bounds intact.
  An unexplained external change instead blocks; it is not silently reconciled.
- Ordinary variation: a materially adequate nonidentical candidate with
  nonblocking carried pressure advances after its actual required assessment;
  an unresolved required criterion cannot advance. When actual evidence and a
  declared local selection support repair, that same owner path proposes and
  admits only the bounded correction. No planted application defect, forced
  extra LLM round or copied expected verdict supplies the witness.

These are future execution obligations, not results. Record actual prompt bytes
and elapsed native/model work separately; no token quota or speedup assertion
is a gate. Preserve focused authority/effect-integrity checks and the original
first cause, then obtain early integrated evidence. Broader negatives,
repeatability and same-candidate assurance stay at D5. This amendment grants
neither source implementation, installed execution, application acceptance nor
resilience/storage/recovery work.

## Decisive Construction And Same-Instance Witness

Before a new live correction, prove known joins against frozen bytes: actual
failure stays failure; unrelated/spliced cause refuses; valid construction repair
keeps every semantic asset; semantic revision changes only its declared affected
closure; a discovered obligation cannot disappear with a superseded asset;
stale/foreign postimages and broader authority refuse; old invalidated proof
cannot close the revised claim; fresh replay derives the same successor.
Use one exact retained cause when available, plus bounded deterministic fixtures
for unobserved branches. Fixture checks establish only their mechanical scope.

Then the separately selected installed D2 witness consumes actual D1 evidence,
records the appropriate re-entry, repairs the same retained app and obtains fresh
execution and independent semantic evidence. Preserve original source, first
cause, four historical assets, pending/discovered obligations, surviving work
and all rejected occurrences. A construction repair does not claim semantic
invalidation; a generic successor test does not claim full S06/D3 or release.
No present counterevidence, application result or D2 acceptance is asserted here.

The Writer freezes this single component and returns to Executive. This grant
contains no code, test, live, tracking, requirement or accepted-D1-design effect,
and does not activate its own review or implementation.

## D2 Narrow Repair And Retained Snapshot Dependencies

This bounded refinement applies only to D2. Reference frames remain
LLM-selected evaluation context, never code-selected controllers or grants.
The admitted selection, existing declaration owners and ABG prefix govern
effects. Product meaning, D1, C0/C1, old C2/C3 arms and installed history remain
unchanged. Selection filtering, initial-preimage freshness and raw-result/child-
closure mismatches are realization corrections to this HOW; the new HOW is the
missing mixed-source snapshot relation below.

### Current basis and selected effects

One revision projection reconstructs the applicable Design, selection, complete
obligation lineage and current worksite basis from authenticated predecessor
history. Its pure derivation and typed constructors are shared by bridge
construction, result admission, pre-child/leaf dispatch and replay. Those
consumers do not implement independent currentness or dependency selection.
Physical observations enter through existing Product observation owners;
replay uses the observation and receipt bytes admitted at the recorded cut,
not today's filesystem. Runtime facts remain in existing ABG events.

For each exact canonical-root/path subject, start at its actual owner-admitted
observation and follow authenticated C0 predecessor/receipt/successor relations
on this app's admitted causal lineage through the selected prefix. Select the
unique surviving legitimate observation, not the original semantic preimage
and not a chronologically latest unrelated C1 result. Reuse Event Calculus
currentness and C0 transition/failure projection; a terminated predecessor
cannot be revived by matching bytes. Where no C0 transition exists, the exact
owner-admitted initial observation remains a possible basis, subject to no
intervening invalidation and exact current physical equality. Missing, forked,
foreign or incoherent history refuses. A same-byte C0 replacement with a new
filesystem identity is valid history, not an unexplained dependency change.
A physical change without sufficient admitted owner reconciliation remains
residue/pressure and withholds the dependent claim; no new reconciliation
authority or successful result is inferred.

Ordinary content changes preserve W under F-WORKSITE-CAUSALITY; they change
observations. Changed A/ProductSet/lock/root constituents require their owning
binding re-entry. Current invocation/grant and subject/territory constructors
are reused; neither caller hashes nor a new W establish content currentness.
The same-app binding refinement below supplies the additional native join when
only the admitted ProductSet/lock/W changes; it does not relax same-W checks.

Let V be the ordered union of the exact applicable Design's target refs and
its existing dependencyTargetRefs, in worksite declaration order. Let S be the
admitted selectedTargetRefs mapped bijectively to current target coordinates.
For a worksite repair S is nonempty and a subset of V. The C1 task contains
exactly S, in that order; R = V minus S is read-only snapshot dependency context.
No text, frame, role pairing or command name adds a write. The current retained
Design declares all 22 dependency refs, so selecting two writes yields two C1
members and twenty retained dependencies without a new dependency language.
A missing declaration or required file withholds verification, not imports an
ambient path. Stage revision uses its newly assessed applicable Design and
declared affected scope; it cannot silently widen S, commands or territories.

Retain the existing obligation/role/contract/policy/shape joins. A role needed
for proof may be supplied by a retained dependency; that grants neither a write
to its target nor new proof credit. Original and discovered obligations remain
in the full lineage union even when their introducing assets are invalidated.
Native assembly resolves the original assets, pressure and dependency context
through admitted parent coordinates and labels invalidated assets historical,
not current proof. Role-filtered author context and assessor-only evaluation
data retain their existing separation. Domain rubrics remain declaration data.

### One closed D2 preparation relation

Keep existing preparation E/S/T and C1/C3 source-result contracts exact.
Add only an owner-declared D2 entry E_D and bound input T_D to the existing
module-local preparation schema source. E_D has exactly kind, schemaVersion,
revisionBasisRef, revisionBasisDigest, constructionTask, snapshotTargetRefs,
dependencyObservations, commands, outcomePredicates and allowedWriteTerritories.
Its kind is worksite_revision_command_preparation_input, version 5.0.0;
constructionTask is the unchanged C1 task over S. snapshotTargetRefs is V;
dependencyObservations is exactly R, ordered as V. Configuration arrays retain
the existing C2 types and validators. The revision coordinate resolves the
actual admitted revision bridge input; it is not a new currentness token.

Each dependency row has exactly designTargetRef, subject, observation and
origin. subject/observation use the existing exact Product constructors and
must denote a current regular file. The unchanged origin arms are: an
admitted_input row has kind, basisAdmissionEventRef, inputAdmissionRef and
inputDigest; an admitted_replacement row has kind, resultAdmissionEventRef and
evidenceEventRef.
These coordinates identify the actual initial observation or authenticated C0
successor, respectively. They confer no authority on their own. ABG reconstructs
the origin, app/subject and surviving observation at the bridge cut and rejects
a spliced row even when its file bytes are equal. An insufficiently reconciled
post-publication failure is not either successful origin arm. The closed sum
also admits only the binding-projection arm defined below; no arbitrary origin
payload or caller-selected currentness token is accepted.

T_D uses the unchanged retain_graph_input relation and has exactly kind,
schemaVersion, entry:E_D and source:the actual closed C1 ConstructionResult.
Its kind is worksite_revision_command_preparation_bound_input. The fixed
E_D/S/T_D declarations, value validators and static retention check derive from
the same owner schema; no tuple registry or general selector is added.
Preparation's first pure leaf returns E_D.constructionTask. Its second matches
every actual C1 member to S and combines those successor observations with R,
in V order, into the distinct D2 C2 task specified in the C2 HOW addendum.
It never synthesizes a full-V ConstructionResult or invokes C0 for R.

ABG obtains E_D from the actual admitted bridge/graph-entry value and C1 source
from its completed child/foldback. The bridge revision basis, selected task,
entry, bound route, child invocation ancestry, result/judgment, A/W/grant and
independently exact installed owners must join. Before C1 dispatch, authenticate
the complete entry, dependency provenance and command configuration. Before C2
basis/leaf dispatch, follow the actual preparation edge, reconstruct its output
and require selected C1 successors plus every retained dependency still current.
Any dependency change after E_D's cut withholds this task; a new lawful
preparation is required rather than silently refreshing the frozen input.

Fresh evidence follows that same admitted revision/bridge/preparation/C1/C2
chain and its complete mixed-source snapshot, not old disk preimages or a scan
for equal/latest results. Preserve command failures as observations and semantic
assessment as a separate declared owner judgment. Reuse only proof whose exact
dependencies still hold; no retained source row itself establishes application
correctness. Old D1 evidence projection and historical-source C2 reuse remain
their own unchanged arms. D3/full-source coverage and release remain open.

### Decisive bounded falsifiers

Before live work, prove: two selected targets produce exactly two C1 members,
with all twenty other declared files available only as dependencies; an admitted
same-byte/new-inode C0 successor outside S is accepted without broadening S;
unexplained, stale, forked or foreign dependency origins refuse; missing/extra/
duplicate/reordered snapshot membership and broadened writes refuse; valid
original observation reuse works when no successor exists; a later legitimate
successor prevents stale predecessor resurrection. Unknown residue remains
non-successful. Superseding the discovering stage conserves every term/binding,
historical pressure and role-filtered context.

The installed witness then shows exactly selected C0 effects, a full declared
snapshot, untouched retained-file bytes and identities, exact before/after
protection, actual command/predicate observations, independent interpretation
and byte-equal fresh replay at the admitted cut. Old C1/C3-to-C2 sources remain
valid when their exact producer/A/W/O1 currentness still holds, including an
older selected result. Stubbed pure checks do not establish these installed,
physical, semantic or downstream claims.

## D2 Same-App Binding Transition Evidence

REQ-P-INSTALL-055, REQ-R-ABG3-BINDING-018 and
REQ-R-ABG3-CONTINUATION-014 permit separately admitted immutable bindings and
require exact covering reprice evidence when a causal spine crosses them.
This D2 refinement applies to unchanged A, canonical app root and declared
worksite meaning across a changed installed ProductSet/lock/W. Changed A or
app roots remain outside this arm. It adds no installed-product update or
retirement lifecycle, global current-binding pointer, event kind or runtime
entity. Old installs, bindings, observations, receipts and failure history stay
immutable. A successor is a separately admitted Run, not resumption of a closed
Run or authority imported from a copied store.

### Exact binding cover precedes observation reuse

Resolve both W identities from their actual workspace.bind admissions and
exact installed environments. The current invocation uses the new W, current
ProductSet/lock, independently verified installed owners and actual capability
grant. Historical execution bases and producers retain their original W and
installed declaration identities; matching public names or new package bytes
cannot impersonate them. Unavailable historical owners withhold the join.

Reuse the existing witness.admit reprice owner and
declaration_reprice_admitted event. For a crossed pair W0 -> W1, its typed
content names W0.bindingId as declarationRef, the exact W0/W1 binding digests
as beforeDigest/afterDigest, and the owning change class, ticket and reason.
Its evidence includes both exact binding ref/digest coordinates. Its basis
context and authority_basis subject are the same actual admitted historical
ExecutionBasis on the selected D2 parent/cause ancestry, under W0; a binding
artifact alone is not that basis context. The witnessing operation is admitted under W1
with its actual actor/capability/public-operation authority and durable prefix.
Both bindings and that historical basis precede the witness, which precedes
the D2 execution using the crossed pair.

The native D2 derivation authenticates this entire relation, not merely the
repriceRef or before/after hashes. The selected parent/cause ancestry, surviving
origin chain and current execution basis determine each crossed pair; no
caller-supplied transition path selects authority. All qualifying witness
occurrences for that exact pair remain evidence, in admission order; none is
chosen by latest/first or value deduping.
Other coexisting bindings do not become a global current binding. Successive
crossings follow the actual earlier revision-projection ancestry, preserving
each covered pair and cut. This includes an admitted projection reached through
a later failed C1's exact entry/child ancestry; choosing an older semantic parent
does not erase an intervening binding or successful member. Missing, reversed,
foreign, cyclic or unjoined coverage refuses as basis_fork_detected before
D2 traversal, revision materialization or worksite effects. The existing native
new-action/child-basis admission owner checks the cover for the exact declared
D2 entry; selection assembly and later consumers recheck the same derivation.
No pre-dispatch refusal manufactures actor evidence. Reprice covers the binding
crossing only; it proves neither file currentness nor application correctness.

### Native physical and causal correspondence

The single native revision projection reconstructs each historical subject's
unique surviving origin from the admitted initial input, successful C0 receipt
chain or an earlier binding projection below. It preserves A/canonical root,
subject URI and relative path, role, territory URI/root/operations, actor and
capability meaning, commands, predicates and write bounds. It then uses the
existing Product constructors to derive the new W-qualified subject,
territory, observation and target identities. These identities are recomputed,
not weakened or substituted with old W. Only the binding coordinates change
in this correspondence; the surviving observation's absent/file state and,
for a file, device/inode, exact bytes, digest and length remain the same.

At live assembly and result admission, Product-owned physical observation
must exactly match that reconstructed survivor under the current authority
and confinement checks. A caller's currentWorksite is an input to validate,
not proof. A native C0 same-byte/new-inode successor can be the survivor;
matching bytes without its owner receipt cannot explain an inode change.
Individual successful C0 members in a partially failed batch remain eligible
when current. Post-publication failure terminates its predecessor without
creating a successful origin, even when physical completion is recorded.
Unreconciled residue refuses; this arm creates no reconciliation authority.

Currentness is checked through the consumer's prefix, not only at the binding
cover or origin cut. Reuse C0/Event Calculus transition interpretation and
conjoin cross-binding physical-subject invalidation: an authenticated native
C0 transition consuming any correspondent observation invalidates that
observation and its earlier binding aliases. A successful successor is reusable
only through its exact eligible admitted lineage; a transition outside that
lineage can invalidate an old candidate but cannot supply a new origin by
global-history guessing. A failure supplies no successor. This test includes
owner-authenticated transitions under other bindings of the same A/root/path,
not only the old observationRef's binding-qualified fluent. Consequently a
new-W C0 prevents resurrection through an older W, even after byte restoration
or another covering reprice. Missing or ambiguous survivor history refuses.
Existing C0 facts, fluent meanings and effect algorithms remain unchanged.

### One additional closed origin arm

WorksiteRevisionObservationOrigin additionally has exactly
`{kind: "admitted_binding_projection", resultAdmissionEventRef, judgmentEventRef}`.
Both references identify the actual successful D2 revision-projection CCall
and its advancing judgment, authenticated with its invocation, GraphFunction,
locus, implementation, result ref/digest and native admission. A foldback or
equal-value wrapper is not that producer. The dependent row must equal one
exact subject/observation in that projection's recorded current worksite.

This arm is a subordinate provenance projection, not a new observation event,
write grant, ledger or proof credit. The projection's existing request/result
records retain the current observation values; at its original predecessor
cut the shared derivation reconstructs the covering witness(es), historical
origins and correspondence. It does not refer to itself while admitting its
result. Only a later bridge/preparation can emit this origin arm after the
projection and judgment are admitted. Earlier projection references strictly
decrease admission order. Native survival checks then reject every later
invalidation through the consumer cut. The two existing origin arms retain
their exact fields and meanings; a translated initial observation cannot
pretend to be admitted_input under the new W, or a C0 successor never written.

### Shared mapping, admission and replay

Keep this derivation in the existing native D2 worksite projection owner.
Native new-action/child-basis admission, selection input assembly, revision
projection, applicable-Design reconstruction,
E_D bridge, C1/C2 input provenance, result admission and replay consume that
same relation. Same-W paths and earlier valid C1/C3-to-C2 arms remain exact.
No consumer independently relaxes W equality or selects a replacement origin.

Resolve the applicable retained Design's actual author basis, including a
later revised Design rather than assuming the original root owns its refs.
Use the authenticated correspondence to map its complete ordered inventory
and admitted selection through historical, prior and current target identities.
The mapping is bijective and preserves declaration order and roles; missing,
extra, duplicate or ambiguous rows refuse. Existing dependencyTargetRefs,
S/V/R partition, constructors and preparation algorithms remain the carriers:
two selected targets produce two C1/C0 members, while twenty retained rows
remain read-only members of the full twenty-two-member C2 snapshot. Retain all
obligations, invalidated historical context and role/contract/policy/shape joins;
binding cover cannot reprice semantics, commands, proof or write authority.

Recorded revision results, binding/witness admissions, observations, receipts
and their exact prefix are sufficient for deterministic admission/replay
derivation. Replay uses the observation values admitted at the original cut,
not today's app files, current install selection or a temporary C2 snapshot.
Live physical checks still run at their existing pre-effect boundaries and
protect all V before/after C2. A change after preparation requires a new lawful
projection/preparation, not a refresh of frozen inputs. No claim of unchanged
whole-log or selected-file inventory survives actual successor appends/writes;
historical prefix preservation and new effects are reported separately.

### Bounded qualification boundary

Decisive positives include initial-origin translation with no repeated initial
C0, successful and same-byte/new-inode C0 survivors, two successive binding
crossings, a later revised Design, and repeated repair under the new W.
Negatives cover wrong/absent reprice or historical basis, current grant with
old W, foreign owners, unjoined/forked origins, unexplained physical drift,
post-publication residue, and stale resurrection after new-W C0 even when bytes
are restored. Each refuses before affected dispatch/effects; success admission
and actor evidence requirements are not weakened to improve a refusal message.

The installed discriminator uses the exact retained store/app instance, a new
immutable install and genuinely admitted binding/cover, then separate successor
Runs with actual selected effects and full-snapshot protection. It preserves
historical prefix evidence, old installs and all unselected byte/inode identities,
and compares fresh result/replay at the new admitted cuts. Synthetic histories,
copied stores, pure constructors and current hashes alone cannot qualify this
claim. Native execution, original application use, integration and D2/S06 or
release acceptance remain separately selected work.
