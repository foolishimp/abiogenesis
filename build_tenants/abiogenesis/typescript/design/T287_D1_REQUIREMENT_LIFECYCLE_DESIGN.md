# T-287 D1: Native Source and Obligation Handoff

Status: The first increment's full-source/paired-open-obligation checkpoint is
Executive-accepted; its coding grant is exhausted. The appended semantic-stage
and Design bridge HOW is Executive-accepted after independent
`D1-REVIEW-NATIVE-STAGE` returned satisfied with no material findings.
`D1-NATIVE-STAGE-IMPLEMENTATION` is active; full HOW/D1 outcome remains open.
Owner: ABG native admission under T-287 D1. Original design activation:
`D1-NATIVE-DESIGN`, 2026-09-09. Exact original inputs, preimages and freeze are in
[native-design](../../../../.ai-workspace/comments/codex/20260909_D1_LIFECYCLE/native-design/).

The sections before **Native semantic stages and Design bridge** retain the
first increment's design and its historical stopping boundary. The appended
continuation owns the current selected HOW; the accepted handoff is its input.

## Product frame and selected outcome

The target remains ABIogenesis 5.0's fixed sixteen families, F01–F11 and
F13–F17. F12/S04 remains 5.1. F02 supplies GTL declarations, F03 the direct
seven-C calculus, F04 probabilistic leaves, F05/F06 Public contracts/projections,
F07 complete One Surface,
F10 native runtime truth, and F17/S06 the required installed lifecycle witness.
GTL declares; HoG traverses; ABG admits and replays. Product wraps supported
operations; C0 retains physical-effect ownership. No root, release, scenario,
runtime engine or Product scope changes are selected.

The exact authority is `stdo_abiogenesis.json`, STDO v2.5.0-rc.6, and the
[project reference frame](ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#abiogenesis-project-reference-frame-basis).
The accepted D1 source authority and separate Writer transition are recorded in
[acceptance.md](../../../../.ai-workspace/comments/codex/20260909_D1_LIFECYCLE/acceptance.md).
Accepted GLC candidate `bb0d46955840d90c53876381eaf1b738461ca6ea4f95fe3161a3d050f18c30f7`
and basis `e60fd7bc451bba8b68f5a893e382f8a1979d25e7c3aea8e7d1c2f44f275773bd`
own application source, semantic use and named stages; their acceptance is a
source/HOW prerequisite, not delivery of the native implementation.

Select one ordinary installed `F_D` C-call that transfers the complete fixed
source bundle, its declared user-input role and source-linked paired obligations
into a typed first-stage input. Admit and replay that exact transfer with
explicitly **non-closing** application coverage. This is a useful prerequisite
to the first semantic stage; it neither executes Intent/Product derivation nor
completes D1. Completion of this Run proves the transfer contract only.

D1 still owes source -> Intent/Product -> Requirements -> Design -> one material
application behavior. D2 owes consequential correction; complete S06/pre-RC
requires D3's mandatory original outcomes, not this bounded handoff or the
earlier 58 tests. GLC publication and maturation remain independent, including
the accepted INTENT clarification. Its semantics remain downstream; ABG owns
admission, coverage, continuation and runtime closure.

## Smallest carrier and ownership decision

Use one existing Program, GraphFunction and `C.of`, resolved from an installed
publication through ordinary Public invocation/result/replay. The new callable
is generic: it has no GLC stage names, Data Mapper identifiers or filesystem
paths in its implementation. Its declared effects are empty. Do not change
`retain_graph_input`, C1, C2, C3, the Public operation inventory or existing
worksite authority. The output contract is the future stage's ordinary input
contract; binding an actual semantic stage is the next dependent increment.

Add a closed, optional `requirementHandoffs` declaration collection to the
existing `ModulePublication`; a GraphFunction selects one declaration by its
existing string-reference `declarations` field. It is subordinate metadata,
not another published target, graph, catalog, ledger or Program. The first
carrier contains these typed projections of the owning GTL requirements:

| Declaration | Minimal first fields and meaning |
|---|---|
| Handoff declaration | Stable declaration ref; selected GF ref; source context ref; required input-role ref; ordered term and fulfillment-binding refs. All references resolve inside this immutable publication. |
| `ContextDeclaration` | Stable context ref, source locator and inventory digest; full member inventory with member ref, relative logical path, exact byte count and SHA-256. The source role is declared independently of invocation input. |
| `RequirementTerm` | Stable requirement ref and source bindings: context/member refs, member digest, half-open UTF-8 byte span and span digest. The selected first witness binds REQ-ACC-01..05; the remaining full source remains present and semantically unassessed. |
| `GtlContractFulfillmentBinding` | Stable obligation ref and requirement ref, realization-output contract ref, proof-output contract ref and proof-policy/shape refs. These declare the required pairing; they are not evidence, fulfillment or closure. |

There is no current implementation of these three GTL types. This introduces
their bounded native declaration subset, reusing the required fulfillment
binding concept instead of introducing a second pair-list algebra. More fields
for general relations, spans, asset surfaces, routing, depth and discovered
obligations remain explicit later dependencies. Unknown fields/ref kinds are
refused; this subset must not silently accept those unsupported declarations.
Missing proof policy/shape is represented as a typed non-closing gap; no default
policy is inferred. Every selected requirement still receives both obligation
roles, even when a role's required contract/policy is unresolved.

The independently admitted publication fixes the source inventory, role, term
vector and bindings. The request supplies exact source bytes (canonical base64
per member), source-role ref and selected declaration ref. It cannot supply an
alternative expected inventory, filter the selected terms or set coverage.
Duplicate, missing, extra or changed members, wrong role, wrong declaration or
source/span digest, dangling refs, duplicate identities and mismatched binding
roles fail closed before an admitted success. Byte hashing uses the decoded
bytes, without Unicode or line-ending normalization; paths identify source
members and confer no filesystem read/write authority.

The fixed publication is not itself proof that somebody captured every user
requirement correctly. In the installed witness, its exact declaration basis
is pinned to the independently accepted GLC five-file inventory and user-role
selection before invocation. Mutating the request and its claimed manifest
together cannot replace that basis. Replacing the publication is a different
declared subject requiring a new admission/disposition, not a passing negative
test. Unresolved source authority or semantic completeness remains visible.

## Native declaration access, admission and replay

`LeafExecutionOccurrence` currently has no generic declaration basis. Its
`LeafExecutionAuthority` is specifically a file-replacement authority and must
not be cast or widened into source authority. Add one optional read-only
declaration-basis projection for the new F_D binding. Existing leaves receive
no new behavior and require no effect grant for this projection.

At the current C-call prefix, ABG constructs this projection from the already
admitted execution basis, current C-call, installed resolution and exact Program
publication available in `ExecutableCCallContext`. Bind execution-basis ref and
digest, publication digest, Program/GF refs and digests, C-call ref/digest,
implementation-resolution ref/digest and durable prefix. Reauthenticate their
existing native relations before dispatch. A caller object or an optional
publication field alone is not authority. HoG forwards only this owner-derived
projection. The deterministic leaf reads the selected declaration from it,
checks the request against it and returns an ordinary realization candidate.
No declaration authority is accepted from the JSON request.

The native owner validates the result against the same admitted input and
declaration basis. It must regenerate/compare the exact source identities,
selected term vector and paired obligations, not merely validate output shape.
An omitted obligation or altered role cannot pass by a leaf returning a
well-formed smaller result. Resolve/validate the basis with existing ABG
execution-basis and prefix machinery; no second store, process-local authority
brand or writable requirement ledger is permitted.

Existing ABG input/result admission facts carry the typed transfer input and
result. The leaf return alone is unadmitted. Reuse existing C-call/result/Run
event ownership, durable bytes and fresh result/replay operations. A returned
handoff contains the full source bytes and fixed inventory; source-role and
declaration identity; the immediate admitted input/basis identity; exact terms
and paired role obligations; and a typed gap projection. Copying source bytes
into an arbitrary JSON property is insufficient without these native checks.
For this first invocation the admitted input is the predecessor; no semantic
stage result is fabricated and no previous run is relabelled as one.

The gap projection is read-only and always non-closing. It reports each selected
realization/proof role as lacking admitted evidence, unresolved policy/shape
where applicable, and the remaining semantic-source assessment, instruction
assembly, stage-result, depth/strength and discovery dependencies. It has no
eligible/fulfilled branch and no caller-set pass flag. These are facts about
this transfer's limited coverage, not an additional closure state machine.
The existing Run may close because the exact handoff succeeded; application
requirement obligations remain open. No `EdgeFulfillmentLedgerRow` for those
application requirements may be manufactured from the transfer judgment.

This first typed admitted result is not a claim that the general Requirement
Ledger, requirement-event family, coverage/depth fold or native continuation
integration is implemented. Those remain dependencies before their outcomes
can be claimed. Queries remain projections of admitted events, not lazy writes.

## Concrete field gaps and coding territory

Paths below are relative to `build_tenants/abiogenesis/typescript/code/src`.
They select narrow owner functions and exports, not authority for general edits.

| Current seam | First increment change |
|---|---|
| `gtl/contracts.ts:140,306,477` has node/GF/module carriers but no typed context/requirement inventory | Add the subordinate closed declaration types in new `gtl/requirement_handoff.ts` and optional ModulePublication field. Provide the ordinary native callable publication/constructor there; export through `gtl/index.ts`. |
| `owner_bindings/run_invocation.ts:577` and `validator/validation.ts` close publication shape/reference validation | Extend only the new optional declaration shape, exact identities, references and selected binding; absence preserves current publication behavior. |
| `implementation/contracts.ts:110` and `hog/ccall_lifecycle.ts:65,407` lack generic read-only declaration access | Add the optional occurrence projection; use new `abg/requirement_handoff.ts` for native basis resolution and exact transfer relation, forwarding it at the existing F_D dispatch. Export the native owner through `abg/index.ts`. |
| `implementation/leaf_invocation_port.ts:397` authenticates occurrence/installed leaf; `abg/c_call_outcome.ts` admits results | Check the new binding's required basis before dispatch and its exact regenerated transfer relation before result admission. Leave existing worksite and F_P branches unchanged. |
| Product contract validation and judgment live in `product/builtin_semantics.ts`; types/exports in `product/index.ts` | New `product/requirement_handoff.ts` owns closed request/result/refusal schemas and subordinate non-closing gap shape. Wire only these value kinds and the transfer judgment. Do not grant Product runtime truth ownership. |
| Installed implementation descriptor/export pattern in `implementation/index.ts` | New `implementation/requirement_handoff.ts` supplies one F_D implementation/descriptor and ordinary success/refusal candidates; exports only the selected callable. No subprocess or physical effect. |

Implementation must preserve the current installed descriptor/publication
packaging path. Any extra pack-registration file actually required is a narrow
export/descriptor dependency to identify in the coding return, not permission
to create a new installer or release. Exact field spelling can follow current
constructor conventions; the mandatory joins and non-closing result above may
not be weakened. If current basis machinery cannot authenticate the added
read-only projection, report that precise field gap instead of trusting input
JSON. No further generic authority mechanism is authorized by this HOW.

`gtl/graph_applications.ts:55`, `validator/validation.ts:891`,
`product/execution_resolution.ts:737` and
`product/worksite_preparation_contracts.ts:112` implement the closed worksite
retention relation. They are evidence of the next stage-binding gap; their
retention logic is unchanged. `abg/traversal_route.ts:325` has only a fulfilled
edge row, not requirement-role/depth accounting; no ledger extension is selected.
C1 forwards caller prompt bytes, so native instruction assembly and admitted
semantic stage results are the next dependent seam, not current readiness.

## Installed discriminator and stopping condition

One focused deterministic check file and one short source-blind installed
witness are sufficient for the first coding grant. The witness uses the exact
packed candidate through ordinary Public operations with no repository imports,
model, subject application execution or release/version change. It admits the
fixed declaration basis once, invokes the one F_D handoff with all five original
source files, then reads result and replay in fresh processes. Confirm full
byte digests, declared role, every selected term and both obligation roles,
literal non-closing gaps, exact installed source identity and no physical effect.

The decisive negative holds that independent basis fixed and removes one source
member or one declared obligation, or substitutes a wrong role/digest. It must
not produce an admitted successful handoff; a fabricated smaller output must
also fail the native relation. Include the two-copy attack: changing request
bytes and a caller-supplied inventory together still cannot change the pinned
publication. Tests of carrier shape alone do not establish this property.

The concrete application contrast is `mapper_requirements.md:448–517`
REQ-ACC-01..05: exclusive and complete source-key partition, persisted atomic
ledger/metadata and deterministic backward lookup. The retained application
ledger currently stores aggregate counts. A count-balanced example with swapped
or missing source keys must remain non-closing under the required keyed proof
relation. The accepted first fixture did **not** declare non-null role contracts,
proof policies or shapes: every executed binding was null. Their preservation
and keyed discrimination were unproved; the original sentence asserting an
explicitly declared keyed contract/policy was an unmet design expectation.
The next section selects its concrete realization without relabelling that
evidence. F_D does not discover the stronger meaning by reading prose. At that increment
all application evidence remains unassessed/non-closing, including count-only
evidence. An actual keyed verifier and its semantic/native admission are later
work; do not add a pretend count classifier or claim that behavior is proved.

Stop after the handoff's installed positive/negative evidence and frozen return.
The next grant binds native instruction assembly and a first real downstream
stage result to this input, keeping the original source and obligations through
that stage. Broader relation/AssetSurface metadata, admitted semantic judgments,
positive and adversarial proofs, depth/strength/discovered obligations, coverage
fold, selective invalidation and continuation remain owed. Nothing here weakens
their inherited laws or adds a prerequisite to GLC's independent maturation.

## Reconciled predecessor designs

The operative [realization constitution](ABI5_REALIZATION_CONSTITUTION.md)
supplies immutable declaration, native event/admission, owner and Run entity
boundaries. This design selects the existing Run/input/result transition and
subordinate immutable payload, not a new runtime entity or donor engine.

The M03 requirement-algebra readiness derivation and proof carry-through
first-slice IACS correctly require paired roles, admitted lineage and replay
projections. Their historical ledger/plan/type names are not current native
coverage. Reuse the fulfillment-binding concept as required by proof
carry-through -027, but do not port the T188 surface wholesale. The M03
instruction-assembly derivation is a dependency for the next semantic stage;
its plan remains subordinate and must never become a lowered Program.

The owning GTL Context/AssetSurface/Requirements Algebra and ABG Requirements
Algebra, Proof Carry-Through and Instruction Assembly requirements remain in
force. This bounded non-closing result is intentionally short of their full
coverage, depth and discovered-obligation closure relation. Source acceptance,
HOW acceptance, implemented behavior and qualification remain separate states.

## Native semantic stages and Design bridge

This continuation keeps the Product frame above. It implements one reusable
native relation through which downstream declarations select Intent, Product,
Requirements and Design. These are GLC asset/stage names, never native routing
branches. The target is one installed composed chain followed by the retained
Data Mapper's keyed accounting/backward-lookup behavior, not another source-only
transfer. The [activation and exact preimage](../../../../.ai-workspace/comments/codex/20260909_D1_LIFECYCLE/native-stage-how-01/activation.md)
bind this amendment. Existing accepted handoff and campaign evidence remain
unchanged; new non-null declarations are a distinct admitted publication.

### Declaration and common envelope

Use the existing ModulePublication, GraphFunction, GtlNode and C declarations.
Add subordinate closed declaration rows for the selected node's AssetSurface,
semantic work/rubric, proof policy/expected shape and instruction assembly rule.
They enter through the existing publication, contribution, Catalog, conformance
and installed-resolution path. No filesystem scan or parallel registry selects
them. Native and declaration-only consumer artifact identities remain distinct.

AssetSurface preserves `kind`, `requiredContexts`, `standardsRefs` and
`outputContractRefs`, with declared constructor/renderer refs, proof obligation
refs and authority slots. Slot dispositions retain the owning three labels;
bounded fallback requires preconditions. This thread selects normal slots only.
The surface resolves its supported native response contract; a domain asset-kind
label is not a new executable value kind or native implementation. Source role
is declared provenance, not proof of human authorship.

The assembly rule binds the selected GF/vector, ordered section rules and exact
full-content/proportionality policies. It does not duplicate the surface's
response contract, renderer, node type, authority, proof role or regime.
Semantic work and rubric are immutable downstream data. No callback, code,
ready-made answer, expected output or worker-supplied closure flag is executable
declaration truth. Policy/shape refs in every selected fulfillment binding
resolve uniquely to immutable declared rows and their digests; both role output
contract refs resolve to existing supported native structural contract kinds.
Opaque names are insufficient. Required evidence kinds, obligation/role refs,
source bindings and semantic criteria are carried explicitly. They describe
application proof requirements; carrying them does not discharge them.

One closed `SemanticStageEnvelope` is the input and output contract shared by
every stage. It contains:

- the exact complete source and independently fixed source-role/context/term
  basis, including the original handoff's admitted source coordinates;
- every original paired fulfillment binding and resolved non-null policy/shape
  row; immutable predecessor assets/assessments and their native provenance;
- ordered semantic assets and explicit pending/conflicting/discovered pressure;
- an optional exact worksite basis for the later Design bridge: immutable
  authority/binding/grant, allowed target subjects with current observations,
  current implementation/verifier bytes bound to those observations, and declared
  command/write bounds. This is input data, not an effect grant;
- application coverage fixed as non-closing, with unassessed depth, strength,
  discovery and full-source-completeness gaps retained.

Native constructors create the initial envelope from the admitted handoff and
the newly admitted non-null declaration basis. Input source/term identities must
equal the fixed independent fixture; role bindings must equal the new admitted
publication. This does not pretend the old null-binding Run admitted new rows.
Later calls consume exactly the prior admitted output. Each stage appends its
own asset/assessment and cannot replace the source, earlier assets or obligations.
No worker echoes or authors the conservation envelope: it returns only its
declared asset or assessment candidate; native code adds conserved fields.

The semantic asset has a closed common body: ordered stable statements with
declared modality, source-span citations, obligation and predecessor-statement
refs, conflicts and pending pressure. Requirements additionally returns native
RequirementTerm and fulfillment-binding candidates plus typed refinement
relations; Design additionally returns a typed worksite proposal. These are
supported optional body members selected by the declared asset interface, not
prose interpreted by host code. New terms/relations retain their original
parents/source and paired roles, or an explicit unresolved-role gap. Unknown
relations and dangling refs refuse; discovered pressure cannot delete or cap
incoming obligations. Admission of these candidates as stage evidence does not
claim complete RequirementLedger/depth/coverage implementation.

Native assembly supplies deterministic numbered source locators. The author
selects source meaning and an exact quote/locator; it need not calculate UTF-8
offsets/digests or echo the source bundle. Native projection resolves each
unique locator/quote against the fixed original bytes, computes the existing
RequirementTerm spans/digests and stable identities, and validates parent/role
links. Missing or ambiguous grounding refuses or remains an explicit ungrounded
candidate gap; it cannot become an admitted grounded term. Derivation is not
capped at the five initially selected ACC terms.

### One reusable C relation, with separate semantic evidence

Declare each stage as ordinary `C.compose(author, assessor)` over two `F_P`
leaves using reusable native bindings. An outer ordinary composition connects
the stage GraphFunctions in caller-declared order. The author returns a semantic
asset; a distinct assessor invocation receives the full original source, every
predecessor and the exact candidate under the immutable rubric. Its response
binds candidate digest, role, rubric criteria, findings/citations and one of
satisfied, falsified or indeterminate. An author's assertion is never its own
semantic proof. Both dispatches use the existing closed-prompt transport with
zero tools; neither mutates the application.

Native C checks establish source/role/contract/lineage conservation and admission
of the assessor's exact evidence. They do not infer meaning from prose. The
declared stage judgment may advance only when the admitted assessor evidence
supports that stage's bounded contract and no required criterion is unresolved
or falsified. Otherwise ordinary C failure retains the asset, assessment and
pressure and prevents dependent dispatch. Satisfying a semantic-stage contract
does not fulfill the carried application contracts or establish general proof
depth. Remaining application work may be explicit and non-closing while a
faithful Intent/Product/Requirements/Design asset is accepted for the next stage.

This composition uses exact output-to-input contract equality. It requires no
new retention tuple, host loop, callback engine or stage controller.
`retain_graph_input` and existing C0/C1/C2/C3 behavior remain unchanged.

### Native instruction assembly and result admission

At the opened C-call, native ABG reconstructs its declaration/input basis from
the actual installed publication, execution basis, selected GF/locus, current
CCall and durable prefix. Unlike the first handoff's root-only authenticator,
this new relation supports both root and ordinary composed child execution:
rehydrate the owning child basis and materialize its graph from its actual
admitted input, conserving parent/callee/composition identities. Verify exact
graph bytes and owning materialization coordinates. Do not cast a worksite
LeafExecutionAuthority or trust an optional caller object as declaration truth.

Prior assets are joined to actual admitted C-call result and judgment at that
prefix. A payload digest alone cannot authenticate an invented predecessor.
Own-result refs do not exist before admission: current output records its
source C-call/actor evidence and content identity; the next boundary resolves
the admitted result/judgment, or fails closed. Public result/replay supplies the
terminal result coordinates. Cross-run handoff use requires the same declared
source relation and authenticated prior prefix; a caller cannot substitute two
mutually consistent weakened copies.

ABG deterministically derives a subordinate materialization plan, binds the
immutable runtime envelope and renders its manifest **before** worker dispatch.
Reuse `ProbabilisticWorkerRequest` and `invokeActorProcess`; do not create a new
transport. Add an exact semantic-call variant to the existing native
`actor_transport_binding_admitted` payload containing the plan, envelope and
manifest, with their digests and source coordinates. The owning ingress checks
them before `actor_invocation_started`/process effects. The historical variant
remains valid; no Public install-artifact ingress is repurposed. Replay derives
the same included sections and prompt digest from these admitted bytes and
immutable declaration/runtime sources, including a fresh process/module.

The selected finite assembly algebra is deliberately small:

| Decision | Total operation and refusal/gap |
|---|---|
| source, role, stage and predecessor | Exact fixed-basis identity/byte/ref joins; missing, stale, substituted or wrong-role inputs block before dispatch. |
| contract, proof, authority and renderer | Resolve the selected node/surface and immutable rows uniquely; missing, ambiguous, unsupported or forbidden rows block. |
| relevance and compression | Include all five source members, selected paired obligations, all predecessor assets and required rubric/context sections in declared order; full content only, with no omission, excerpt or fallback. Budget insufficiency is a typed block, never truncation. |
| proportionality | Dispatch only the declared semantic F_P role whose interpretation is not a deterministic identity check. Pure assembly/conservation remains F_D and dispatches no worker. |
| rendering | One native UTF-8 renderer over typed sections; exact decoded source bytes, no Unicode/line-ending normalization. Delimit source as quoted input, not executable instructions. Record every section and digest. |
| non-tautology | Reject unsupported answer/disposition-bearing instruction slots and caller final prompts. Domain prose is not decidable by keyword; differential semantic tests must expose a weaker substituted candidate. Evaluation-only oracle answers never enter author materialization. |

Missing assembly cannot fall through to an unguarded new semantic F_P call.
The manifest's derived response contract must equal the actual request/schema;
the actual prompt must equal the native rendered bytes. Existing raw exchange,
tool-count, timeout, process, candidate and result evidence joins still apply.
After transport, native output admission regenerates the conserved envelope
and validates only the candidate's permitted delta against the actual input,
surface and actor evidence. Shape-valid source loss, forged assessment, changed
bindings, missing criteria or unsupported body fields refuse before dependent
use. The manifest and raw transport are distinct from semantic adequacy evidence.

### Design to the existing worksite pipeline

Current `selectWorksiteConstructionTask` selects a caller-prebuilt task; it does
not implement this join. Add one native F_D projection from the admitted Design
envelope to `WorksiteCommandPreparationInput`. The Design selects exact allowed
target refs with implementation/verifier roles, obligation refs and current
observation coordinates, plus the existing C2 command, outcome-predicate and
write-territory shapes. A prose pathname or arbitrary shell string cannot
escape those closed owner shapes and declared bounds.

Native code joins selections to the immutable allowed worksite basis and actual
current observations/grant at this prefix. Missing/stale targets, role confusion,
ungranted paths/commands, replaced source or absent admitted Design assessment
block before C1. It constructs the existing C1 target vector and renders its
prompt from full source, paired obligations and admitted Design; the host cannot
translate, summarize or substitute a prompt. Required verifier/implementation
roles remain paired; unchanged retained targets are conserved according to the
declared repair territory. Native constructors validate all known command,
predicate, observation and target joins together before the first expensive call.

The selected construction target's dependency sufficiency must be known from
the declared dependency/obligation relation and admitted predecessor evidence.
Missing source files, unknown required dependencies or unresolved target proof
requirements block C1 rather than being hidden by a passing Design shape.
Unrelated full-application gaps remain visible; they do not acquire false
closure from this bounded construction. The prompt includes the actual current
implementation/verifier bytes needed for the repair, not just path/digest names.

The output is the existing preparation entry, so ordinary
select -> C1 -> existing retained worksite tuple -> prepare -> C2 follows without
changing their semantics. C0 alone performs file effects. Bind the generated C1
prompt/task and assembly projection to the bridge result; the selected native
dispatch guard verifies that exact task/prompt provenance before this C1 call,
without changing C1's worker request or result contract. C2 still receives its
source construction result and protected successor observations from the existing
prepare relation. Neither a successful bridge nor passing commands close the
stronger keyed requirement: application semantic assessment and actual positive
and negative keyed evidence remain required in this D1 thread.

Add the narrow companion F_D evidence-input projection in the same owner cone.
After C2 it reconstructs the conserved source/Design envelope through admitted
bridge/selection/construction ancestry, joining the exact C2 observation and
native C1/C0 source/result/observation provenance. It binds actual realization
and verifier artifact bytes by their admitted observations, separately from
verifier execution/log/report evidence. Stale, foreign, missing or role-swapped
evidence refuses. It supplies the common semantic envelope to the reusable
assessor under the downstream keyed rubric. The host does not rebuild an
authoritative evidence packet. Full original requirements, current Design and
actual verifier artifacts/results are available to that assessor, without
feeding its expected judgment to construction.

Domain role contract refs here declare expectations over supported native C1/C2
carrier kinds. Their non-null preservation alone is not an executable alias or
application-proof admission. This projection authenticates the actual owner
contract, obligation/role and policy/shape joins before semantic evidence can
support the bounded application claim. Native contract declarations retain the
current `contractVersion: "5.0.0"`; domain ref versions may be independent.
No new coverage ledger or caller-written application-complete state is added.

### Coding territory and decisive witness

Paths are relative to `build_tenants/abiogenesis/typescript/code/src`. These
select the new relation only; unrelated behavior stays outside the grant.

| Owner seam | Smallest required realization |
|---|---|
| `gtl/contracts.ts`, `gtl/index.ts`, new `gtl/semantic_stage.ts`; existing `gtl/requirement_handoff.ts` constructors | Closed subordinate AssetSurface/stage/rubric/policy/shape/assembly rows, typed body/refinement declarations and reusable author/assessor/bridge publications. Preserve original handoff semantics; permit complete non-null consumer declarations through ordinary construction. |
| `owner_bindings/run_invocation.ts`, `validator/validation.ts`, `product/execution_resolution.ts` | Canonical closed publication shape and complete selected declaration/reference/contract closure, including child GF composition; no relaxed catalog or installed owner checks. |
| new `abg/instruction_assembly.ts`, new `abg/semantic_stage.ts`, `abg/index.ts` | Authenticate root/child/predecessor basis, derive/render plan, regenerate admitted output/bridge and companion evidence-input relations and read-only replay projection. Reuse existing execution/CCall result and judgment projectors. |
| `implementation/contracts.ts`, `hog/ccall_lifecycle.ts`, `implementation/leaf_invocation_port.ts`, `abg/actor_process.ts`, `abg/event_store.ts`, `abg/c_call_outcome.ts` | Optional native read-only stage/assembly basis, pre-dispatch admission variant, exact result delta guard and bridge-provenance guard for the selected C1. No change to generic worksite retention or physical authority. |
| new `product/semantic_stage.ts`, `product/builtin_semantics.ts`, `product/index.ts`; new `implementation/semantic_stage.ts`, `implementation/index.ts` | Closed input/raw asset/raw assessment/output/failure contracts; native author/assessor wrappers, F_D bridge and companion evidence-input projection using existing worksite constructors. Native schema validation and semantic assessment remain distinct. |
| existing manifest generation and focused installed test machinery | Necessary descriptor/publication registration only; exact source/build/pack and declaration-only consumer artifact join. No release/version allocation. |

The first coding grant covers this one generic composition and bridge together.
First compute every known declaration/catalog/schema/API join deterministically;
then use one retained installed proof instance. A focused model-free discriminator
must cover all non-null contract/policy/shape rows, two composed stages, exact
prompt/manifests and fresh replay, plus lost source/obligation, altered non-null
binding, fabricated predecessor/output, wrong role/digest, missing assembly,
changed prompt and forbidden/stale Design-target negatives. Preserve the first
causal failure and last lawful prefix, not a fresh reset per test.

The actual D1 witness then runs the declared Intent -> Product -> Requirements
-> Design -> bridge -> existing construction/execution composition through
installed Public on the retained Data Mapper instance. It must produce semantic
content, have the next stage actually use the preceding admitted asset, preserve
all five sources and non-null paired rows, and expose real actor/command/result/
replay evidence. One generic implementation serves every stage; no separate
setup or model qualification round per label. A same-count but wrong-source-key
candidate must not gain keyed application credit. GLC owns that exact positive/
negative oracle and application interpretation; ABG owns admission and replay.

The same witness must use the companion evidence-input projection and admit the
final semantic assessment of actual keyed behavior. Merely carrying a named
policy or passing the earlier count-only tests cannot satisfy that claim.

The installed structural checks do not prove semantic quality. One live chain
does not prove repeated judgment stability, general compression/relevance,
universal instruction assembly, all proof-policy depth/discovery, D2 correction,
full original Data Mapper/S06 or release. Those obligations remain explicit and
cannot be cleared by stage status or a local coverage table. The historical M03
assembly documents supply source-trace and carry-through questions; their
compiler/registry/ledger surfaces are not current runtime authority and are not
ported. This amendment supplies the next concrete D1 path, not Product closure.

### Retained assessed-stage tail and current worksite (D1 continuation repair)

A separate dependent tail Program may name the unchanged lifecycle through
`policies["abg.semantic_lifecycle"]`. The existing exact reachable publication
closure resolves its single lifecycle owner, source owner and historical stage
GFs as dependencies; this adds no callable membership. Current root Program
authority remains distinct from the borrowed lifecycle/GF owner. Historical
source, assets, assessments and their admitted CCall/prefix identities remain
unchanged; no copied declarations or caller-supplied history acquire authority.
The entire retained envelope must equal a prior native result with an advancing
judgment; separately valid assets do not authorize a different assembled value.

The bridge accepts that exact retained Design and derives a separate current
preparation task from the current admitted workspace environment and invocation
grant. Its one-to-one target projection must conserve canonical physical root,
path, kind, file identity and bytes (or exact absence), roles and all declared
commands, predicates, write boundaries, dependencies and obligation selections.
Missing, ambiguous, stale, changed or broader authority refuses. Current
subject/O0/target identities belong only to the new task; no old record is
rebound. Explicit empty-obligation snapshot dependencies carry no proof credit;
at least one nonempty selected obligation must retain both resolved roles.
No pre-C0 byte-preservation guarantee is implied; returned dependency bytes are
compared mechanically. F_D refusals retain deterministic input/output evidence.

The admitted bridge input/output owns the historical-to-current join. The
evidence-input projection follows that actual bridge and current C1/C0/C2
producer relation while retaining the original Design/source envelope. Physical
preimage reads occur before construction; later evidence/result/replay uses the
admitted bridge join and stored C2 snapshots, not unchanged live worksite bytes.
The construction prompt exposes the computed historical-to-current target refs.
This ordinary tail Run reuses unchanged assessed stages; it is not a claim of
general D2 invalidation, application success, or full lifecycle closure.
