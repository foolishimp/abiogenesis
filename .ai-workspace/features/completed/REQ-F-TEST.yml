id: REQ-F-TEST
status: completed
satisfies:
  - REQ-F-TEST-001
  - REQ-F-TEST-002
description: >
  Restructure test suite to be integration-primary.
  Primary surface: command-level workflow scenarios exercising the F_D→F_P→F_H
  evaluator chain against real workspaces.
  Secondary: property invariant tests (replay determinism, idempotence,
  no duplicate edge_converged, stale spec_hash rejection).
  Minimal: write-primitive unit tests only (emit, project, EventStream).
  Remove unit tests that mirror implementation structure without testing seams.
