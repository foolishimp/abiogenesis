# Strategy: Three Goals To Closure — HoG, Re-entrance, Consciousness

**Status**: strategy commentary, not ratified specification
**Date**: 2026-07-06
**Scope**: consolidates today's discourse into three deliverable goals,
maps every open ticket to a closure condition, and fixes the execution
order. Companion to the Four-Recursions post (2026-07-05) and the
ratified HoG design (ABG_3_UNIFORM_C_CALL_ENVELOPE_DESIGN §1–§18.1).

---

## The frame, in three sentences

The envelope made every unit of compute visible and judged. The board
now carries exactly three goals, and every open ticket serves one of
them. Nothing is called delivered until the replay says so.

THE ACCEPTANCE CRITERION (user, governing this and every wave): the
deliverable is the atom set — robust and composable. Higher-order
decision networks (tuner, intent observer, lifecycle overlay, consensus
panels, and any number yet unnamed) are free constructions over the
atoms, each inheriting law for free. Work is judged by what it does to
atom robustness and composability, not by feature count.

## Goal 1 — Complete the HoG

One compute ontology, fully realized: programs interpreted, handlers
standard, envelope universal.

| Ticket | Closes when |
| --- | --- |
| T-200 | Ruling (a) executed: ONE spine-authority module (generator form) replaces the eight bracket bodies; design §8.1 amended; erase-able items erased; transitional clause narrowed to interpretation-pending; gate green with the -012 audit. |
| T-205 | Step-2 interpretation live at ONE seam (catalogs, selections, -017 ladders consumed; census from the admitted program); the four standard handlers shipped (pipeline + capability classes, obligations O1–O8); F_D mechanical-transform spine; odd_glc binding reduced to declarations. |

Delivers: envelope universality claimable for the first time; a
standard-path product ships ZERO plugin code; 4.5.0-rc.1 is the cut.

Success measure: the CCALL Realization State clause retires; archives ≡
spines on every arm of a live run; the data-mapper binding diff is
net-negative code, net-positive declarations.

## Goal 2 — Proper Re-entrance and Iteration

All three iteration heights lawful, wired, and used — the zoom gesture
completed: boundaries cheap going down, folds and landing points
computed coming up.

| Height | State | Work |
| --- | --- | --- |
| retry (same vector) | live (ladder, -017) | none |
| resume (same arc, frontier) | engine live (T-072/T-084); lane flag missing | T-030 run-18 spec: ODD_GLC_LIVE_RESUME; verify budget-refresh-on-ratified-resume |
| re-enter (upstream, consequence-routed) | machinery live (T-152 bridge, catalogs, foldbackTargetRef); nobody declares it | data-mapper declares its consequence catalog (failure class → landing vector); repaired_* linearized stages retire |

| Ticket | Closes when |
| --- | --- |
| T-030 | Resume mode wired; consequence catalog declared; campaign runs iterate IN the system (operator supplies only F_H fixes). |
| T-195 | The clean data-mapper run: converged, tests green, `data_mapper_full_sbt ok` — the outcome, not a narrative. |

Success measure: a fix costs one stage's wall-time, not a 50-minute
re-proof; convergence is a replay event; T-195's acceptance is the run
itself.

## Goal 3 — The Consciousness Loop

The tuner, bundled and first-class: reads replay, proposes terms,
never touches candidates or truth.

| Ticket | Closes when |
| --- | --- |
| T-206 | Per-configuration -012 cost projection lands; `tune report / propose / ratify` verbs live; staged-draft write path enforces §13.1; the default tuner ships as gtl://abg/tuner/* (drift-witnessed, overridable per -016); first ratified proposal round-trips (report → propose → human ratify → admitted ladder). |

Absorbed inside T-206: T-196 (witness rows) and T-201 (verdict
consumption) — its read surface, not separate waves.

Success measure: one real tuning cycle on data-mapper replay — a ladder
or proportionality change proposed from observed cost, ratified, and
visible in the next run's selection rows.

## Two top-tier items the three goals must not orphan

**Backlogged (user ruling) — Intent generation as a higher-order
system (T-207).** Not a Goal-3 prerequisite: intent generation sits ON
TOP of a product graph — an observer node that computes the declared-
vs-reality gap and FORCES RE-ENTRY at the height the gap implicates.
It composes from existing machinery (overlay frames, consequence-routed
re-entry, the change-class ladder) and sits above the tuner: T-206
adjusts terms within a set-point; T-207 revises the set-point. The
manual INT-008 reprice stays available as cheap hygiene whenever the
stale set-point becomes the binding constraint.

**Goal 4 (next arc, named now) — the factory recursion as data: the
GLC ticket-lifecycle overlay + release vector.** Release truth becomes
replay-derived instead of prose; ticket lifecycle becomes overlay data
traversed by ABG; odd_sdlc's 4,800 lines delete row by row as rungs are
earned (~odd_glc T-031, blocked only on Goal 1's interpretation seam).
Today's evidence for why it is top tier: the release-note sed-bump
class fired THREE times in one day and was caught by witnesses each
time — the release vector kills the class structurally instead of
catching it. Sequenced after Goal 2 (it needs re-entry heights for its
review/rework loops).

## Execution order (dependencies, no dates)

1. **T-200 closure** — spine-authority consolidation + erase + gate.
   Cut 4.5.0-rc.1.
2. **T-030 resume flag** (parallel-safe, odd_glc-side) — campaign
   iterates in-system immediately, even before T-205.
3. **T-205** — interpretation + handlers; odd_glc adoption converts the
   binding to declarations; consequence catalog declared here too.
4. **Clean data-mapper run** → **T-195 closes** (Goal 2 delivered).
5. **T-206** — the tuner over the accumulated campaign replay
   (Goal 3 delivered).
6. **Follow-on arcs (confirmed)**: Goal 4 — ticket-lifecycle overlay +
   release vector (~odd_glc T-031); T-207 — intent-generation observer
   (backlog, higher-order).

Out of scope, named honestly: legacy actives (B-010, T-092/094/095,
T-110, T-178/179) predate this arc — disposition in a separate hygiene
pass, not silently carried.

## One-line rule

```text
Three goals, five tickets, one order: finish the envelope, wire the
loop, then let the system read itself. Close nothing by narrative —
the replay is the signature.
```
