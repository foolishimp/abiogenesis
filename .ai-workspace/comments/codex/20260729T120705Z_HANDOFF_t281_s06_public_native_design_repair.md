# T-281 S06 Public And Native Design Repair Handoff

## Purpose

Review one exact bounded replacement for returned design `9fb14e68`.
Realization candidate `4953508d` remains returned evidence. The worker changed
no realization file and issues no semantic verdict.

The repair closes five decisions returned by independent design review:

1. the exact owner contract and port for all 56 public definition keys;
2. the common catalog, operation-row, invocation, outcome, and
   projection-refusal carriers;
3. local pending selectors versus linked semantic occurrences;
4. stable occurrence, target, and owner-relative dependency identity; and
5. packed versus installed Product verification.

Realization, Prime entropy reduction, S04, unified M5 freeze, M6, and M7 remain
held.

## Exact Subject

| Identity | Value |
|---|---|
| design commit | `8eb7564c04673cab26d938ad9bb2b026c1597d15` |
| tree | `9c753f86727fc1bc7fe0836f517d5157aa5de7d8` |
| parent | `1199add68f87d39b2b9b3be836b5a7099e989026` |
| returned design | `9fb14e6859af51e97789a599ad0fae6c367c34b3` |
| design file | `build_tenants/abiogenesis/typescript/design/M05_S06_PUBLIC_FUNCTION_AND_NATIVE_OCCURRENCE_CLOSURE_DESIGN.md` |
| design SHA-256 | `5c3e985c1895abd339e2ecda8c0617cc9a147caf0e3c86233f640ddde0418d35` |
| clarified requirement | `specification/requirements/product/REQ-P-POLICY.md` |
| requirement SHA-256 | `9567539aa176ec64ced2c4200a1980197a5d1da937a69d457d5643304641f9d3` |
| two-file aggregate SHA-256 | `9005bf7d170dda679b91ec13baa1784118644ff24209026ddab224402e868f4e` |

The aggregate is SHA-256 over the two displayed `shasum -a 256` output lines
in displayed path order. Review the blobs at the design commit. The later
status/evidence child does not alter them.

Accepted parent design remains:

| Identity | Value |
|---|---|
| native-closure design commit | `4f80f84a826de86b4cfb4d9fec3baff428dcb44a` |
| tree | `7070dca7d0f2ca90374b525faa60d5b810488763` |
| design SHA-256 | `ab44417157853490f4a3d8f9055b5eca8c295fd16f9615020b70e327f57c09fe` |

## Repaired Constraint Network

### Complete public source map

One parameterized owner-contract source map now expands to exactly 18
operation rows, 32 non-read members, and 24 read members. Every key locates:

- one exact owner schema export and member path;
- closed request, result, refusal, and optional non-terminal fields;
- defaults and metadata;
- one owner port; and
- deterministic native, schema, SDK, CLI, and operation-row projections.

The five interaction responses, six witness acts, and both `run.invoke`
members have exact structural relations. No missing owner or adapter convention
may complete a definition.

### Common carriers

`PublicContractCatalogCoordinate`, `PublicOperationContractRow`,
`PublicInvocation<K>`, and `PublicOutcome<K>` are exact carriers. Invocation
binds the selected catalog, definition, request, expected result/refusal/
non-terminal contracts, authority, correlation, time, and provenance.

Outcome is a closed result, non-terminal, semantic/admission refusal, or typed
projection-refusal union. The projection-refusal member has one native symbol,
one schema definition, one contract coordinate, and adapter exit `70`.

### Native phase separation

`NCC-F01` performs Product-local checking and emits only
`ContractIndexedPendingExternalSelector` evidence. It cannot assign a final
Product, symbol, contract, occurrence, binding, or lock identity.

`NCC-F02` alone constructs the linked Product-set checker, assigns each selector
one disposition, derives exact semantic occurrences and canonical target
identities, and binds each occurrence to one direct dependency contract.

### Identity and re-anchoring

Occurrence identity carries orthogonal `relationForm` and `semanticUse`
dimensions. Canonical target identity contains only stable Product, package
export, symbol-space, declaration-witness, and contract-closure coordinates;
TypeScript object identity and traversal order are excluded.

For `A -> B -> C`, A binds B's admitted boundary contract and stops. The
checker may follow B's contract meaning into C only to validate B. B evaluates
its own B-to-C relation under B's source contract and direct edge. A checker
alias destination cannot manufacture A-to-C authority.

### Verification clarification

`REQ-P-POLICY-049` now defines one closed target sum:

- `packed_artifact` admits locally decidable Product truth and pending external
  selectors before dependency resolution and forbids lock/install identity;
- `installed_artifact` requires the exact existing lock, installed Product,
  and install manifest; and
- `product.verify` never constructs linked cross-Product truth or a lock.

`product.resolve` remains the sole linked-resolution and lock-construction
authority.

## Design Module Method Evidence

The exact design contains:

- requirement traceability and global-to-local falsification;
- complete functions and composition;
- an affected Ontology with entities, relations, cardinality, and authority;
- whole-family Prime contraction;
- IACS and singular module/interface mapping;
- domain, sequence, and lifecycle Mermaid views;
- cross-view axiom evaluation;
- operational lifecycle and algorithmic constraints;
- realization projection and module proof; and
- positive and falsification obligations.

Both Ontology and design verdicts remain `candidate` pending independent
acceptance.

## Mechanical Readiness

| Check | Result |
|---|---|
| Pandoc GFM parse | `2/2` |
| Mermaid render with `mmdc 11.3.0` | `3/3` |
| operation identities | `18` |
| definition keys | `56` |
| project-read cases | `24` |
| owner source-map rows | `18` |
| requirement/design operation-set equality | exact |
| `git diff --check` | pass |
| realization files changed | `0` |

These establish readiness and exact identity, not semantic acceptance.

## Delta Review Questions

1. Does the parameterized source map completely determine schemas, fields,
   domains, defaults, metadata, and owner ports for all 56 keys?
2. Are the common catalog, operation-row, invocation, outcome, and
   projection-refusal contracts singular and constructable?
3. Does F01 stop at local pending selectors while F02 alone admits linked
   semantic occurrences, target contracts, and exact bindings?
4. Are occurrence dimensions orthogonal, canonical target identity stable,
   and A-to-B-to-C authority re-anchored at each admitted Product contract?
5. Does the requirement clarification preserve verify -> resolve -> install
   authority without a publication, verification, or resolution cycle?

If one of those relations still leaves materially different systems lawful,
return this exact design. Otherwise direct acceptance may authorize one bounded
realization pass against Section 11.
