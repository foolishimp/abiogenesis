# T-267 Self-Review: Traversal Result Interface And Bind Conservation

Date: 2026-07-13
Reviewer: Codex
Disposition: implementation is ready for delegated F_H closure
Implementation checkpoint: `f85bee83`

## Reviewed Boundary

T-267 compiles one exact T-255 selected-program handoff or T-260 structural
fan-out source into the existing compute-composition, compute-stage,
plugin-result-interface, bind-conservation, and TraversalUnit projection
families. The compiler consumes one admitted result authority from T-256/T-257,
T-258, deterministic target truth, or a T-259 through T-262 runtime atom. The
existing `typecheckGtlProgram(...)` remains the conformance judge.

The final gate distinguishes static contract closure, whole-program blocking,
capability blocking, and runtime-addressable-but-not-closed truth. Runtime entry
for declared F_P work consumes the exact T-256 request, prompt plan, selected
catalog GraphFunction, and runtime-addressable T-267 admission. F_H remains on
the separate public interaction path.

This boundary does not execute the Consensus graph, admit a worker or human
result, discharge an obligation, close a vector, publish tenant capability, or
create a Consensus-specific controller.

## Findings And Repairs

| Finding | Repair | Result |
|---|---|---|
| Current T-256 evidence refs initially entered static traversal identity | Separated stable source/result structure from current authority and evidence identity | two fresh T-256 closures retain one static authority and bundle |
| The first gate treated any nonzero report as failed static closure | Split exact T-267 static closure from stronger whole-program runtime addressability | unrelated report issues remain no-effect program blocks |
| Missing or malformed traversal rows could suppress the prior C-program diagnostic | Required zero T-267 row-family issues before suppression | both missing and malformed rows preserve the original diagnostic |
| Runtime startup still enforced the old unconditional T-267 fence | Joined the exact declared request, prompt plan, catalog binding, and runtime-addressable admission at canonical engine entry | valid F_P entry passes; missing, stale, foreign, or F_H prompt entry fails closed |
| T-252 proof initially described contracts without invoking all canonical joins | Compiled and admitted all 35 canonical traversal sources through the real generic compiler | 35 static units observed with zero T-267 conformance issues |
| The static result-authority carrier could be mutated and then used to recompile a new bundle | Recomputed its content digest and ref before every bundle compile | stale or widened authority identity is rejected before conformance |
| `capabilityDisposition` was carried but not covered by runtime admission identity | Included it in the admission digest and runtime assertion basis | capability mutation invalidates the admission |
| A synthetic transform before evaluate-owned work did not preserve carrier continuity | Bound its outputs to the exact work-stage inputs | explicit evaluate-owned non-Consensus fixture proves both stage joins |
| Projected unit matching accepted a bundle as a subset of a wider unit | Required exact target, closure, composition, stage, result-interface, consequence, and conservation sets | wider unit truth cannot authorize the submitted bundle |
| New public carriers made generated package inventories stale | Regenerated and rechecked the native inventories, catalog, and product toolchain manifest | package publication is exact again |

No remaining closure blocker was found in source reprojection, result-authority
admission, row compilation, conservation, conformance joining, capability
separation, runtime-start association, package exports, or the canonical T-252
projection.

## Residual Boundaries

- The canonical Consensus body remains unchanged. T-267 adds no product-specific
  branch, runtime loop, prompt shell, result parser, event family, or controller.
- All 35 canonical T-267 contracts close statically, but remain
  `static_contracts_admitted_program_blocked` because the full submitted
  structure still reports 5 C-algebra and 20 graph reachability issues.
- The only product gap in the ownership census is the T-268 canonical
  tenant-conformance-manifest coverage gap. No Consensus effect is yet
  authorized by this closure.
- Static result-interface authority is not runtime output truth. T-257, T-258,
  and existing runtime ledgers retain output, response, closure, and replay
  authority.
- T-267 currently admits one aggregate result-bearing work authority per
  TraversalUnit. It does not generalize arbitrary multi-result stage families.
- The pre-existing untracked `node_modules` link was not staged or modified.

## Observed Proof

| Gate | Observed result |
|---|---|
| `npm run test:t267` | GTL law 82/82; focused and inherited lane 50/50; packed public API 1/1 |
| Generic non-Consensus fixtures | F_P runtime join, F_D transform-owned work, evaluate-owned work, capability orthogonality, stale identity, malformed rows, and exact unit refusal |
| Canonical T-252 traversal census | 35 contracts; 35 program-blocked static admissions; zero T-267 conformance issues |
| `npm run test:semantic` | 1688/1688 after all repairs and generated publication refresh |
| T-223 package families | 70/70 dedicated run; included again in the final aggregate suite |
| T-250 drift families | 13/13 dedicated run; included again in the final aggregate suite |
| `npm run check:abg-product-publication` | 82 schemas; 40 generated assets over 1112 immutable payload files |
| `npm run test:design-mermaid` | 5/5; 42 diagrams across 14 registered files |
| strict TypeScript and semantic lint | passed |
| `git diff --check` | passed |

## Exit Judgment

T-267 is eligible for delegated F_H acceptance and closure. The canonical body
remains
`sha256:e4555c21cdb4292b64f7f4d5a625c2a520195aa8d6e9c759498eed4bf28d0ea0`.
The pre-closure probe manifest is
`sha256:ed420dede144c595ce639aeed4bea9967a822d79208eb28926b8a772378e5875`;
it reports one observed T-268 product gap, zero unowned or duplicate families,
and T-267 as active-owned-but-not-observed because the static gap has closed.
The ownership projection must be regenerated after moving T-267 to completed.
