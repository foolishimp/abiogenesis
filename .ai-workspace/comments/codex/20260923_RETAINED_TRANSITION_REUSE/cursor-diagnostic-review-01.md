Product Frame: ABIogenesis 5 retains all fifteen selected families. This T-287 slice restores lawful F03/F10 cursor provenance and recoverable failure evidence through F06 reads on the selected F17 continuation. GTL declares, HoG traverses, ABG alone admits runtime/effect truth, and replay/Public reads project it. Cursor-origin selection and diagnostic rendering are derived owner relations, not new execution entities or authority. Governing basis is STDO v2.5.1-rc.1 manifest `5d306da13994e69aa9f215d4c1cd2d0be96283c1e33a652b58e6e9262d036b64`, ABI5 `F-END-TO-END-INTERFACE-INTEGRATION`, Product/Owner/Reuse/Proof and the existing contraction frame. No Product relaxation, second runtime, private event writer or required process-local authority is authorized.

**CLOSED — SATISFIED for the exact composed source and bounded component proof. No supported blocking finding.** Independent astra/max Reviewer; Executive owns acceptance, checkpoint/publication and installed continuation. The prior witness, retained-transition and membership corrections remain accepted.

Exact subject: [cursor-01/subject.json](cursor-01/subject.json) SHA256 `958805566246bb8d1cf2331424b6a935f41b0bae118e06fb4b0fc455c30e564e`; [return.md](cursor-01/return.md) `9751f38effcaedf369bf750ca7d03ffcac7b4aa0db8b4e9b0ab5923084d7f700`; [freeze.json](cursor-01/freeze.json) `0689fdd54094ea239a98537bf3b2f6010248bd0ee837ca6db46df2f304085f0b`. All 26 successor/current path pairs match their selected hashes, and all 254 frozen records match. The seven-member diagnostic source handoff also matches the composed source, bound by source-freeze `7a4ba9ddbd96a8f623c2a2c49368f7786dd2f23d7d44e6e897e1f2bbdbbf4e61`. I inspected cursor source preimages/deltas and diagnostic emitted preimages/deltas against the frozen source and composed compile evidence; this does not claim that every diagnostic TypeScript preimage was separately retained. New content is `5edadeed02e13e5db2e1788c02849f6052d39989c9d57e4e93256f74088533b1`, manifest semantic digest `ab20cf39fb845f36451f470dd60bd489670f82ac24bc67130def6e9b1107ab87`.

The source correction preserves the required joins:

- `code/src/abg/traversal_cursor.ts:153–211` factors the already-supported entry, route-target and interaction-resume origins into one indexed relation. Selection binds Run, GraphCall, Frame, basis, aggregate role and cursor ref; callers retain digest checks. Resume uses the execution basis inside its admitted successor cursor, rather than confusing it with the continuation envelope basis. Initial-cursor admission still prevents a second initial cursor (`:338`). Warm reads and cold provenance now share the owner instead of demanding an extra entered event after an ordinary route.
- `code/src/abg/c_call.ts:3960–4005` obtains the workflow input digest through admitted sub-traversal evidence. It requires one origin with the exact cursor digest, preceding and first-causing the CCall open; one canonical-identity evidence event in the same scope; exact foldback identity, child basis and output joins; foldback-before-evidence-before-Result order; and the Result's evidence reference. Existing evidence admission checks the candidate input digest against its expected input (`:6038`). Entry/resume retain their direct input-digest check. This conserves an established owner proof; it does not replace the input join with a matching cursor ID.
- `code/src/abg/worksite_input_provenance.ts:194–272` preserves exact route-body membership, parent/child invocation and basis, source graph/result contract, successful Result/J, closure/foldback, retained raw reconstruction and output equality. The target must have the route itself as its unique matching origin. The existing relation-version function now includes source/target origins and CCall evidence (`:149–189`), so a later competing origin invalidates a previously projected result. Prefix-owned indexing preserves cut/scope exclusion. The accepted membership narrowing and retained-value reuse remain present.

The diagnostic representation respects existing reference meaning. `runtime_failure.ts:79–135` uses a standard self-contained JSON data URI as a locator for the exact diagnostic subject, then verifies canonical encoding and `subjectDigest`; cold projection also verifies the original failure ref/digest and Run/GraphCall/Frame/basis binding. This conclusion rests on the locator actually resolving the committed diagnostic and the existing owner verifying it, not merely on a permissive string schema. The diagnostic class remains explicit inside the subject. `run_invocation.ts:1682–1711` selects inline retention only for its caught exception's Definition key, class, stage, code, message and cause/stack. Generic failure candidates and child Result bodies are not automatically embedded.

The failure owner preserves explicit causation; otherwise `runtime_failure.ts:140–154` selects the greatest-ordinal material traversal event in the exact scope, excluding telemetry and other frames, with frame-open fallback. The existing active-run gate and post-open terminal check preserve first admitted cause. Error rendering retains native nested causes and AggregateError members; if both reporting attempts fail, the Effect failure retains the original Cause before both reporting failures (`run_invocation.ts:1742`). Ordinary historical route reads remain nullable, while staged projection throws the existing outer TypeError with its underlying replay error as `cause`. These mechanisms preserve evidence, not permission to continue a failed Run.

`project_read_ports.ts:1011` adds verified failure evidence to the exact canonical run context. `project_read_definition_bindings.ts:377` exposes that derived evidence as a data-URI ref plus its canonical digest through the existing `run_evidence` ref/digest schema. It does not change event profiles, Public schema or the failed-state algebra. Older digest-only records report `availability: not_retained` and `subject: null`; their absent body is not reconstructed or backfilled.

The retained proof supports this disposition. The exact predecessor projector fails and rolls back on the same temporary-store route-origin fixture; the successor stages, commits and cold-replays the retained input, with equal replay digest and durable coordinate. The executed baseline comparison is separately retained in `baseline-candidate-02.txt`; the ordinary combined lane otherwise injects the missing-provenance refusal. The material fixture uses the banked 47,037,803-byte carrier with unchanged digest `sha256:a09e3c8a62a0e8beb285e23b46a07ce58dfe00bd4304a89380441dc39fd046ac`, and commits/cold-projects it. Its 24.827 seconds and approximately 4.58 GB peak RSS cover the whole fixture, not isolated transition cost or an installed cure.

The composed 10/10 checks and compiler/generator pass are retained, not rerun. Entry/route/resume positives and scoped, ordering, evidence, input, closure and competing-origin refusals are exercised. Diagnostic checks cover actual event codec/admission and Event Calculus failure, closed/cloned cold read and a fresh process, Public adapter coordinates, malformed/substituted body and scope refusals, explicit/fallback frontier, first-cause preservation and staged Error.cause. Declaration/materialization, admitted bases, completed CCall carrier and frame-clock seams remain supplied premises; the diagnostic fixture also supplies its post-admission replay result. Full installed declaration admission, Public entry-to-exit execution and assessment are not proved by these components. AggregateError rendering and exhausted recovery composition were source-inspected rather than separately executed by the focused lane.

The initiating RCA return independently matches `f54431b9c2c8dbfda42254d8ffbef488e4a4aa4833d728f632b34f19abb8419d`, freeze `7ffb5389630a5a13fea34db0767e989b077bd9e8cd58e6ac2ebbb54619616c27`. Its native digest match remains a reconstruction of the old committed failure subject, not a native stack capture. This review did not read/replay the original 916 MB resource or rerun its actors. No extra diagnostic was needed; only this report was written. Installed continuation, independent assessment, truthful terminal Public delivery and RC qualification remain distinct unproved outcomes for Executive selection.
