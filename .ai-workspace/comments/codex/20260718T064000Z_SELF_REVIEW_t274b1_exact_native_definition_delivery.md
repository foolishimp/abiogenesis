# T-274B1 Exact Native-Definition Delivery

## Verdict

Accepted. T-274B1 is implementation-complete and unblocks T-270. T-274 remains
active for B2 publication and atomic P2 installed admission.

## Delivered Truth

- The repaired T-252 Module derives exactly 15 process-local native-definition
  relations from one frozen source family: three existing public runtime assets
  and twelve engine-private definitions.
- M04 total-joins the complete Module topology by symbolic schema ref plus
  contract coordinate. Repeated rows share one relation; missing, extra,
  duplicate, unused, nonselected, divergent-carrier, and forged relations
  refuse before capability construction.
- Source provenance is conserved through one opaque chain:
  `exact owner row -> resolved source -> native definition -> M04 relation`.
  This rejects the reproduced six-source counterfeit and exact-value source
  clones without adding Module metadata, a registry, a public carrier, or a
  Consensus branch to generic M04.
- The all-runtime-ref case remains lawful at zero requirements, zero bases, and
  zero capabilities. Scenario 09 proves the same generic relation outside
  Consensus.

## Proportional Boundary

Deliberately resolving an already-relabeled private source clone and then using
internal constructors remains outside the trusted-desktop boundary. It requires
hostile production-code misuse, not malformed GTL, malformed F_P output, or
public input. Adding a source-family registry for that case would be misplaced
hedging.

## Verification

- independent implementation review: accepted, no findings;
- full semantic suite before the final provenance increment: `1931/1931`;
- final focused T-270 relation lane: `10/10`;
- T-281 native projector lane: `9/9`;
- Consensus Prime lane: `6/6`;
- strict build, semantic lint, GTL guard, Prime gate, and diff check: passed;
- publication: 82 schemas and 40 assets verified from 1,250 payload files;
- packed public catalog: unchanged at 62 rows with digest
  `sha256:e0f2e4510184cf2d9295180bc681f4a0e74007bb8b23e1b2cf35ba2da7587bcf`;
- internal relation and provenance APIs are stripped from declarations and have
  no package or index export.
