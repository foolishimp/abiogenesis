# Bootstrap native-D2 acquisition — CLOSED at unsupported Public relation

**No supported core19 Public selector exposes the requested semantic parent/cause packets.** The required contract inspection established that boundary, so no event resource was acquired and no read attempt was spent. This is an interface limitation, not an observed refusal from the preserved Run and not an eligibility ruling.

Worker astra/xhigh; GOAL035/T287/T043, End-to-End Interface Integration with Owner/Conservation; ABG STDO2.5.1RC1 and GLC RC4. Reader is the already-verified immutable compiled19 archive `14f82f87f5ca76f2f10dfdaadc3621e8ce014a4ec898098d32d03d5e0f3f4408`. Six actual installed members match its archived hashes. Historical producer remains core18 `64093fb74a474c7e246333975c556c4cb34797326dbc81d1c5929455a69498bb`/dev8 `aef3df7a43da5d8af620c624a4c88f5eb30b6de26437458a8fd81156b6d877c9`, Run `3b04b7d2ca1e969fd8f6591d8ac013f1cec7338c7291d5807b45884b9af5df9e`, genuine close104,482,827B/`0db32d448006dbd43d3a303eea9648002798fa1bca8e5e1c4bec5a733433a7d1`. Original source/oracle and history remain untouched. [Exact reader/producer/request/member refs](refs.json).

The installed boundary is specific:

- `abg/project_read_operation_contracts.js:67–94,204–229,277–285` declares evidence as only `evidence`/`eventAtoms` ref-digest sets and CCall replay as subject/replay/page coordinates. It has no selector/output carrying an intermediate semantic Result value plus its CCall, admission event and judgment event.
- `abg/project_read_ports.js:776–805,891–908` already derives CCalls and event atoms inside the read owner. `abg/project_read_definition_bindings.js` functions `evidenceProjection` and `replayProjection` drop those detailed owner fields to the declared Public output. The `run_evidence` runtime-failure addition does not represent this typed semantic refusal.
- `run_status` can expose the admitted root execution-basis coordinate but not its semantic-job input or the required leaf pairs. `run_result`/`graph_call_result` expose typed terminal Results. The blocked workflow has none; native child terminal observations are not automatically the advancing Product or rejected Requirements semantic-fold leaf. `c_call_replay` cannot return its native CCall record through the present Public schema. No request-field spelling or current-reader catalog correction restores the missing output.

The refs record compares the exact requested fields against each relevant supported projection. `run_replay`/`graph_call_replay` add only status and nullable terminal Result to page identity; `workspace_replay`/`c_call_replay` expose no leaf bodies. `result_evidence`/`assessment_evidence` likewise return reference sets even when a source identity is already known.

The missing supported read relation is **exact Run/prefix → original semantic-job/root input and owner-authenticated intermediate Product-parent/Requirements-cause packets**, each retaining value and exact CCall/Result/result-admission/J provenance for semantic-revision ingress. A coordinate set, actor transport output, workflow terminal substitution or local event parser cannot supply it. The lower owner's existing data does not authorize bypassing the selected Public surface.

The retained bootstrap request was inspected and matches the original Run/close; cold-read02 shows the existing current-reader verification/catalog construction path. Neither was invoked after the unsupported-selector finding. Zero preparation calls, zero Public reads, zero acquisitions/appends/actors; native read elapsed/RSS and read receipts are inapplicable because no process started. The default heap/600s allowance was unchanged and unused. No source checkout module was imported, and no journal, worksite, source, package, Git or qualification effect occurred. Only this return/refs were written. Root owns any subsequent interface selection.
