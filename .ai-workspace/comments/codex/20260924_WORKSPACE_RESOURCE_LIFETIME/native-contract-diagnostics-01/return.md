Product Frame: ABIogenesis 5.0 retains the fixed fifteen Product families. GOAL-035/T-287 selects the continuing steel thread; this Worker addresses only the accepted S2 diagnostic loss at native author fold. GTL declares, HoG traverses, ABG owns admission/runtime truth, and GLC owns lifecycle meaning. Immutable definitions, mutable worksites and runtime observations remain separate. No example-product context enters core code.

Worker result: **produced**, returned to Root Executive. Local contract-detail preservation is **satisfied** on the frozen source and focused component evidence below. This is realization_refactor, not upstream law, admission or recovery change. No independent review, installed qualification, native outcome, release or acceptance is claimed. Worker stops editing with this handoff; Root owns disposition and any later activation.

Activation: `/root/native_contract_diagnostics`, astra/xhigh Worker under Root's explicit bounded grant. Selected frame: `repo://abiogenesis/build_tenants/abiogenesis/typescript/design/ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-end-to-end-interface-integration`. Exact starting and final HEAD: `701026970da59538e6dcc203ada4538b80e3d6de` in `/Users/jim/src/apps/abiogenesis` (`main`); the subject is the four-file uncommitted diff, not HEAD alone. GOALS/T-287 operator-stop tracking was already dirty and is conserved byte-for-byte in `postimages.json`.

Authority: current workspace/repo AGENTS, README, GOALS, Intent, Product, REQ-R-ABG3-INSTRUCTION-ASSEMBLY-014, accepted T287_D1_REQUIREMENT_LIFECYCLE_DESIGN Requirements-stage candidate/assessment ownership and T287_NATIVE_WORKSPACE_WORK_DESIGN ordinary failure/consumer-judgment boundaries were read. Exact STDO basis is `v2.5.1-rc.1`, commit `dc4742d08af0a1c42c437a4ce645ea7ca55f3a3a`, manifest SHA-256 `5d306da13994e69aa9f215d4c1cd2d0be96283c1e33a652b58e6e9262d036b64`. `stdo verify v2.5.1-rc.1` succeeded; full receipt is `stdo-verification.json`. Member inventory verification is distinct from substantive reading: bootstrap, Worker and legitimate-path clauses and the selected local interface frame were read for this activation. The stale map was unused; companion composition remains as pinned by `stdo_abiogenesis.json` with no map or companion-tool claim.

Closed input finding: `../requirements-failure-review-01/review.md`, independently supplied SHA-256 `203b726b134f626fc6dae2115657b1cc9b86c1bde95caa6150a611c6aa72b514`, rehashed equal. The investigation and rejected real candidates were not rerun or repaired. Root selected only finding 3, the existing computed contract issues being discarded on the native author path.

Source realization:

- `code/src/product/semantic_job.ts:426` retains the evaluator's issue array at the original failure branch (434-435) and reports it through an optional synchronous `onContractIssues` argument. The existing author derivation still returns `null`. `deriveNativeSemanticAsset` (759-768) forwards the observer while retaining the same native checks, single parse and derivation.
- `code/src/abg/semantic_job.ts:477` forwards the optional observer only on the existing author arm after its unchanged source/owner/task checks. Envelope-or-null returns and the assessor arm remain identical.
- `code/src/implementation/semantic_stage.ts:128` captures that exact array in the current invocation and passes it to the existing failure helper's `contractIssues` slot. There is no revalidation, extra envelope reconstruction, global state, cache, registry, new result algebra, exported helper, event kind or Public definition.

Failure/acceptance equivalence: all preconditions, validation predicates and branch order remain. Candidate issues still return `null` and `native_semantic_source_relation_mismatch`; the diagnostic URI remains `diagnostic://abiogenesis/semantic-stage/native_semantic_source_relation_mismatch@5`. Success retains the same envelope bytes for the same observation/provenance. Existing deterministic evidence hashes the now-more-informative failure payload, so that failure's output digest changes truthfully with its payload. The event format, effects, traversal, authority, timers and declarations are unchanged. The observer executes only after the existing validator finds issues; the production observer only assigns the frozen issue array. Earlier schema/source refusals and failures without computed issues retain generic detail. Assessment diagnostics are outside this increment.

Bounded tradeoff: an optional detail observer is threaded through three existing owner functions to preserve their current envelope-or-null contracts. It is local to the call and its sole production consumer is the existing failure carrier. This avoids a second whole-envelope/candidate validation or a new admitted carrier. No extra source/export file or design change was necessary.

Focused proof: one regression was added to existing `test_env/tests/t287-native-semantic-job.test.mjs`, using its existing fixture and the repository's established VM module seam technique. A real local Requirements-stage candidate with a same-response requirement reference in `asset.statements[0].requirementRefs` is paired with the nearest valid candidate whose field is empty; its incoming domain is empty. Actual Product parsing/validation, ABG native fold checks and implementation failure run. Upstream authentication, predecessor lookup, Program ownership and native-producer facts are explicit component doubles, never runtime events or admission. No journal is opened. The test proves:

- The invalid candidate still refuses with exact issue `{"path":"asset.statements[0].requirementRefs","rule":"eligible_reference_domain","expected":[],"actual":["component:requirement"]}`.
- The actual issue array survives Product → ABG → implementation by identity; the native derivation runs once per fold, without a diagnostic recovery call.
- Existing failure class/URI and deterministic input/output digest relations remain correct.
- The valid neighboring candidate succeeds with an envelope equal to the existing pure derivation.
- An unavailable native producer refuses before Product candidate derivation and carries no stale/borrowed issue array.

Actual checks, executed once each:

- `./node_modules/.bin/tsc -p tsconfig.json` from the tenant: exit 0, 5.747 s; `compile.json` / `compile.log`. This is current strict TypeScript typecheck and emit, not `npm run build`, packaging or manifest generation.
- `node --experimental-vm-modules --test --test-concurrency=1 test_env/tests/t287-native-semantic-job.test.mjs`: exit 0, six passed, zero failed/skipped/cancelled, 0.662 s; `focused-tests.json` / `focused-tests.log`. The VM experimental warning is retained. No broad suite, performance campaign, provider dispatch or paid/native actor was run.
- `git diff --check` over the four subject files: exit 0; `diff-check.json`.

Only these compiler-generated byte contents changed under the tenant; all 713 generated files were inventoried before/after. TypeScript may rewrite other outputs with identical bytes. Exact generated hashes are in `postimages.json`; no package manifest or install was regenerated:

- `build_tenants/abiogenesis/typescript/build/code/src/abg/semantic_job.d.ts`
- `build_tenants/abiogenesis/typescript/build/code/src/abg/semantic_job.js`
- `build_tenants/abiogenesis/typescript/build/code/src/implementation/semantic_stage.js`
- `build_tenants/abiogenesis/typescript/build/code/src/product/semantic_job.d.ts`
- `build_tenants/abiogenesis/typescript/build/code/src/product/semantic_job.js`

Frozen SHA-256 subject (paths relative to repository root):

| File | Preimage | Postimage |
|---|---|---|
| `build_tenants/abiogenesis/typescript/code/src/product/semantic_job.ts` | `e60893ce71f9325679f0d2005d35b20837d6847eb54a3e185716d844d5a674a2` | `a9c3f626aad25a23e009c13e28ab8eff52f844600bc1c801a550cff4d82b6421` |
| `build_tenants/abiogenesis/typescript/code/src/abg/semantic_job.ts` | `afe6844bff1433d04c245d2ccc1af640b3301c14144fd80030ee3627e8210c68` | `897f1e69a98fd35a19224e1ce8c31b3f47fd5210e612d478c96a6faf24a35323` |
| `build_tenants/abiogenesis/typescript/code/src/implementation/semantic_stage.ts` | `fb50fc45dba166c0c16427b01eb560c9d6149534a6d4cdca192f3b7cfc5e0a47` | `32fc80f9af9029fbcc8ab5ffcb84065373a8d76a3e41d4cd89b401bf168bf8e9` |
| `build_tenants/abiogenesis/typescript/test_env/tests/t287-native-semantic-job.test.mjs` | `352cc4736337eb367d1d0ecb21e9b9f43ea6d21d195d3af02dcd63b0095a60ff` | `fed90e4d8cf076c6b6db082d39de0a77bb9cc581fca81831287b1eafb49a957b` |

Exact diff: `subject.diff`, SHA-256 `a4c3cf370359cef18cd041887bdd5c8bcb8bc84a14c87571b95b8a5a2e4e3145`. Preimage bytes are retained under `preimages/`; `preimages.json`, `postimages.json`, `tracked-before.diff`, and the before/after tracked status bind the dirty-tree context. This is a source checkpoint; compile outputs are not a constructed immutable Product or installed subject.

Scope and residuals: contract/detail propagation is satisfied on this bounded malformed-reference case and its nearest valid neighbor. Actual installed end-to-end behavior and ordinary native-agent usability are unexercised/out of frame; six component passes do not close Product qualification. Runtime decisions, accepted domains and existing fail-closed authority are conserved by source inspection and the stated bounded cases; no universal runtime-equivalence proof is claimed. The three active core18 runs, frozen installs, workspaces, journals and controls were neither opened nor mutated by this activation. No rejected real candidate was edited, no retry/recovery was introduced, and no package/install/commit/push or Reviewer activation occurred. All writes were the three permitted sources, the one existing focused test, this commentary directory and inventoried tenant compiler outputs. Further effects require Root's next activation.
