Product Frame: ABIogenesis 5.0 retains its fixed fifteen Product families; this review covers T-287 F17/S06's Requirements-stage interface within the installed downstream lifecycle witness. GTL declares the Program, HoG traverses it, and ABG owns effects, admission and runtime truth. GLC supplies generic lifecycle meaning. Immutable Product/Program/WorkspaceBinding definitions remain distinct from mutable subject assets and admitted observations. Hello and Data Mapper confer no special substrate behavior. The accepted fresh-native selection uses twelve sequential occurrences and three actor families, not the post-5.0 optimizer design.

This is a closed, independent Reviewer return to Root Executive, not disposition or mutation authority. Result: **falsified** for successful Requirements progression in these exact attempts. Both stops have valid grounds; the JavaScript assessor also makes an unsupported claim. No admission-correctness defect or runtime exception is established. One bounded diagnostic-usability defect is established.

Authority: ABG selects STDO `v2.5.1-rc.1`, commit `dc4742d08af0a1c42c437a4ce645ea7ca55f3a3a`; its manifest and the read baseline, SPEC_METHOD and DESIGN_MODULE_METHOD members verified against that manifest. GLC retains `v2.5.0-rc.4`. Applicable routes: `ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-end-to-end-interface-integration`; Derived Reviewer; baseline legitimate-path clauses 981-1004; SPEC_METHOD proportionality 1413-1470; DESIGN_MODULE_METHOD computational projection 1510-1556. The stale map was unused. GLC Product lines 34-70 and accepted `ODD_GLC_NATIVE_INTENT_ASSET_TRANSITION.md:434-442` govern the selected lifecycle.

Exact subjects:

- `B=/Users/jim/src/apps/odd_glc/.ai-workspace/comments/codex/20260924_HELLO_PROJECT_BASELINE/full-native-remaining-01`.
- `P=B/parallel-core18-preparation-01`; case job evidence is `P/resources/<case>/jobs/<case>/`; subject assets are `B/<case>/subject-<case>/semantic-assets/`.
- Frozen core18 archive: `64093fb74a474c7e246333975c556c4cb34797326dbc81d1c5929455a69498bb`; dev8: `aef3df7a43da5d8af620c624a4c88f5eb30b6de26437458a8fd81156b6d877c9`. Coordinates agree with ABG `20260923_COMPOSITE_READINESS/compiled-18/selected-core.json` and case preparation.
- Rust stage-2 SHA-256: `1af1b125549c0ec0711bf50710c4a50cd57d5e5d3aa0b57a8bafe43e0596e84e`; JavaScript stage-2: `bb93b54aed07ea9c68a91d1aa152b80f972d08ee4523ffc0421cc68b0d7d5fc1`. Both were independently hashed.
- Rust contract/archive: `archives/fp-d9c699d6019788b6-prompt.txt`, corresponding transport `finalOutput`, and `causal-capsule.json`. JavaScript author: `fp-fa71974e9d9722cc`; independent Requirements assessor: `fp-3e6cd7ba47a0a5f2-transport.json#/finalOutput`. Its SHA-256 is `1188c4bc55540b5e7b5d25328d25a4bf967de55badae1ea7f2e8119389ae8df5`.

Ranked findings:

1. **Rust: candidate reference-domain violation; valid deterministic refusal.** Seventeen statement arrays use newly authored `requirement-candidate:*` references while the actual incoming `contract.requirementRefs` is empty. The contract separately permits same-response references in candidate bindings. The author's final report explicitly acknowledges using candidates despite uncertainty about incoming-only fields. Product `semantic_job.ts:416-421,434,758-766` rejects this input, and ABG `semantic_job.ts:477-487` folds that refusal. The capsule binds the actual observed candidate bytes, not a reconstruction. This prevents Requirements admission and hence Design/construction. The empty domain was supplied, so no hidden-rule failure is established; however, the schema lacks an explicit field-level description connecting statement requirementRefs to that domain. One rejected response cannot establish generally inadequate context. Bounded candidate correction is needed, not broader admissibility.

2. **JavaScript: two material conservation defects support refusal (S2 candidate defects).** The actual original source is `original-js-sdlc-bootstrap-declaration.txt`, SHA-256 `c799b9e43d660f3abc3adf0da32baab17a771f1efe20843c4688162b9e62ced4`. Its admitted Product asset, SHA-256 `37cd402698f52fd722a6f00b20566e03c8037daeef7ce10fb5f53744c26fec9a`, preserves the typed-observation obligation in `product-pressure:06`. Requirements `req-pressure:06` drops that rule and binding candidate 01 permits passing assertions alone. REQ-GLC-WORKSITE-LIFECYCLE-013 (file lines 74-81), also present in the actual author prompt, requires exactly one typed observation per declared outcome predicate inside the ABI-owned Worker turn. Passing tests without an admitted return observation is a concrete counterexample.

   Candidate 13 separately replaces conformance-to-design-to-source authority with end-state consistency. Mutually consistent documents retrospectively fitted to code satisfy that candidate while reversing the source's authority direction. Preserving that direction does not require reinstating eight legacy vectors. Requirements statement 21's claim that no normative meaning was lost is therefore false. These grounds survive removal of the assessor's unsupported argument.

3. **Rust: computed diagnostics are discarded (S2 usability defect).** `evaluateSemanticJobActorCandidate` computes path/rule/expected/actual issues; `deriveSemanticJobAsset:434` reduces them to `null`; native fold emits only `native_semantic_source_relation_mismatch@5`. `implementation/semantic_stage.ts:33-44` already supports optional `contractIssues`, but its native-author-fold call at 130 passes none. The installed core18 JavaScript has the same behavior. This explains why an ordinary bad reference required deeper closed-evidence diagnosis. It does not cause the bad candidate or invalidate refusal. A bounded remedy would preserve already-computed diagnostics through the existing refusal carrier, subject to ordinary owner admission; no new diagnostic API or repeated validator implementation is justified.

4. **JavaScript: same-run rejection argument is unsupported.** REQ-GLC-LIFECYCLE-VOCABULARY-ASSETS-020 (135-139) distinguishes evidence roles, not run identities. Bindings 11/12 distinguish the realized execution-result surface, verifier files, their execution and independent semantic assessment. One admitted run can supply separately attributed facts for these roles. The assessor identifies no lost fact or independence breach from shared run identity alone. Distinct observations must still exist where required by 013; a second run is not implied. Historical vector names remaining recoverable from exact source are likewise insufficient grounds to demand duplicated stage text or restore historical runtime authority.

Recommendation to Root: retain the failed/blocked attempts and valid predecessor work. Correct only the stated Requirements defects in any subsequently authorized subject; do not rewrite admission or discard the useful JavaScript requirements. Root's supplied recovery check excludes current D2: it requires a successful constructed parent, which neither subject has. The accepted fresh-native experiment grants no retry. Root must select any lawful successor/re-entry and its exact grant before further actors; no automatic retry follows from this review.

Closed replay reports Rust `failed`, JavaScript `blocked`; completed Result is absent. General success probability remains unknown. The cross-stage source union at `native-lifecycle-declarations.mjs:236-245` is confirmed; prompt volume as a cause is unmeasured. No tests, builds, actors, journal reads, archive expansion, candidate/specification/ticket/Git edits or further agents were used. Only this commentary return was written. Root owns conjunction and disposition.
