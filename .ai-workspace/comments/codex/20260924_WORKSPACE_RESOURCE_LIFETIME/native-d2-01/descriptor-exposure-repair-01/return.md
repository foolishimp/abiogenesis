# CLOSED — five native-D2 descriptor exports

Worker astra/xhigh; realization_refactor under T287/T043, E2E / Owner / Conservation / Code Construction. [Frozen subject](frozen-subject.json) records exact preimages/postimages.

`implementation/semantic_revision.ts` now exports the five already-constructed native Intake, Request, Construction, Execution and Evidence descriptors individually. The original seven exports, loader, functions, contracts and graph identities are unchanged. The source delta is +3/−1 lines plus one focused existing-file test; ordinary compilation updates the emitted module.

Compile passed in 5.753s. The existing focused native revision file passes all three checks in 0.931s. Its new regression derives all twelve required bindings from the published declaration and exercises the actual descriptor loader against the real emitted module namespace; only installed-byte custody is supplied by a component seam. It never reads the descriptor array directly. Installed package correspondence/discovery remains the next selected check before setup.

Predecessor [diagnostic](../bootstrap-resolution-diagnostic-01/return.md) establishes the five-export omission; setup02 and its successful admitted effects remain immutable. No native acquisition, eligible-cause, selector or correction execution is claimed by these checks. Root authorizes the conditional core22/dev12 installed path; source is frozen.
