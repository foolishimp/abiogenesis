**Product Frame.** The fixed fifteen-family ABG5 outcome remains A5-F01–F07, F09–F11 and F13–F17 under GOAL035/T287/T043. This diagnosis concerns the installed catalog boundary required by the generic native lifecycle correction and Product F17/S06, QUAL062 and SCENARIOS013. GTL declares; HoG traverses; ABG admits runtime truth. Generic core owns mechanics; GLC owns lifecycle policy and interpretation. Publications and Catalogs are immutable definitions; their factories are deterministic construction tools; the actual refusal is observed evidence. Trusted single-developer laptop scope applies. No compiler/lowered Program, hidden selector, caller traversal/controller, private writer, rival runtime/catalog, fabricated origin or hostile-operation hardening is authorized.

ABI's Definition-selected STDO 2.5.1 RC1 and GLC's 2.5.0 RC4 remain the governing bases. The selected frame is `repo://abiogenesis/build_tenants/abiogenesis/typescript/design/ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-end-to-end-interface-integration`, composed with Owner, Conservation and Code Construction. Prior source/component acceptance and core20/dev10 package readiness retain their recorded scope.

**Closed finding CATALOG-R01 — S1 selected-path blocker: the GTL native-construction factory emits a duplicate carried contract.** Confidence is high. The installed refusal is correct for the malformed published graph; this first refusal is a producer/consumer congruence defect. The catalog validator is enforcing its existing rule, and the request preserves the installed publication exactly.

The first failing publication is index 10, `module://odd-glc/generic-lifecycle-witness@5`; the ten preceding ABI publications validate. Installed `validatePublishedDeclarations` rejects `graph-function://odd-glc/native-semantic-revision/construction@5.environment.carries` because `contract://abiogenesis/semantic-revision/envelope@5` occurs twice. It has seven entries but six distinct references. This yields the sole `duplicate_identity` diagnostic at `$.graphFunctions[graph-function://odd-glc/native-semantic-revision/construction@5].environment.carries`.

The exact producer is [semantic_revision_publication.ts:107](/Users/jim/src/apps/abiogenesis/build_tenants/abiogenesis/typescript/code/src/gtl/semantic_revision_publication.ts:107), installed as [semantic_revision_publication.js:102](/var/folders/rz/r6wxvr0n15d906k2s0jw8j2h0000gn/T/abi5-composite20-install-xp2g82ge/node_modules/@abiogenesis/typescript-tenant/build/code/src/gtl/semantic_revision_publication.js:102). `nativeConstructionGraph` constructs `[ids.envelopeContractRef, bound, ...nodes.map(node => node.term.outputCarrierRef)]`; the final `/evidence` node already outputs that envelope.

GLC invokes this generic factory with `role: "nativeConstruction"` at installed `native-lifecycle-declarations.mjs:292,316`. The prepared declaration and dev10 installed payload preserve its exact canonical graph. `materializeD1Publication` binds artifact coordinates/provenance without changing that graph. Canonicalization sorts the carrier inventory and preserves duplicate occurrences. The frozen request exactly matches this materialized installed publication, and its semantic digest `4551c58efa1c82e9fd36792dc4698476ee086bf9cda3d2c02c8f8e26e94f9e21` matches the verified manifest binding.

The installed catalog path is `CatalogOperationPort.admit` → `constructReadyCatalog` → raw publication/contribution admission → `validatePublication` → `validatePublishedDeclarations` → `appendDuplicateDiagnostics(environment.carries)`. `catalog_operations.js:93–95` wraps the static rejection as `catalog_validation_refusal`; `catalog_definition_bindings.js:131–133` projects it to the observed `malformed_contribution`, `/contributionManifests`. The helper's result-only assertion is downstream of this refusal. No exception caused it.

**Minimum lawful correction:** a local `realization_refactor` at the existing GTL factory should construct this carrier inventory uniquely while retaining all six required references. Retain the current validator and exact-factory retention guard. Then derive successor declaration/package identities through existing owners; preserve the historical lifecycle and original cuts. Caller-side cleanup alone is insufficient: a diagnostic in-memory copy with only the duplicate removed passes the full publication validator, but fails the unchanged `isNativeSemanticRevisionGraphFunction` exact-factory predicate. No validator relaxation, GLC policy change or runtime redesign is indicated.

**One focused falsifier:** the complete historical-lifecycle-plus-intake/suffix publication produced through the corrected factory must pass ordinary raw admission and full `validatePublication`, while its native-construction graph remains accepted by that same factory's retention predicate. A remaining duplicate refusal falsifies the correction. This targets the previously untested publication boundary; Program-validation success does not establish this predicate.

Two bounded pure probes used the frozen request and actual installed code; neither admitted a Catalog nor touched runtime resources. The single-field diagnostic preserves lifecycle digest `89e672a24697bc653745ff5066a379ec681fee9f2d03bef8a93e0e6b4c2e2267`, shared by the prepared publication, installed payload and request. The entire installed intake setup is blocked because catalog validation covers the supplied publication, even though intake itself does not invoke construction. Containment is fail-closed admission; no Run or actor launched.

Later catalog readiness/conservation, allowlist, conformance, resolution, source eligibility and native correction remain unexecuted. The 337.565s/2.79GB preparation observation has no causal attribution from this diagnosis. No history/replay read, retry, rebuild, source/design/install edit, package operation or broader review occurred. Root owns repair and continuation; this diagnostic is closed. Exact identities and source routes are in [evidence-refs.json](evidence-refs.json).
