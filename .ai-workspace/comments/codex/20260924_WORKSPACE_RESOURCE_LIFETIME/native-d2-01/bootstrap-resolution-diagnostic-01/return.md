# CLOSED — installed native-D2 descriptor exposure

Worker astra/xhigh; E2E / Owner / Conservation / Code Construction. This is a new diagnostic observation of preparation02's same resolver relation; the missing original resolver response remains unavailable.

The exact installed resolver refuses `absent` at `implementation`: **no packaged LeafImplementation matches the validated executable requirement** ([full response](resolution-result.json)). Catalog, allowlist and Program conformance had passed. No native intake or actor started.

One authorized existing-owner acquisition recreated the authentic install-admission basis, invoked the same resolver once, then genuinely closed at the unchanged 109,136,203-byte prefix (`65a79fbc…`; [close](close-receipt.json)). Acquisition 2.712 s, current environment projection 19.467 s, resolution 8.100 s; process 30.729 s, peak RSS 1,252,458,496 B. These are measured operation intervals, not a full causal profile. No events appended, admission or application effect.

The installed namespace contains seven top-level descriptors and an array of twelve. `product/implementation_resolution.ts:206` intentionally selects only top-level exports satisfying the descriptor guard. `implementation/semantic_revision.ts:25–27` exports only the original seven descriptors individually. All five new functions and exact matching descriptor values already exist, but are offered only inside that array:

| Missing top-level descriptor | Binding / implementation suffix | Published use |
|---|---|---|
| NativeIntake | `semantic-revision/native-intake@5` | intake |
| NativeRequest | `semantic-revision/native-request@5` | intake |
| NativeConstruction | `semantic-revision/native-construction@5` | construction repair; from Intent/Product/Requirements/Design |
| NativeExecution | `semantic-revision/native-execution@5` | same five suffixes |
| NativeEvidence | `semantic-revision/native-evidence@5` | same five suffixes |

[Exact required versus offered coordinates](descriptor-comparison.json) binds every package/module/symbol/contract/descriptor digest to the retained conformance catalog and immutable core21. The comparison invokes no resolver or runtime owner. The producer is the existing implementation module's export inventory. The smallest correction is to export those five already-constructed values individually; preserve the loader, contracts and historical graph identity. Public resolution currently discards the inner requirementKey, so the original first leaf cannot be claimed from the top-level refusal alone; the finite comparison establishes the complete five-leaf omission.

Core21 archive `6e8dc1b785abdbc439cdedc78751deee3bc9020d69a6ceba1c81c8854d0a7028`; retained input and source/module hashes are in the comparison. Original preparation is [CLOSED failed](../bootstrap-preparation-02/return.md). No runtime/eligibility/correction qualification is claimed.
