# NATIVE-D2-01 frozen Worker return

Disposition: **source/component ready for the one independent review; installed/native qualification remains open.** No real correction was invoked. The source, design and focused tests are frozen; the Worker stops here.

Executive authority is `executive-selection.md` SHA256 `859f3ca3f5029fe3aad2766c8524d4ba4255385b584ffd45d6b2d8a2cd36232d`, including the later exact role-classifier and current-capacity grants. This is one bounded design reframe of existing D2/native-work HOW under T287/GOAL035, T043 and the end-to-end interface frame. ABI exact STDO 2.5.1 RC1 and GLC RC4 remain selected. Product outcomes, Public operations, family and runtime authority are unchanged.

## Frozen subject

- `frozen-subject.json` SHA256 `0ed98c98e3a28b53da8b9a71e428b823f5734ff1e58dd2db820c42deea5681c8` inventories exact pre/post hashes: 13 production files (+676/-107 lines), 3 existing HOW documents, 2 focused test files. It is relative to the Root-supplied paired checkpoints ABG `5c4245ea900fddea9911e73183f5fb7b0631ce91`, GLC `7724346a477691d69ddc2b08ff7290fb92027cb0`.
- `source-design-tests.diff` SHA256 `d9622836b089f91b7a8c6da6707ea2dfd271eac459a0d625059d799b2ea9f694` is the exact delta. Original bytes remain under `preimage/`; additional exact grants are recorded in `additional-preimage.json` and `test-preimage.json`.
- `compiler-output-delta.json` inventories the 19 changed generated files under the ABI tenant's `build/code/src` only. No other generated output was written.

## Supported route

1. `program://odd-glc/native-semantic-revision/intake@5` takes `{kind:"native_semantic_revision_intake",schemaVersion:"5.0.0",sourceRun:{ref,digest},sourcePrefix}`. The F_D owner authenticates the public prefix/Run, original job/root, unique eligible cause, successful advancing semantic predecessor, current workspace/binding relation and phase through existing owners. Missing/ambiguous causes or parents refuse explicitly; no chronological latest selection occurs.
2. Preconstruction retains actual rejected candidate/assessment as counterevidence and has no construction coordinate or target selection. Postconstruction requires the real native construction source. The admitted acquisition supplies the existing F_P affectedness selector, then F_D seals the existing `SemanticRevisionRequest`. Ordinary terminal read returns the request; no caller extracts private leaf ids.
3. A second ordinary invocation selects `program://odd-glc/native-semantic-revision/<entry>@5`, where entry is `construction-repair`, `from-intent`, `from-product`, `from-requirements`, `from-design` or `from-evidence`. The projection requires the exact admitted selection. HoG traverses that complete declared suffix.
4. Existing D2 semantic actors preserve source, oracle, root, unaffected assets and obligation/binding history. Native construction restricts affected write paths; native C2 snapshots all required targets/dependencies; Evidence uses actual native Result/J coordinates. Evidence-only semantic re-entry retains its authenticated prior evidence. Native observations never become C0/C1 parents.

The implementation intake stamps the existing selector's current execution limits into immutable operating context. New revision author/assessor contracts and readiness use the existing Product budget calculation. Old job bytes, missing legacy fields and historical task correspondence remain unchanged. New construction refuses a changed current selection. Existing diagnostic-retention behavior and legacy input arms are preserved; accepted unrelated repairs were not re-reviewed.

## Checks and limits

`checks.json` retains every actual command, status and log hash. Final `tsc -p tsconfig.json` passed (`build-05.log`). Final focused command passed **2 tests, 0 failures, 0 skips** (`focused-03.log`). Earlier local compile errors, a GTL result-cardinality refusal, a fixture contract-digest error and the concrete Evidence-entry comparison failure are retained, not hidden. The Evidence fix compares the preparation's actual graph entry with the retained entry, using the declared factory relation.

The component proof exercises native rejected Requirements → public-coordinate intake → absent/ambiguous refusal → recovered original root → admitted selection/request → matching suffix projection → Requirements/Design correction → native construction/C2 → fresh Evidence. It checks current limits on both new role contracts while the old job retains no limits field, preserves accepted predecessors, rejects a future-stage selection/wrong suffix, and refuses malformed native snapshot evidence. Actual GTL validation covers all 8 fresh/intake/suffix Programs, exact Program-scoped role admission, unknown bindings and wrong retention owner/factory neighbors.

The tests use temporary component files, supplied prefix ancestry/leaf/admission/authentication facts, and synthetic command/actor receipts. No actor, helper command, journal, paid run or installed admission was performed. Postconstruction intake and Evidence-only correction are declared and compiled, but this focused execution does not separately qualify those branches or the physical workspace-binding transition. The existing budget formula's aggregate boundaries remain prior accepted evidence; this check adds old-job/new-context composition, not a new budget campaign.

## Remaining actual input

No complete genuinely eligible source has been acquired. Successful tenant/service outcomes supply no invented failure. The parallel preflight remains budget pressure, not proof of application correction. Bootstrap has a real rejected Requirements shape, but its exact source relations and frozen workspace still need ordinary acquisition.

**Lifecycle byte equality remains a refusal.** The current convenience factory constructs the current lifecycle and exposes no historical-lifecycle selector. The smallest existing supported declaration route is to select the exact original lifecycle bytes from the retained original GLC Product publication, author them unchanged through `gtl.constructSemanticJobLifecycleDeclaration`/`gtl.modulePublication`, and construct/validate the new correction graphs against those exact stages through the existing graph factories. The resulting Product/publication/package selection must be explicit; equal declaration references are insufficient. This was not constructed or installed here. If the historical publication cannot be selected intact, this worker provides no ready bootstrap start and no lifecycle-successor mechanism. No private leaf extraction or old Run mutation is a substitute.

No Git, package/install, running-resource, control, actor, application-worksite, tracking or Reviewer effect occurred. The separate Writer's selection and trackers were preserved.
