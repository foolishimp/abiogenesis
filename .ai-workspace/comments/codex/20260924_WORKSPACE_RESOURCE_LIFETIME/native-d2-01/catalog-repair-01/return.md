CLOSED source correction — astra/xhigh Worker.

The existing nativeConstructionGraph carries inventory now uses Set uniqueness: one production line, preserving all six references and the same exact-factory retention guard. Existing native-revision test adds complete publication validation, retained factory acceptance and duplicate rejection, using distinct component artifact coordinates. No schema, authority, graph progression or lifecycle change. Compile passed; two targeted tests passed (0 fail). The first check omitted the existing VM flag; a later fixture check exposed duplicate zero provenance, corrected only in the test materialization. All first failures remain. Source/emitted identities are frozen in frozen-subject.json; installed whole prospective publication and historical lifecycle equality remain the next package discriminator. No actor or runtime attempt yet.
