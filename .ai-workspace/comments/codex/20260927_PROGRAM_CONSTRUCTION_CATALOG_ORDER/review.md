Product Frame: the fixed fifteen-family ABIogenesis 5.0 Product remains the destination. GOAL-035/T-287's selected installed program-construction thread exposed a catalog-readiness defect before a Run. This bounded `realization_refactor` preserves the ordered ProductSet and exact lock required by REQ-P-INSTALL-049/051. The catalog is a deterministic validation/index over immutable Product, lock and workspace-binding coordinates; it gains no event lifecycle, runtime controller, local ledger or GLC policy. GTL, HoG, effect, admission and replay owners retain their existing responsibilities. Core45 and native44 retain their prior scopes; S06, exact qualification and release acceptance remain open.

**Closed result: `satisfied` for the frozen source correction and bounded pure-owner regression. No actionable finding; severity and technical triage are `not_applicable`.** Executive retains acceptance, priority, disposition and subsequent packaging/installed-execution authority.

Activation: `T287_PROGRAM_CONSTRUCTION_CATALOG_ORDER_REVIEW_01`, 2026-09-27; independent Reviewer `/root/program_construction_source_review`. Root's separate Worker produced the patch and stopped; this Reviewer authored none of its code or tests. The selected ABI method is STDO `v2.5.1-rc.1`, manifest `5d306da13994e69aa9f215d4c1cd2d0be96283c1e33a652b58e6e9262d036b64`, as selected by the current Product Definition. I reused the unchanged verification basis cited by the freeze and the closed calculus review, and directly read the exact installed closed-result, Reviewer, Owner and Proof relations. The project refinement is `ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-end-to-end-interface-integration`, with source/contract congruence and pure-owner behavior selected. GLC's separate RC4 selection and accepted source reviews are preserved.

The exact subject is [freeze.json](freeze.json), SHA-256 `5cc9b862cbb15ed106b9c037951c38086bd4c78452efdb45f6527b8328b952a3`. All eight frozen members match their declared bytes and hashes, including:

- `code/src/product/catalog.ts`: `bd2a160d28f7a8b6c67dcbfb3fc559ea3d0d28059f2dc873c5df530b3639c75a`.
- emitted `build/code/src/product/catalog.js`: `be43625ca325855297a70e22707f951e4773b0594fbb4b4e0d021527758605a1`.
- `regression.mjs`: `2f3d2606b2bff5206731f299c3811b480b288550c55f4074685f8aacde15f59d`.
- `regression-result.json`: `abdf73520a33075bf961fb3089724aa91c0ace2111ddac70db307d280b48a6da`.

The first two paths are relative to `build_tenants/abiogenesis/typescript/`; proof paths are relative to this directory. Direct comparison with selected core45's installed `catalog.js` (`080871d43df3a9f4941d72fa90b6711c2e4e92df26cbf24cf639584f1ef2d387`) confirms the emitted runtime delta is exactly the installed-product sort removal and its explanatory comment.

The correction has the correct owner and preserves the required relation. `REQ-P-INSTALL.md:286-302` requires an ordered exact Product set and the exact resolved lock. `environment.ts:290-294` preserves artifact order in lock rows; `environment.ts:891-925` checks that order and derives ProductSet identity from `orderedInstallRefs`. Previously `catalog.ts:407-408` sorted the selected installs by product id before reconstructing the ProductSet at line 484. That transformed a valid ordered binding into a different sequence and provoked `binding_lock_mismatch`. The successor copies the selected install sequence without sorting it. Exact lock, identity, content and ProductSet checks remain intact. Independent verified-product and publication collections retain their existing canonical ordering. No caller-side reorder, weakened comparison or substitute lock is introduced.

The retained gzip fixture is the finite actual `abg.operation.catalog.admit` call, not a journal. I independently verified its expanded length, 33,590,677 bytes, and SHA-256 `49f803a2d35a50086e0887a45b2a1986821f36b8d93ce2a0062f7aa833567b38`, matching both input provenance and regression result. Its lock and admitted-install sequences both read ABI, `odd_glc/route-one-typescript`, then `odd-glc/program-construction-fixture` dev.2. That is the material non-lexical order. Its original ProductSet digest is `e511ce4cb103d75db8e19fa45bcac13a8fbe666820b316cae9b6575b60750ade`.

The regression projects the retained admitted resource rows into the existing candidate inputs and invokes the actual `CatalogOperationPort.admit` owners. It does not fabricate runtime admission. I inspected all six assertions and reused their matching result: unchanged core45 reproduces the refusal; the corrected owner returns a catalog for the same input; selected install order and workspace ProductSet are conserved; reversing installs under the unchanged lock refuses; reversing independent verified-product/publication collections preserves the readiness-basis digest; and omitting an install still refuses. The result records six passes in 1.238 seconds. The frozen build record reports `npm run build` exit 0 and empty stderr. No additional tests were run by this Reviewer.

The original failure is fail-closed loss of a valid catalog path, not demonstrated false admission or historical corruption. The repair is confined to deterministic construction and leaves mismatch refusal effective. Previously valid lexical-order inputs retain that same sequence; the newly accepted case is the valid non-lexical order already selected by its lock. Installed successor behavior remains a distinct, unproved boundary.

**Acceptance scope:** source/build congruence and the six pure-owner predicates support accepting this bounded catalog-order repair. They do not establish a new installed core's identity, successful installed catalog setup, authenticated native44 continuation, positive/negative Program execution, fresh readback, full qualification or RC1 release readiness. Package/install and actual installed continuation must use their separately frozen successor. No GLC computation, source-interpretation, prior source acceptance or original-task/S06 residual is repriced by this result.

The sole write in this activation is this review. No code, test, package, worksite, history, Git, actor or runtime mutation occurred. Material changes to the frozen code/build, lock/ProductSet contract, selected installed baseline, fixture, method or claim invalidate the affected evaluation. Reviewer is CLOSED and returns to Executive.
