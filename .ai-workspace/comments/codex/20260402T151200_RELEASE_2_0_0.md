# RELEASE 2.0.0

Date: 2026-04-02
Branch at cut: `main`
Release intent: first full GTL/ABG 2.0 release

## Scope

This release closes the rewrite from transitional graph-function globalization to
operative recursive locality.

The active surface now ships:
- stable published module carrier
- invocation-local recursive frames
- explicit GTL recursion law with declared fold-back
- fail-closed frame-local publication
- parent rebind plus parent re-evaluation before certification
- fresh frame-attempt identity on reset/reopen
- tail-loop recursive machine control with explicit continuation/frontier state
- compressed precreated install-time GTL bootstrap

## Version Surfaces

- `pyproject.toml` -> `2.0.0`
- `build_tenants/abiogenesis/python/code/gen-install.py` -> `2.0.0`
- `build_tenants/abiogenesis/python/code/genesis/__init__.py` -> `2.0.0`

## Verification

- Full suite: `python -m pytest build_tenants/abiogenesis/python/test_env/tests -q`
  - result: `115 passed, 5 deselected`
- Install lane: `python -m pytest build_tenants/abiogenesis/python/test_env/tests/test_v2_sandbox_install.py -q`
  - result: `5 passed`
- Clean install:
  - target: `build_tenants/abiogenesis/python/test_env/test_install`
  - `gen-install.py --target ...` -> `status: installed`, `version: 2.0.0`
  - `gen-install.py --target ... --verify` -> `status: ok`
- Bootloader consistency:
  - `python -m pytest build_tenants/abiogenesis/python/test_env/tests/test_m03_engine_kernel_integration.py -q -k bootloader_consistency`
  - result: `2 passed, 24 deselected`

## Release Notes

1. GTL recursion is now explicit and law-bearing rather than decorative metadata only.
2. ABG recursive execution is local, composable, and containment-safe over explicit frame state.
3. Recursive child closure no longer certifies the parent; fold-back is lawful rebind input.
4. Operative recursive next-action truth is owned by interpreter machine control rather than service-driven replay scans.
5. Installer/runtime/bootstrap surfaces are aligned to the present `2.0.0` line.

## Git Objects

Intended tag: `v2.0.0`
Intended support branch: `release/2.0`
