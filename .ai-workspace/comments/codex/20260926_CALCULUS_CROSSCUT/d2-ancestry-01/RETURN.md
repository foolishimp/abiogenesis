Product Frame: fixed fifteen-family ABG5, GOAL035/T287/LIFE01; GTL declares, HoG traverses, owners effect, ABG admits and replay projects. ABI STDO2.5.1RC1 / GLC RC4 remain selected. Exact frame: `stdo://releases/v2.5.1-rc.1/standards/STDO_REFERENCE_FRAME_BASELINE.md#derived-end-to-end-interface-integration-frame`, joined project `F-END-TO-END-INTERFACE-INTEGRATION`, with Owner/Conservation/Reuse/Computational Whole-Path. The Executive's exact release verification remains the method basis; this Writer did not repeat it. Product SHA256 `bb0c211a3fe04be9b3d8a12779387f6b26e3e0d0bd2bf513cf7fe826c8ba5176` is unchanged.

Writer CLOSED — one bounded NATIVE-D2-ANCESTRY-01 correction is implemented, emitted and self-checked for independent review. Source preimage is HEAD `2a4dee94c6fee79ed177700472bdc26d5f616244`. Classification is the selected narrow tenant design reframe plus realization repair; no Product, graph, contract carrier or Public-operation law changed. Runtime successor qualification remains OPEN.

The first cause was a same-Run assumption in both native intake and its HOW. Native41 failed before an actor at Result ordinal92550, `native_revision_parent_absent`. Its source native40 contains a pending Design projection at88557 and rejected assessment at92499, with no advancing all-satisfied predecessor leaf in that Run. The exact retained request names native38's authored Result `a281897b…`; that parent's request names accepted Requirements Result `7580c6b2…`. Native40 revision basis `b4b381f5…` retains native38 basis `7bdb86ef…` as parent. The pending Design's original author provenance and accepted asset prefix are unchanged. This is evidence of the missing conserved relation, not a rank7/8 regression or a reason to retry native41 as a semantic cause. `source-chain.json` records the bounded coordinates and equality checks; it does not claim fresh reconstruction of the final historical leaf.

The existing owner now resolves that chain in `code/src/abg/semantic_revision.ts:85–135`. Each hop reuses the exact admitted intake acquisition and parent Result/J; matches the request to its own invocation root, historical binding, original job/basis/lifecycle and parent revision ref; and requires parent judgment < acquisition Result/J < descendant Result. Strict decreasing admission order rejects cycles without another ledger. Existing exact-coordinate leaf and execution-basis owners remain authoritative. The chain stops at the existing eligible advancing predecessor; missing or wrong provenance refuses. It neither enumerates another historical Run nor searches for an equal value. Current same-Run parent selection remains unchanged (`:222–252`).

At `:260–279`, the failed cause still resolves to the selected source Run root. A conserved predecessor keeps its older identity and historical binding instead of being required to share that invocation. The new binding cover starts at the failed source Run's W, using the existing cover owner; prior admitted acquisitions retain their own earlier covers. Exact environment authority, original root/lifecycle, invalidation since the new cause, and current physical context checks remain mandatory. The acquisition relation was factored from the existing downstream consumer and reused at `:323–329`; there is no new authority or repeated failure discovery. The owning D2 HOW's first two ancestry paragraphs now state this distinction.

Validation is retained in `checks.json` and the named logs:

- The new finite case against unchanged core41 emission reproduced `native_revision_parent_absent` in0.543s wall. This is the expected failing baseline.
- One authorized `tsc -p tsconfig.json` passed in6.177s; manifest generation passed in13.676s. No clean, install or pack ran. CPU attribution was not measured.
- Five focused checks passed, with zero skips/failures, in1.729s wall. The new two-hop assessor-only rejection case took393.6ms and uses distinct author, assessor and new-intake Runs/bindings. It proves exact old-parent/new-cause selection and subsequent ordinary selection/request/projection; original job, accepted assets, binding versions and rejected Design source survive.
- Negative cases remove each exact parent/acquisition, substitute an equal-valued unrelated producer, alter acquisition implementation/source, cross a request root, violate event order, cross cause ancestry or cover subject, invalidate the source, and change current physical context. All refuse. Existing same-Run D2 composition, root-backed cover and Product author/assessor recovery checks also pass. Syntax and scoped diff checks pass.

The fixture explicitly supplies historical admission, install, prefix and leaf-lookup facts. The production semantic ancestry, acquisition reuse, binding-cover algorithm, Product derivation and physical observation are exercised; this is not installed or provider qualification and does not replace the prior real operational-current-input proof. Root's next discriminator is the installed native42 intake from native40's genuine blocked Run/prefix429545568, entered at the actual latest physical close433209393. No actor, original journal acquisition or source-worksite mutation was performed here. Only Root's selected 4.03MB native40 event extraction and already retained native38 selected Result were read for the causal relation; the final older accepted leaf was not reacquired.

Changed source territory is one ABG owner, one focused test and the narrowly selected D2 HOW. Emitted `semantic_revision.js/.d.ts`, generated capability graph and Product manifest are frozen for packaging without recompilation. `generated-delta.json` identifies the four generated byte changes. `freeze.json` binds exact source/test/HOW/emission/generated identities and proof artifacts; accepted ranks1–3/7–8 and predecessor freezes were preserved. No commit was made. Executive owns independent review and any successor execution.
