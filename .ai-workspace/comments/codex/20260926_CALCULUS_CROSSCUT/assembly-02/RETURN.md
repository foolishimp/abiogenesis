Product Frame: fixed fifteen-family ABG5, GOAL035/T287/LIFE01; GTL declares,
HoG traverses, ABG admits and replay projects. Rank 8 is a realization refactor
under STDO2.5.1RC1 Interface Integration with Computational Whole-Path,
Owner/Conservation/Reuse. INSTRUCTION-ASSEMBLY-005/010/021..023 govern.

**Writer closed; independent review pending.** Five assembly paths now share
private plan/envelope identity and manifest/request completion. Four use the
same ordered-section renderer. Native work retains its distinct renderer;
each branch retains its role, publication, source, selection, schema and bound
checks. No public helper, new carrier, prompt framework or stage-arm deletion.

The source contracts from 88,807 to 86,069 bytes: 65 lines added, 77 removed.
Repeated bookkeeping is owned once. Actor prompt size does not change: the
retained assessor remains 712,820 bytes, preserving rank 3's earlier saving.
This is source-context reduction and drift prevention, not another measured
token, latency or actor-context improvement.

Root's single shared TypeScript emission and manifest generation pass. The
bounded differential runners use the accepted core40 JavaScript preimage and
current source emission with identical component inputs. They compare 21
results/refusals, including ten successful assemblies across native work,
worksite command execution, semantic-stage author/assessor, semantic-job
author/assessor and revision selection. Canonical complete assembly values,
digests and rendered bytes agree. Existing tests additionally retain role,
opaque application JSON, source/term sharing and bound-refusal assertions.

`component-regression.mjs` is a copy of the existing focused fixture with output
capture, plus a finite stage-rendering case. `worksite-regression.mjs` reuses
the native-C2 fixture and compares both source cuts within the same worksite.
Their upstream admission/role dependencies are explicit component premises;
this is assembly equivalence, not installed authority or runtime qualification.
The first runner attempt had three harness defects: capture of undefined
component-only fields, moved-file Ajv resolution, and missing stage fixture
proof fields. Those failures and their bounded corrections are retained in
`before-fixture-failure.log`; none required a production change.

No historical journal, native/provider process or accepted prior evidence was
changed. `freeze.json` binds the source, emission, unchanged existing test and
evidence. The shared manifest is Root-owned. Writer effects are stopped; the
next selected action is joint independent review with rank 7, then resume the
admitted Design-assessor suffix through the installed successor.
