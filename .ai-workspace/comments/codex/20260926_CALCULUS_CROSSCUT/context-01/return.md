# Ranks 2–3: role and exact context presentation

Writer `/root`; source/component READY for independent review. Fixed fifteen-family ABG5, T287/LIFE01; STDO2.5.1RC1 interface-integration/context correspondence and owner/reuse frame. Product INSTRUCTION-ASSEMBLY-021..023 governs. Subject is `freeze.json`; existing prior work is preserved. No installed or semantic-UAT acceptance is claimed.

Assessor instructions evaluate the given candidate against the existing rubric, domains, path/dependency/readiness and execution constraints. Author-only requirement/binding/Design commands remain in author context. The response contract is unchanged. Within one assembly, a single active-binding projection supplies contract and context through private construction functions; standalone exported entrypoints still establish their own binding input. No supplied proof parameter, global cache or new Public export was introduced.

Only known typed slots share material: historical and current grounded-term arrays select exact ordered rows from retainedTerms; strictly parsed observed candidate values reference complete identical historical candidates. Original observations/envelopes, byte identities, assessment dispositions, source qualifications and opaque application JSON are preserved. A typed JSON view preserves the parsed value, not its original whitespace encoding. Unmatched or non-I-JSON observations retain complete text. No history, version or governing source span was deleted.

The shared ordinary build passed. Three focused cases passed in 1.942 seconds: opaque application/C2 data, actual retained author successor, and actual assessment-first successor. Checks independently expand the typed views to the full original assets/terms, preserve rejected versus accepted Requirements and pending Design, compare the combined and standalone projections, and refuse malformed bindings, missing required selectors/context and excessive required role material. Unmatched text and duplicate JSON keys remain text. Commands, environment and stdout are retained in `checks.json` and `checks.stdout`.

For the same retained assessment-first input `6b4917a4…`, full prompt size is 712,820 B, previously 994,022 B: 281,202 B removed, with the unchanged 1,048,576 B declared bound. All 29 retained terms, five historical assets, original source and pending `75ddeb06…` Design survive. Author successor is 650,393 B. `measurement.json` records the exact component result. These are prompt bytes, not token/cost estimates or observed live latency.

Tests use actual finite retained inputs and role/source content with disclosed upstream authentication/currentness component premises. They establish presentation and response-contract correspondence, not installed admission, semantic sufficiency or repeated stochastic judgment. Rank 1 real composition and the preserved native assessor/Public boundary remain separate checkpoints. No native actors, provider calls, journal scans, shared install changes, Git commit or publication occurred. Writer stopped editing this frozen subject and returns to Executive.
