Product Frame: fixed fifteen-family ABG5, GOAL035/T287/LIFE01; GTL declares, HoG traverses, ABG admits, and odd_glc owns lifecycle declarations. ABI STDO2.5.1RC1 / GLC RC4 remain selected. Product execution/context calculus, Owner/Conservation and the existing native D2 HOW govern this read-only triage. Core40/dev16 is the actual runtime subject; reviewed core41 ranks7–8 are not substituted for it.

CLOSED — the supported first classification is downstream Design/proof-realization mismatch, with an explicit context limitation. This record does not demonstrate an ABG execution/admission defect or justify an engine patch. A valid independent assessment can correctly block an inadequate candidate: valid response, satisfied stage and completed task remain separate.

The retained Public replay reports Run `1d11eb78db2cb203eddb1229b2fb695ea37ebe499b4abdcdff71a6d2a8fcc51c` blocked, with no terminal Result. The Result read reports absent/not_found; both reads completed at the same prefix. The assessment satisfies three criteria and falsifies obligation-conservation and executable-proof. Its semantic explanations are claims evaluated below, not adopted wholesale as runtime facts.

1. **Supported candidate defect: the proposed proof does not discharge its own active bindings.** Actual prompt `task.historicalAssets[4].candidate.asset`, statements07/10 and pressures01/02, explicitly defers stdout-fragment comparison to Evidence and states that construction-basis predecessor digests cannot be proved in its single activation. Binding proposals10/11 on the accepted Requirements asset require a separate admitted deterministic derivation/evaluator over one run, including per-suite outcomes and per-condition results. Proposal12 and related proposals5/6/9 require predecessor digests in construction evidence. The Design declares nine commands, no expected reports, and predicates for aggregate exit/pass counts plus module return; its separate component/UAT runs cannot themselves prove attribution inside the planned combined run. It nevertheless marks dependencyDisposition sufficient.

   The installed dev16 lifecycle at `build/native-lifecycle-declarations.mjs:33–35` gives Evidence application_assessment and expressly prohibits tools. Assigning absent deterministic computation to that assessor is therefore not a lawful implementation of these active proof bindings. This supports the refusal without relying on the assessor's speculation about the old cause. It does not establish that every alternative Design is impossible.

   Core40 already declares `module_export_return_exact`, aggregate `test_pass_count`, process-exit, stdout-exact and report predicates (`build/code/src/product/worksite_command_execution.js:520–537,754–791`). Its request instructs the existing ABI-owned helper to execute ordered commands and retain computed command/stream/predicate rows (`:1473–1477`). Thus the broad suggestion that the typed module observation lacks an owner is not supported. Conversely, the enumerated grammar has no stdout-substring or named per-suite predicate. Whether a lawful alternative declared command/proof arrangement meets the exact admitted single-run evaluator requirement has not been demonstrated here; missing one built-in predicate alone does not prove a generic engine defect.

2. **Historical file mismatch is real and expected, not evidence of stale logical admission.** The actual worksite view binds `semantic-assets/stage-2.json` digest `bb93b54a…` to the rejected candidate at `#/task/historicalAssets/2/candidate`. The predecessor view selects accepted Requirements `7a978d3c…` at index3, and `task.currentCandidateRef` selects pending Design `75ddeb06…` at index4. No stage-3 file is observed. These are distinct, explicitly presented facts. Current native D2 HOW preserves old files as historical when superseded by admitted D2 assets; installed `product/semantic_job.js:949–967` consumes the current assessed Design and, for revision construction, supplies current admitted assets while protecting historical semantic files. A compact admitted Design need not acquire an extra on-disk authority merely to be assessed. The assessor's conditional hypothesis that this caused the earlier preparation failure remains unsupported.

3. **Real context limitation, not an established cause of this block.** The prompt carries old cause ref/digest and generic native-source-refused selection reason, but no cause body: `evidence.observed` is null. Installed `abg/instruction_assembly.js:194–205,503–525` explains the projection: revision metadata retains coordinates, while this stage's observed-evidence slot is the semantic input evidence. The assessor therefore cannot diagnose or certify resolution of that earlier operational failure from this prompt. Its refusal to make that claim is appropriate. In this assessment-first entry the pending Design was preserved for independent judgment; the earlier preparation problem is not automatically a Design obligation. This omission warrants neither invented causality nor invalidation of the two independently supported candidate deficiencies. If future instructions require assessment of correction causality, the existing assembly/context owner must select the necessary authenticated cause material or explicitly limit that question.

**Earliest lawful re-entry and next delivery step.** Use the existing semantic D2 intake over this newly blocked Run and its exact closed prefix, so its authenticated assessment becomes counterevidence and the admitted selector chooses the earliest affected stage. Core40 `abg/semantic_revision.js:149–158,508–519` accepts a genuine non-satisfied assessor outcome and permits preconstruction stage revision at or before the affected stage; dev16 `native-lifecycle-declarations.mjs:338–358` publishes the intake and whole Requirements/Design suffixes. This needs no new host loop or engine repair merely to route the failure.

A Design re-entry is sufficient only if it supplies a concrete supported realization under the active bindings. If those bindings require repricing, re-enter Requirements through its existing assessed binding-version relation; do not silently relax source obligations. The current published suffix has one native construction occurrence. It does not by itself prove the sequential construction-basis remedy suggested by the assessor. Retaining that exact digest-basis requirement may require a selected GLC declaration change using existing composition, or a demonstrated ABI capability gap; neither is established or authorized by this triage. Preserve all15 obligations, original source/oracle, accepted Intent/Product/Requirements, pending/rejected Design and actual observations until a lawful successor changes their status. Do not repeat the same assessment hoping for acceptance.

Evidence: output `preparation/invocation/archives/fp-794017cab8238718-output.txt`, 19,377B, SHA256 `8c130d8386a1d0f33216b48f7469aa8aa1f47bb037b0ebb9e7172cf9888f9f9e`; actual prompt 713,666B, SHA256 `e89db55b8416611d007c57915d731080f4ebeba180a7583a2d8242d3fe88f1b7`; `suffix-01/read-run_replay.json` `c8b4cf1a2154833b6c065a566c0ac132edc3afec4a9f3bcaeb8a6356bac66681`; `public-readback.json` `094e7a1bbe8bfb303291a1c2660322ac45f90f0f4498c1d557bfd938d0a91eb9`. Source routes above are under the exact retained `preparation/products/core40/node_modules/@abiogenesis/typescript-tenant` and dev16 counterpart. Inspected source hashes: core40 instruction_assembly JS `bdd73d35…`, semantic_revision JS `3b8482fe…`, semantic_job JS `488b6e6c…`, worksite_command_execution JS `43451ebc…`; dev16 native factory `580f7680…`. Product SHA `bb0c211a…`; native D2 HOW SHA `0ae62fc8…`.

**Current disposition controls.** Root selects a fresh intake on core41/dev16 from native40's actual closed blocked Run and prefix, preserving existing source ancestry and changed-binding/currentness admission. The old core40-bound request is not transplanted. Continue only through the new admitted choice's declared whole suffix: no host-selected stage, hand-patched Design or repeated unchanged author. Escalation beyond existing D2 requires either a concrete source-required relation that cannot be expressed or discharged through the existing declared composition and owners, or an actual refusal showing an owner defect. Distinguish a GLC declaration gap from an ABI capability gap; the assessor's assertion alone establishes neither. Preserve the original obligations and evidence throughout.

No original journal acquisition, replay execution, tests, build, provider call, code change or retry occurred. This report does not independently qualify all assessor claims, later construction, correction eligibility on a new acquisition, or whole-suffix completion. Root owns the next selection.
