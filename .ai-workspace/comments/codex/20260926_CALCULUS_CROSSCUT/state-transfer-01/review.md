# CLOSED — T287_CALCULUS_REPAIR_01 rank 1

Product Frame: fixed fifteen-family ABG5, GOAL035/T287/LIFE01. GTL declares, HoG traverses and ABG admits; GLC lifecycle/context authority and RC4 selection remain unchanged. ABI uses immutable STDO2.5.1RC1, manifest `5d306da13994e69aa9f215d4c1cd2d0be96283c1e33a652b58e6e9262d036b64`. Product semantics remain independent of tenant technology and risk. The selected TypeScript realization is the trusted developer desktop. Interface Integration, Owner/Conservation/Proof and the Product state-transfer/reuse/proportionality laws govern. No new controller, runtime truth, ledger, Public authority or provider effect is permitted.

**Disposition: one supported source blocker, R-ST01. The frozen rank 1 candidate is not ready for native continuation until Root disposes the finding.** No additional supported functional-transfer blocker was found in the inspected finite caller cone. This is independent review; implementation and release disposition remain with Root.

## Exact subject

Reacquired and verified all **56/56** frozen files, including 14 production TypeScript owners, 28 emitted companions, three test/support files, HOW, Product, generated artifacts and retained evidence. No hash or byte-count mismatch.

- `RETURN.md`: `c6e586c38206dae2e3cf8598edaba23e316210b2849511b367b365c249fe850e`.
- `freeze.json`: `ed8856ef5aef3da762661e9e39d3d223550fd8ab8efdb6990fa10a59c9c15880`; this is the exact full member/hash reference.
- Governing Product: `bb0c211a3fe04be9b3d8a12779387f6b26e3e0d0bd2bf513cf7fe826c8ba5176`.
- Accepted ranks 2–3 context freeze: all seven hashes remain unchanged; its review was reused.

Reviewed scoped changes against `420336a50b8fbec42ab244521cfa4d8a0351c4af`. Paths below are relative to `build_tenants/abiogenesis/typescript/code/src/`.

## R-ST01 — P2: warm current-input selection enumerates unrelated history

**Trigger and source.** On an ordinary advanced leaf or structural transition, `hog/graph_execute.ts:502–504,638–640` passes the complete authority prefix to `projectTraversalInputAtPrefix`. Its `traversalInputOrigins` generator (`abg/traversal_cursor.ts:243–263`) walks every event in that prefix until the selected reference is reached. A recent Result near its end therefore incurs work proportional to unrelated prior history despite the caller already carrying the exact input reference and digest.

Shared-batch continuation and failed-consumer cursor reconstruction additionally call `cursorAtCoordinate` (`traversal_cursor.ts:288–294`). That function enumerates the entire origin inventory and constructs/hashes a cursor candidate for every origin, even after encountering a matching admitted cursor. The batch path uses it at 357–364; the historical opened-call path at 311–321. HoG target derivation and independent ABG route admission both consume the same relation. The new common owner therefore introduces this work on live paths, not only genuine cold recovery.

**Violated relation and impact.** Product Execution/Context Calculus, Live State/Valid Reuse and Computational Proportionality require ordinary internal handoffs to consume applicable established facts without unrelated-history growth absent a named dependency. The governing dependency here is the exact admitted cursor, its declared ancestry and selected origin—not all earlier input-producing events. This is source-proven linear history enumeration and per-origin cursor construction. No CPU, wall-time, RSS or large-journal magnitude is attributed from the finite installed timings.

Affected exact owners: `traversal_cursor.ts` SHA `cbbeb047c1fa2c23146f2df151bdd9c42eaeb91afe438b9240b74af38f5c8549`; `hog/graph_execute.ts` SHA `b15acbbc0280833b7e7e5f0e367fbc7ebffee4e3ecf503d11253d8bd0f01bb55`.

**Smallest correction.** Keep current-input projection at the existing cursor owner, but select from the admitted cursor/origin coordinates and relevant ancestry through the existing prefix indexes or already-carried established relation. Available owners include `traversalCursorAdmissionEventsAtPrefix` at 158 (graph-call index and exact scope), `event_prefix.ts:425–439` (event ID, basis, graph-call and Result-reference indexes), and initial-cursor payload input coordinates at `traversal_cursor.ts:539–555`. Do not create another cache/ledger or merely stop after a global scan finds a match.

Retain exact reference/digest/value correspondence; enclosing-batch entry; scope/order/causation; raw/cold refusal; distinct retained-input, interaction, retry, reentry and foldback meanings; currentness and independent route admission. The direct preservation discriminator is equal selected inputs/producer identities and admitted transitions for the existing finite cases, with unrelated history no longer enumerated/rehashed per warm handoff. This review ran no new check and does not prescribe a broader campaign.

## Unchallenged affected relations

Source review supports the intended transfer repair: `gtl/source_path.ts:476,583,933,1049` distinguishes compose progression, enclosing shared-batch entry, fan-out members and structural identity; `hog/graph_execute.ts:502,638` consumes the admitted selected value. The finite production caller census matches the return: leaf, workflow/retained-input, fan-out, recursion, interaction and structural callers supply prefix authority; ABG continuation/structural admission independently derives the relation rather than trusting HoG's target.

Recovery at `abg/semantic_revision.ts:155–189` selects the failed consumer's opened cursor and exact successful producer through the existing `worksite_input_provenance.ts:112` owner. Payload equality alone does not choose another producer. Original job/lifecycle/root ancestry, binding cover and mutable-context duties remain at 198–220. Full installed native intake with the preserved F_P author Result is still OPEN.

## Evidence limits and closure

Consumed retained build, installed-composition, identity and Product-value logs; no reruns. Final source supports compose, composed tasks inside batch, progressed batch entry and identity-ending behavior. The final compound command was not green: its identity fixture used nonexistent `C.identity`; the disclosed `C.id` fixture-only correction passed identity separately. Ordinary batch, batch-in-batch and failed-consumer/equal-value-producer evidence belong to earlier cuts; the earliest pair lacks frozen intermediate hashes/stdout. They are not final-cut qualification.

The two Product recovery-value checks conserve accepted/pending assets and assessment-first declaration under supplied premises. They do not execute full native intake, role authentication or semantic judgment. Fan-out, retained-input, retry, interaction, recursion, reentry and foldback were source-accounted, not newly executed. Setup/traversal timings provide no isolated attribution for R-ST01 and impose no invented ten-second cap.

The return's attribution of a Root import correction in `semantic_revision.ts` is a documentation attribution to reconcile: Root identifies its change as `instruction_assembly.ts`. This is not a runtime finding.

Review is CLOSED. No source/docs/tickets/Git mutation, test/build/package/runtime/provider execution, journal scan or worker activation occurred. This file is the sole write.

