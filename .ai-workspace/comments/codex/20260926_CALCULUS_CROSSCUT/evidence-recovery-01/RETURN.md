Product Frame: fixed fifteen-family ABG5, GOAL035/T287/LIFE01 under ABI STDO2.5.1RC1 and unchanged GLC RC4. Product execution/context recovery law and CONTINUATION-015 govern this bounded `design_reframe` plus realization. GTL declares; HoG traverses; existing owners effect; ABG admits; replay projects. Writer returns to Executive `/root` with no new runtime controller.

**RETURN — source and finite component ready; frozen.** `freeze.json` subject `sha256:50cee013fc4bb518d1cb15fb53823cd6ebc15a1905ab94ac3f6b8ac2bc81e613` binds the two existing semantic revision owners, D2 HOW, directly required test, emitted owner files and shared manifest. `activation.md` and `preimages.json` retain the exact grant and preimages. No production TypeScript changes followed the final shared emission.

Cause and correction: the actual native42 Evidence-author preparation failure follows a successful native Evidence-input producer. Intake formerly required `evidence === null` and an assessed-asset producer; the Product constructor and selector grammar separately restricted operational recovery to preconstruction. The existing input owner now resolves the exact consumer input through closed workflow folds to the admitted native Evidence leaf. Intake retains its actual construction coordinate and complete evidence. Phase, raw selection schema, owner selection and pure projection agree on postconstruction operational author entry. No semantic rejection or accepted Evidence is fabricated. Empty affected obligations remain lawful only under the owning subject conditions; all governing bindings survive.

The existing source, root ancestry and W0-to-W1 cover owners remain operative. Fresh acquisition compares the observed context; live selector/request/suffix preparation applies physical currentness even for retained evidence. Relevant admitted invalidation after the producer and after acquisition refuses reuse. The declared `from-evidence@5` suffix contains projection, Evidence author/assessor and terminal; it omits construction and command work. Historical actor and Result identities are preserved.

Proof on the final emitted cut:

- `focused-tests-02` passes the four unchanged preconstruction, assessment-first, ancestry and ordinary revision cases. `focused-tests-04` passes the complete producer/fold -> genuine typed preparation refusal -> intake -> selection/request -> declared Evidence-only projection case in 1.137 seconds (case 0.841 seconds). Wrong/missing input producer, wrong fold, producer owner, role, phase, stage, target and obligation, missing/wrong binding cover, admitted invalidation and physically changed dependency all refuse at their owning boundaries. No Evidence asset is minted and the construction constructor refuses the successor.
- `retained-product-proof.json` independently checks exact native42 Result109418 value `03e5a0bd…`: four satisfied stages, fifteen obligations, fourteen artifacts, nine exit-zero commands and thirteen predicate observations remain byte/value-identical, with original construction/execution and revision ancestry. Pure proof takes 0.525 seconds. Its operational premise and missing construction/selection admission coordinates are explicit component premises; this is not an admitted request or fresh currentness proof.
- The assembly Writer alone emitted the shared cut: final tsc 5.777 seconds and manifest 12.953 seconds, both exit0 (`../evidence-context-01/build-final.json`). Syntax and scoped diff checks pass. No broad suite, provider, helper, journal replay, package/install or Git mutation occurred.

Retained failures and oracle corrections:

1. `focused-tests-01`: the new path reached selection, but the raw Product guard/schema still required a nonempty affected-obligation set after construction. Exact native42 pure derivation also refused. The two existing grammar functions were corrected; contextual owner eligibility was preserved. This was a realization defect, followed by shared successor emission.
2. `focused-tests-02`, old line627: the test expected `jobRevisionSelectionMatchesBasis` to reread changed files. That function deliberately uses `projectJobRevisionSubject(..., readPhysical=false)` for cold Result validation. Live selection assembly supplies true; request and suffix preparation also enforce live currentness. The corrected negative invokes `projectJobRevisionSubject(..., true)` and still demands refusal, with a separate stale suffix-projection refusal. All four phase/stage/target/obligation alternates had already refused. No physical gate was waived and no production TypeScript changed.
3. `focused-tests-03`: the test expected two suffix nodes and omitted the declared terminal. The independent oracle is now the exact `[projection, Evidence, terminal]` chain plus absence of native construction effect. `focused-tests-04` passes. This changed only the test expectation.

Component limits: CCall/fibre/native-source admission, traversal's selected current input, install/binding admission and invalidation fact establishment are supplied lower-owner premises. The real input/fold provenance, recovery owners, physical context checks, covering-witness algorithm, Product derivation and ordinary declaration selector are exercised. Full runtime admission, native actor usability, installed path and Product acceptance remain root-owned and unclaimed. The separate actual-shaped selector fixture supports the assembly Writer's measurement; its corrected construction `valueKind` and body digest prevent an invalid raw fallback measurement.

Disposition: return this exact subject for independent review and the separately authorized installed continuation. Writer stops editing.
