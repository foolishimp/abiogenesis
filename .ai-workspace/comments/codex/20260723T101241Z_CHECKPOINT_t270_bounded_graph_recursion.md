# T-270 Bounded Graph Recursion Checkpoint

## Status

Superseded by the child-lifecycle repair at
`0ed75e8f27c551451cf6998d24ccce9f4ccfccab`. The original implementation
commit remains historical evidence but does not independently prove the
`graph_recursion` row.

## Delivered

- Whole-Program validation now requires a declared gate target to equal its
  adjacent `workflow.C` target.
- One Product-published GTL.TypeScript recurse application binds an exact
  Boolean termination field, published Rule and Evaluator, child
  GraphFunction, identity foldback, and bound four.
- HoG evaluates the parent locus, traverses each declared child in an admitted
  child GraphCall and Frame, then re-enters the same parent Frame only after
  ABG admits the child result, judgment, foldback, and application route.
- The successful installed path advances parent attempts `1` through `4`,
  folds three children, closes once, and agrees with replay.
- The installed bound path opens no fourth child, admits the fourth
  non-terminal parent as a blocked application route, and terminates with
  `run_stopped`.

## Conservation Progress

The fixed forty-row matrix advances from `19/40` to `20/40`:

- `structural_form/graph_recursion`.

The remaining `20` rows stay explicit typed gaps. This checkpoint does not
claim fan-out/fan-in, F_H continuation, or `ABG5-S02` closure.

## Exact Candidate Basis

- artifact SHA-256:
  `50924833d69949f29c0dab90f5d004b196150bc69abf400e538c83d81a574849`
- Product content digest:
  `sha256:0bbb15434783efb107458915a20796c82c3a03069f821de83f84c4262af1e9b1`
- manifest digest:
  `sha256:430e4402773ee2be8bb3b02aebc8d034df3132dcad752af96fda20517ef22596`

Two independent package builds reproduced the exact artifact digest.

## Verification

- `npm run test:m5`: `57/57`
- retained `npm run test:m4`: `26/26`
- conservation: `20/40` proven, `20/40` explicit `todo`
- `ABI5-ROOT-001`: `root_satisfied`, R1-R10 true, no failures
- `git diff --check`: green

## Boundary

No parser, lowering, semantic IR, compiled carrier, public traversal
controller, policy engine, second runtime path, or new ticket was introduced.
Recursion is one declared GTL application traversed by HoG and admitted as ABG
runtime truth. `ABG5-S02` remains open; fan-out/fan-in is the next typed
frontier.
