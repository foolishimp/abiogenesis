# Self-Review: T-277 PC-008 Scenario Factorization

**Scope**: T-276 prospective installed Consensus qualification design

**Closure claim**: the one-driver realization shape is ready for independent
review; T-276 scenario implementation and qualification remain blocked

## Finding

No installed Consensus scenario implementations exist in the live tree. The
Prime work is preventive: prevent three outcome families multiplied by three
workspace applications from becoming nine harnesses.

The accepted design defines:

- one exact three-row scenario-definition register
- one existing workspace-application contract with three admitted instances
- one source-blind installed scenario driver
- one immutable archive per actual execution

Three paired primary executions cover every required outcome family and every
required workspace application. This follows the requirements' separate
outcome and workspace clauses without inventing a Cartesian obligation. A
structural proof must show that both coordinates use the same orchestration.
If it cannot, or independent review requires behavioral cross-product evidence,
all nine execute through the same driver.

## Proportionality

| Measure | Result |
|---|---:|
| Existing scenario implementations | 0 |
| Target scenario-driver implementations | 1 |
| Scenario definitions | 3 |
| Workspace applications | 3 |
| Primary execution archives | 3 |
| Maximum execution archives without new code | 9 |

No scenario framework, matrix scheduler, runtime controller, or feature CLI is
authorized. T-276 must reuse existing installed-fixture support and public
operations.

## Remaining Gate

The design does not claim installed proof. T-267, T-268, T-270, T-271, T-272,
T-274, and T-275 remain dependencies. Independent review may change the number
of executions but not the one-driver architecture without design re-entry.
