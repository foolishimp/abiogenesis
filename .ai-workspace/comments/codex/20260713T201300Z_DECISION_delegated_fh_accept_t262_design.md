# Delegated F_H Decision: Accept T-262 Design

**Date**: 2026-07-13
**Ticket**: `T-262`
**Decision**: accepted; implementation authorized

The user delegated F_H authority to continue through bounded sections while
away, with self-review and proportional remediation between sections. Under
that authority, the T-262 three-view design is accepted after the review
recorded in
`20260713T201200Z_SELF_REVIEW_t262_typed_recurse_design.md`.

Implementation is authorized only for:

- one direct `recurse(GraphFunction<A,B>, termination, foldback)` relation;
- one positive admitted domain-policy projection bound to the exact A input;
- exact selected catalog entry and digest-bound Module authority;
- a module-local operand with stable outer A/B contracts;
- replay-derived application ordinal, dangling stage, frame lineage, and
  continuation truth;
- child execution, declared termination evaluation, exact B-to-A foldback,
  and deterministic parent rebind admission as separate steps;
- preservation of prior evidence and unchanged policy across foldback; and
- isolated generic and canonical-structure proof while T-267 and T-268 remain
  authoritative product gates.

No Consensus controller, hidden budget, C.retry round, graph rewrite, helper
publication, nested-recursion engine, or canonical effect authorization is
accepted by this decision.
