# Superseding Self-Review - T-278 One Surface Target-Shape Candidate

**Date**: 2026-07-15

**Verdict**: ready for explicit F_H target-shape review; not accepted or ratified
**Supersedes**: `20260715T082100Z_SELF_REVIEW_t278_public_control_plane_ontology.md`

## Review Disposition

The external review was materially correct:

1. One Surface was absent from the first ontology candidate.
2. The first constitutional ambiguity is in INTENT, where `GraphFunction` was
   called the callable program despite retained program-traversal law.
3. The first self-review counts were stale and internally contradictory.
4. closed T-244 still mixes the 19-operation realization with the obsolete
   exact-36 target and must be reopened only after target acceptance.
5. the T-267 manuscript collision is real but isolated to the sibling worktree;
   it must become T-279 before commit.
6. current T-270/T-272 code remains provisional realization evidence until the
   accepted authority basis is propagated and the affected designs are
   rederived.

Two execution-plan refinements avoid paperwork churn and source-line damage:

- T-278 itself is reclassified to `intent_reprice`; no T-280 is required.
- `codex/t266-stage` remains the singular 5.0 integration line. The five
  `main`-only commits are commentary/status changes and do not justify merging
  or rebasing the large dirty runtime wave before this design gate closes.

## Structural Repairs

The `/4-candidate` ontology now:

- distinguishes the admitted GTL program from its callable GraphFunctions and
  structurally binds execution to program, workspace, catalog view, and
  program-owned function membership;
- preserves `synthesizeModel`, `evalGap`, `evaluateNext`, and `evaluateAction`
  as distinct authorities without adding four public SDK/CLI verbs;
- models the complete domain chain, exact eight-case `NextActionBasis`, public
  invocation sequence, immutable public-invocation state, and separate ABG
  runtime state;
- derives current lawful-action truth only after `AF-16 -> exact basis ->
  AF-11/12/13`, before returning the current public outcome;
- keeps F_H response as admitted input to `AF-17` over the same current intent;
  selection occurs only after admitted closure truth;
- makes `ActionCatalog` a subordinate projection of one admitted program and
  narrowing catalog view; current eligibility remains exclusively AF-13;
- reserves closure for the deterministic F_D/ABG complete-evidence fold;
  F_P, F_H, process, liveness, and worker output are evidence only;
- requires every published inner refinement boundary to inherit One Surface;
- contracts `EvidenceRecord` to a typed projection of one
  `AdmittedEvidence` identity; and
- distinguishes same-basis projection staleness from a changed basis identity.
  The latter terminates typed as `basis_fork_detected` unless an exact covering
  declaration reprice admits the old/new pair.

## Exact Evidence

| Check | Result |
|---|---:|
| exact source-basis digests | 30/30 match |
| discovered behavior rows | 38 |
| atomic function families / authority rows | 27/27 |
| higher-order product compositions | 7 |
| candidate public operation identities | 19 unique |
| retained T-244 feature rows | 17 |
| current capability identities | 16 |
| Mermaid ontology diagrams | 7/7 rendered with pinned 11.3.0 CLI |
| Pandoc GFM parse | ontology and ticket pass |
| `git diff --check` | pass |
| DS governance gate | 19 tickets, 73 comment refs, pass |
| Prime contraction gate | 7 accepted designs, 13 candidates, pass |

Two independent adversarial passes reviewed the corrected One Surface authority
and the Prime/census projections. Their final verdicts are accept for F_H
target-shape review. No runtime test suite was rerun because this checkpoint
changes design/ticket surfaces only and intentionally freezes the dirty
T-270/T-272 realization.

## F_H Decision Boundary

F_H acceptance is requested for four linked target claims:

1. the admitted overlay/GTL composition is the program and `GraphFunction` is
   its sole public named callable library function/work contract;
2. One Surface and its four authorities govern root and published recursive
   refinement without becoming four new public operations;
3. 27 atomic families and seven higher-order compositions are the Prime
   semantic algebra for this boundary; and
4. 19 public operation identities are the correct external projection.

Acceptance authorizes INTENT/PRODUCT/requirement/GOALS propagation and a single
T-244 regeneration. It does not ratify the final ontology against superseded
source digests and does not authorize runtime continuation yet. Final ontology
ratification follows constitutional propagation and basis recomputation.
