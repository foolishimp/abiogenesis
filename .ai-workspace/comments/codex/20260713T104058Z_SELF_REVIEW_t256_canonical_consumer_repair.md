# T-256 Canonical Consumer Repair Self-Review

**Date**: 2026-07-13
**Base checkpoint**: `ec172f2`
**Verdict**: bounded repair complete; ready for independent re-review; do not close T-256 or start T-257

## Finding Disposition

1. **Contained helper resolution repaired.** The public catalog execution row
   remains invocation authority. The join resolves the exact T-255 helper
   GraphFunction and GraphVector by unique containment in its admitted Module,
   then revalidates program selection and digest identity.
2. **Domain and compute roles separated.** The C stage retains domain roles
   such as `reduce_round` for protocol matching. The generic T-183 compute role
   derives from the exact regime-matched composition binding.
3. **Instruction truth is no longer caller-authored.** The public input and
   package surface expose no instruction-basis carrier or constructor. The
   adapter derives selected inputs, full-content section decisions, slots,
   runtime facts, proportionality, and work kind from admitted graph, program,
   protocol, carrier, and catalog truth. Legacy caller input fails closed.
4. **The census invokes the real join.** The regenerated T-252 manifest records
   ten canonical F_P/F_H join observations. Every observation compiles exact
   field closure and protocol-role closure, then stops at the expected T-268
   capability block. The T-256 gap families disappear only from those results.
5. **Prompt and result contracts are separated.** Worker output contracts
   derive only from the selected target Node. Prompt-asset output contracts do
   not enter the plan or envelope output-contract relation.

## Self-Review Correction

The first repair draft mislabeled runtime-catalog admission events as F_P
validation evidence. Catalog events are compiler evidence, not validation
evidence. The repaired plan carries an empty optional F_P-validation set and
retains catalog/admission refs only as compiler and runtime-binding evidence.

## Proof

- `npm run test:t256`: pass; GTL 82/82, focused integration 51/51, packed API 1/1.
- `npm run test:t252`: pass; body tests 11/11 and exact regenerated manifest.
- `npm run test:semantic`: pass.
- `npm run check:abg-product-publication`: pass; 63 public schemas and 33 generated publication assets exact.
- `npm run check:t252-consensus-probe`: pass; unchanged body digest `sha256:e4555c21cdb4292b64f7f4d5a625c2a520195aa8d6e9c759498eed4bf28d0ea0`, ten observed joins, ten retained successor gap families.
- `npm run check:design-mermaid`: pass; 27 diagrams across 9 files.
- `git diff --check`: pass.

## Retained Boundaries

- T-267 still owns traversal result-interface and conservation closure. Every
  constructed request remains startup-blocked before effects.
- T-268 still owns the admitted tenant-conformance manifest. Canonical
  Consensus joins stop at its exact capability block.
- T-257 remains blocked pending independent acceptance and closure of T-256.
- No Consensus-specific runtime branch, second registry, prompt fallback,
  worker dispatch, response admission, or effect path was added.
