# T-255 Compiled Handoff Realization Proof

## Claim

T-255 realizes the accepted generic boundary from one admitted GraphFunction,
GraphVector, selected C program, effective composition declaration, target
binding, and canonical tenant-conformance manifest carrier or absence to one
typed, startup-blocked execution handoff outcome.

The implementation checkpoint is `d569c0be4bf62ca1a4323d433f73e6afdf04bd44`.
It does not change the accepted T-252 Consensus body.

## Realized Boundary

- M04 admits only `abg.schema.tenant-conformance-manifest/1` input and resolves
  its public-contract, capability, effect-binding, enforcement, proof, and
  predecessor references against the existing canonical public-contract
  catalog.
- M03 consumes only the admitted shared carrier or explicit absence. It has no
  dependency on M04 application code and does not admit raw profile input.
- The handoff compiler consumes the exact T-254 vector/program binding and the
  T-265 composition owner, derives one canonical target row and edge-closure
  row, and preserves the admitted manifest basis in its capability projection.
- Missing or incompatible capability truth is `blocked_capability`. A
  selector-free relation is `structural_only`. Successor constructors remain
  `blocked_successor_constructor`.
- Every published handoff is `published_startup_blocked`; runtime addressing
  and effects remain false until T-267 supplies traversal-result and
  conservation authority.
- T-268 owns publication of ABG 5.0 tenant-conformance-manifest coverage,
  including Consensus. T-255 creates no second tenant profile or manifest
  authority.

## Exact Derived Census

The unchanged T-252 body compiles to 35 handoffs:

| Outcome | Count |
|---|---:|
| `blocked_capability` | 28 |
| `blocked_successor_constructor` | 6 |
| `structural_only` | 1 |

The compiler also derives 35 canonical target rows and 35 edge-closure rows.
The five T-255-owned gap families are absent from the observed compiler census.
The remaining T-252 census contains 12 gap families, including the T-268-owned
manifest-coverage gap.

## Clean-Tree Evidence

| Gate | Result |
|---|---|
| Full semantic suite | 1597/1597 passed |
| T-255 focused and GTL lane | 27/27 and 82/82 passed |
| T-255 compile-time raw/admitted seam | strict TypeScript passed |
| T-266 packed/public API lane | 125/125 and 82/82 passed |
| T-250 version-basis lane | 13/13 passed |
| T-252 body/probe lane | 11/11 and 82/82 passed |
| Mermaid design gate | 5/5 passed across 27 diagrams in 9 files |
| Public contract schemas | 63 verified |
| Generated publication assets | 33 verified from 1035 immutable payload files |
| Semantic lint and diff checks | passed |

The clean committed T-252 body digest is
`sha256:e4555c21cdb4292b64f7f4d5a625c2a520195aa8d6e9c759498eed4bf28d0ea0`.
After ticket closure, the exact generated probe manifest digest is
`sha256:6b6e1f39294cfc6735d0fb1206a50ea2f70ba75d2dbc3de30321232387619adb`.
It derives T-255 from the completed-ticket surface, reports zero active-owned
but unobserved families, zero duplicate owners, and zero unowned gaps, and
retains T-268 as the active owner of manifest coverage. The body digest remains
unchanged.

## Non-Claims

This proof does not claim traversal, plugin invocation, effects, archive
writes, successful assessment, closure truth, or Consensus capability
publication. Those remain behind T-267 and T-268. It also does not treat a
generated manifest, test, or local adapter as runtime admission authority.
