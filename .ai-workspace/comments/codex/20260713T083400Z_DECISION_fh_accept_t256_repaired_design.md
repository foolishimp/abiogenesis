# Decision: F_H Accepts T-256 Repaired Design

## Decision

The human authority explicitly stated:

> Accept T-256 design and authorize implementation.

The repaired candidate in
`build_tenants/abiogenesis/typescript/design/M03_INSTRUCTION_PROTOCOL_BEHAVIOR_DESIGN.md`
is therefore accepted for implementation.

## Scope Of Authority

This decision authorizes realization of the accepted T-256 boundary only:

- strict execution-context and instruction-protocol Rule profiles;
- derived source/type/regime truth;
- replay-preserved declaration-Module closure;
- stateless bound-Module Rule/Node resolution;
- invocation carrier projection;
- the canonical T-183 F_P rule, plan, startup-admission, and envelope bridge;
- the distinct F_H interaction-request branch;
- typed refusal and the retained T-267/T-268 blocks; and
- focused, genericity, no-fallback, packed-publication, and full regression
  proof required by the accepted design.

It does not authorize T-257 response admission, T-258 human act/resume, T-267
traversal closure, T-268 manifest publication, a new registry, a new GTL term,
or a parallel instruction path.

## Prior Record

The invalidated pre-review acceptance record remains historical evidence of a
governance failure and grants no authority. This decision is the first valid
post-repair F_H acceptance.
