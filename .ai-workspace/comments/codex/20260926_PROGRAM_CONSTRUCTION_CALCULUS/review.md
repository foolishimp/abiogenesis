Product Frame: the fixed fifteen-family ABG5 Product remains the destination. Core45 remains accepted at its prior bounded scope; S06, exact qualification and RC1 remain open. GOAL-035/T-287 selects the program-construction definition under `T287_PROGRAM_CONSTRUCTION_CALCULUS_01`, before implementation-owner selection. The outcome is a source-conserving contract for fitting ordinary GTL to a task and its residual obligations, connected to the existing execution/context calculus. GTL declares; HoG executes; existing owners perform effects; ABG admits runtime truth; replay projects it. Product still excludes a second controller, interpreter, execution language or writable ledger. This review evaluates contract sufficiency only.

Review activation: `T287_PROGRAM_CONSTRUCTION_CALCULUS_REVIEW_01`, 2026-09-26. Reviewer: `/root/construction_calculus_review`, independently activated from the candidate's Writer. The Reviewer authored or repaired none of the candidate bytes and exercised no code, build, install, provider, native-actor or Git mutation effect. The only granted write is this review carrier. Executive consumes this closed result and retains acceptance, disposition and further work selection.

The operative basis is `stdo_abiogenesis.json#/constitution/stdo/basis`: `stdo://releases/v2.5.1-rc.1/`, manifest SHA-256 `5d306da13994e69aa9f215d4c1cd2d0be96283c1e33a652b58e6e9262d036b64`. The activation supplied the same-turn STDO manager result (`valid: true`, no failures). I independently acquired the selected definition and exact installed bootstrap, Reference Frame Method closed-result law, and Derived Reviewer/Product/Owner relations under `/Users/jim/Library/Application Support/STDO/releases/v2.5.1-rc.1`. The project refinement is `repo://abiogenesis/build_tenants/abiogenesis/typescript/design/ABI5_PROJECT_REFERENCE_FRAME_BASIS.md#f-end-to-end-interface-integration`, with its contract/context-sufficiency claim selected. Actual producer/consumer execution, native-path qualification and user acceptance are outside this result.

The exact subject is the four-file tracked delta over HEAD `02885af032680749464da517ed1a8a4d1a4da1c7`. Independently computed hashes matched the assigned freeze both before substantive review and immediately before this return:

| File | SHA-256 |
|---|---|
| `specification/PRODUCT.md` | `64d8fe9c0a7e0508aa902624af5c5e7f01f021c493d1679c5bef3767a31a74d9` |
| `specification/requirements/gtl/REQ-L-GTL3-SELECTION-BOUNDARY.md` | `b05b046ceb91bfd6bfcf2d36682a911e88da6a1d9ea46aef1774b91471e47b03` |
| `specification/GOALS.md` | `9e62fc0c3bcb6d4f2ad7904a385db9d20c26f9bfb6d0dfe45b1ace7a0c49d01d` |
| `.ai-workspace/tickets/active/T-287-deliver-abiogenesis-5-feature-waves.md` | `18511316ba205712d4b2a45044ea691573652167c0a0eb9f4c2ce2fbc0a69048` |

Result: **`falsified` for the unqualified contract-sufficiency claim, by one bounded finding below.** The general construction boundary is coherent; the worked completion predicate needs one correction before it serves as a reliable implementation oracle. This is a semantic definition finding, not evidence of an implemented/runtime defect or a disposition of any accepted candidate.

1. **Moderate severity — the worked completion predicate does not require the independent assessment's satisfactory verdict and scope.** Evidence: `REQ-L-GTL3-SELECTION-BOUNDARY.md:276-295`, particularly `assesses(j, e, k)` at line 279 and the completion row at line 294. The text defines `j` as the required independent judgment, but neither the predicate nor its explanation says that `j` establishes the required condition with a satisfied verdict over the exact selected evidence and criteria. `assesses` can therefore be implemented as an assessment-occurrence relation.

   Counterexample: retain a conforming artifact `a` and valid admitted execution evidence `e`. Admit an independent `j` about `e` and `k` whose result is `indeterminate` because the required assessment has not established the selected completion claim. Artifact conformance need not become false merely because that independent judgment is indeterminate. The written relations `conforms`, `executed`, `assesses` and `independent` can all hold, allowing the "Every conjunct established" row to select `established(completionEvidenceRefs)`. The required independent completion judgment is still missing. An assessment about a narrower part of the same `e` and `k` exposes the related scope ambiguity.

   The impacted boundary is the construction model's completion oracle and prospective qualification examples. Product's completion law (`PRODUCT.md:601-602`, `755-765`), the evidence-role distinction (`REQ-L-GTL3-SELECTION-BOUNDARY.md:99-105`), and retained independent-assessment duties (`114`, `238-240`) otherwise preserve the stronger requirement. They do not define the worked predicate's missing verdict/scope relation. This inconsistency is reachable on ordinary semantic assessment; its frequency is unmeasured. No realized false closure has been demonstrated, and existing owner admission remains required. The explicit positive-verdict sunny-day path remains constructable.

   Smallest correction: define `assesses(j, e, k)` to entail the required satisfied verdict and exact scope, or add an explicit satisfied-verdict/scope predicate to the worked conjunction. Add one paired case for a present independent judgment that is negative, indeterminate or outside the required scope: preserve valid artifact/execution evidence, retain the unresolved duty, and select only justified evaluation, investigation or repair. Its falsifier is closure based only on the judgment's presence. Keep the condition specific to this worked task; some other tasks can legitimately request a negative finding as their outcome.

   Repair scope and risk: one worked equation/definition and one discriminating row, within the current requirement owner; no new algebra, API, runtime entity, code or campaign. Complexity and regression risk are low if the correction preserves owner-defined assessment meaning. Recommendation: bounded local definition repair before accepting this model as the code foundation. Executive assigns priority and disposition; this review does not activate repair.

Other evaluated relations produced no additional actionable finding:

- Source conservation binds `T`, exact `K` and source-linked `M`, retains unknown clauses, and requires completion equivalence rather than a requirement-count score (`008`). Alternatives, conditions and temporal dependencies remain meaningful relations; the conservation equation applies only while its support remains valid (`009`). A partial interpretation cannot certify full coverage or use an empty residual to close the task (`008`, `012`).
- Increment selection retains unselected residuals and prerequisites, derives required evaluations from governing applicability, and limits unknowns to dependent claims/effects (`009`). An artifact with missing execution selects execution; valid artifact/execution with missing judgment selects assessment; an invalidated execution dependency preserves an unaffected artifact. These are different work selections from the same task contract, not renamed lifecycle stages.
- Exact GraphFunction input/output, environment, effect, declaration and provenance law remains the contract source. `010` projects these contracts, preserves actual input bindings and effect-time preconditions, distinguishes promised outcomes from observed results, and reports missing capability instead of hiding it in a prompt or adapter. Future outputs remain declared dependencies.
- `011` and Product's existing context law bind the consumer's progressing input and applicable evidence at its actual call. They preserve accessible source material, counterevidence, uncertainty, role/response congruence and required independence; a name, schema or equal payload cannot replace causal provenance.
- `012` preserves failed-Run work, completed-Run residuals, historical provenance and prospective corrective evidence. It cannot rewrite a running Program or execution basis. Recomposition uses existing entry/re-entry authority; unavailable installed routes remain gaps. The added source-provenance case cannot manufacture an old construction relation from current files.
- `013-014` preserve computed checks, semantic judgments and reserved owner decisions; they allow scoped valid reuse and require finite scope/stopping conditions. Cost includes interpretation through necessary recovery without promising global optimization or creating a cost ledger.
- The subject classes remain explicit: Programs/GraphFunctions are immutable definitions; `M`, `R` and selection results are semantic views or construction proposals; observations and admitted evidence retain their existing owners. Nothing in the delta grants those views a new runtime lifecycle. The Product boundary and ticket preserve odd_glc's live requirement-compiler exclusion and its declaration/read-interpretation scope. They select neither a general automatic preprocessor nor wider 5.1 executive behavior for the fifteen 5.0 families.

Evidence population: the full four-file diff; the complete short selection-boundary requirement; Product construction and execution/context law plus relevant Product exclusions/release boundaries; Intent's Program/runtime boundary; GraphFunction and requirements-algebra ownership; the selected project frame and exact STDO frame law; and the relevant sibling odd_glc Intent/Product compiler and runtime exclusions. The historic strategy was not required to resolve the finding. Runtime evidence trees, installed ABI candidates, old tickets and code were not used as proof of the candidate's sufficiency. The Writer's reported whitespace/link/anchor checks were not rerun or treated as semantic proof.

Residuals: representation choices, concrete relation algorithms, semantic interpretation quality, actual function availability, implementation ownership and all future selected-realization qualification remain open. The conjunction-only worked model is not an exhaustive executable semantics of every alternative or temporal relation; `008-009` nevertheless constrain those relations, and their exact realization belongs to a later selected implementation. The ticket's in-progress return pointers are expected to be reconciled by its Writer after this closed result; they do not change the semantic finding. Core45/native44 retain only the scope explicitly carried by the current Product/Goals/ticket; this review did not reacquire or enlarge their runtime evidence.

Invalidation: a material change to any frozen candidate byte, the selected task/completion claim, STDO/Product basis, required independence or relevant owner contracts invalidates the affected part of this result. An exact bounded successor may reuse unaffected acquired evidence; this result does not accept a successor that it has not read. Return relation: Executive conjoins this result with the Writer's readiness evidence and applies the existing disposition. Reviewer is closed and stops here.

Delta readback, 2026-09-26: Executive reactivated the same independent Reviewer solely to evaluate the correction of finding 1 and append this result. The original review above is preserved; its pre-append SHA-256 was `b16e8d6e70a8daf40ed4ef99bcb6d4829355ec0dcae2305a8b0d329e4976f951`.

Result: **`satisfied` for the finding-1 correction.** At requirement lines 279-292, `assesses(j, e, k, requiredScope, satisfied)` now explicitly requires an admitted, still-applicable judgment over exact `e` and `k`, full required scope and the satisfactory verdict established under the owning assessment/evidence rules. An independent indeterminate judgment or narrower positive assessment cannot make this conjunct true. The added case at line 302 retains the unsatisfied/unresolved duty and valid support while selecting justified repair, investigation or missing assessment. This closes the exact counterexample without requiring reconstruction or execution merely because judgment remains incomplete.

Independently verified successor hashes:

| File | SHA-256 |
|---|---|
| `specification/PRODUCT.md` — unchanged | `64d8fe9c0a7e0508aa902624af5c5e7f01f021c493d1679c5bef3767a31a74d9` |
| `specification/requirements/gtl/REQ-L-GTL3-SELECTION-BOUNDARY.md` | `1024e59448cd0f01b3a7b8e4423c77961f3ba07b0394f701967a9c8e8cb2fdf8` |
| `specification/GOALS.md` — unchanged | `9e62fc0c3bcb6d4f2ad7904a385db9d20c26f9bfb6d0dfe45b1ace7a0c49d01d` |
| `.ai-workspace/tickets/active/T-287-deliver-abiogenesis-5-feature-waves.md` | `2009790543a952d7a891f5d318d30408e04cd74e42449143e20654966473da97` |

The ticket's return bookkeeping and correction record were read as tracking, not semantic authority. Unaffected evaluation and residuals above are reused. No actionable finding remains from this bounded review on the successor. Implementation ownership, actual realization and runtime/native/UAT qualification remain unverified and unclaimed; core45's accepted scope and the open S06/qualification/RC1 frontier remain unchanged. This is a delta-only result, not a second broad review. Executive retains disposition. Reviewer closes and stops.
