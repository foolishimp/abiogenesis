# T-272 Proxy Decision: Accept Product-Selected Graph-Span Re-entry

## Authority

Direct human instruction on 2026-07-25 delegated authority to Codex to
continue the accepted ABIogenesis 5.0 plan through completion or until the
human returns for status review.

This decision exercises that authority as a bounded `F_H` proxy. The human
remains the underlying authority. The proxy may accept implementation and
design cuts that preserve the accepted Product and current ticket plan. It
does not authorize a Product reprice, a new ticket hierarchy, a compiler,
controller, second runtime, or replacement Product trajectory.

## Exact Subject

- implementation candidate:
  `16f1e17358bf4c7c59259ffed0b6e0907a9428e7`
- complete M05 design SHA-256:
  `071055b3bd992f71c69d29fd3ed8d3f97cc80314b7df61b952999cfee51804be`
- packed artifact SHA-256:
  `58c73217a40ddb507fcdb6e87d5cea6198582483735f540c532cce37f8ff6c4a`
- Product content digest:
  `sha256:22fc63b086bf6a3e9f13d9508ef69918bd401cd38e623993181ce823c0ee7b5e`
- manifest digest:
  `sha256:90ddc027deb03688d65aebac43be89dc3be42da1b67b881a3dc167103056bc7d`

## Review

The candidate advances one existing S03 conservation obligation through the
same independently packed external Product:

1. the Product publishes one bounded `re_enter` application between exact
   loci in its own GraphFunction;
2. the validator admits only a static backward target with exact contracts;
3. the Product emits either one stable re-entry selection or ordinary
   continuation from its selector C-call;
4. HoG derives and applies the selected cursor while ABG admits the route
   against the exact Product result, C-call judgment, application, replay
   basis, loci, target input, and remaining budget; and
5. replay preserves the admitted route and the Product observes the second
   target visit before converging.

Installed mutations refuse a forward target during catalog validation and a
second Product selection beyond the admitted runtime bound. Neither mutation
produces false closure truth.

Verification:

- `test:m5`: `98/98`;
- `test:m4`: `26/26`;
- external developer Product: `27/27`;
- conservation projection: `55` pass and `7` explicit TODO, representing
  `33` proven rows and `7` open rows;
- two independently produced package archives are byte-identical; and
- `git diff --check` passes.

No compiler, lowering carrier, public controller, second runtime, new event
family, new public operation, or new ticket was introduced.

## Decision

Accept implementation candidate `16f1e173`, M05 Section 12.8, and complete
design digest `071055b3...1804be`. The `graph_span_reentry` conservation row
is proven. T-272 and S03 remain active for the seven explicitly open
consequence, runtime-disposition, and public-control obligations.
