# T-286 Clean Successor Boundary

## Result

T-286 A1 is complete at commit
`9d442b4c8390c0c1e767c1bd46bd7bde282ebd99` on
`codex/t286-abi5-root`.

The checkpoint is deletion-only. It removes 1,657 inherited TypeScript Product
files and 639,425 lines before any ABIogenesis 5.0 realization source enters the
successor line.

## Removed Authority

- canonical runtime and application source;
- runtime, type, live, sandbox, and downstream tests;
- package manifests, lock files, exports, configuration, and toolchain files;
- generated contract, catalog, schema, and capability products;
- tracked dependencies and historical packed tarballs.

## Retained Authority And Evidence

- `specification/` and its exact `ABI5-ROOT-001` binding;
- `.ai-workspace/` tickets, decisions, reviews, and donor freezes;
- the TypeScript design history, including accepted
  `M03_DIRECT_GTL_TRAVERSAL_BEHAVIOR_DESIGN.md` at SHA-256
  `9faeb41ddac839edc9cd2ccb83ae11b05bb54d32168fc35e74a1a9cfb97e92f0`;
- RC5, X, and final-integration as sideways donor references only.

After the checkpoint, the TypeScript tenant contains 279 tracked files, all
under `design/`. It contains zero tracked files under `code/` or `test_env/`.
The checkpoint is pushed to `origin/codex/t286-abi5-root`.

## Frontier

The clean boundary establishes local construction readiness. It does not move
the Product frontier. `ABI5-ROOT-001` remains red at `R1 exact artifacts
verified`; B1 is now the active implementation step.
