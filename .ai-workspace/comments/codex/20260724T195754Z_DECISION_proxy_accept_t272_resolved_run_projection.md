# T-272 Proxy Decision: Accept Resolved Run Projection

## Authority

Direct human instruction on 2026-07-25 delegated authority to Codex to
continue the accepted ABIogenesis 5.0 plan through completion or until the
human returns for status review.

This decision exercises that authority as a bounded `F_H` proxy. The human
remains the underlying authority. The proxy may accept implementation and
design cuts that preserve the accepted Product and current ticket plan. It
does not authorize a Product reprice, a new ticket hierarchy, a compiler,
controller, second runtime, or replacement Product trajectory.

## Exact Subject

- candidate commit:
  `9cd33d15410292c63dc8e7ec0182f9fd8889e280`
- complete M05 design SHA-256:
  `709cd70411529d42b1041dcfb4c34df95575e7b8cb906f18f6bf35084bea254a`
- packed artifact SHA-256:
  `13615f2aa564693f2f30b176687d351a4fd12c7e6b5787f51768721a4bc46084`
- Product content digest:
  `sha256:84f0d7a75576637ac75e03b928285bfa301b36579029a671e0aac19ac13147d1`
- manifest digest:
  `sha256:a2bc96e417dff6a545fa80e0fba1779c5e3c64aab4d9ba4bab589d0eec637fe8`

## Review

The candidate advances the same independently packed developer Product:

1. `run.continue` returns the exact resolved continuation authority bound to
   the appended event-log prefix;
2. a fresh public context can project status, result, ordered replay, and
   lawful actions from that exact durable authority;
3. the resolved result is available only after replay-derived closure and the
   governed construction fold;
4. lawful-action projection uses judged Product `NextActionProjection`
   C-calls consumed by admitted ABG routes; and
5. every read is side-effect free and stale authority refuses.

Verification:

- `test:m5`: `95/95`;
- `test:m4`: `26/26`;
- external developer Product: `24/24`;
- two independently produced package archives are byte-identical; and
- `git diff --check` passes.

No compiler, lowering carrier, public controller, second runtime, new event
family, new operation identity, or new ticket was introduced.

## Decision

Accept candidate `9cd33d15` and M05 design digest `709cd704...254a`.
Release the exact-review hold on Section 12.6. T-272 remains active and may
continue only through the same independently packed external Product toward
the remaining `ABG5-S03` consequence and runtime-disposition outcomes.
