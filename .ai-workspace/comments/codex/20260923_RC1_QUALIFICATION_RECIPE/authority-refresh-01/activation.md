# Worker activation — qualification authority refresh only

Worker `/root/rc1_recipe44` → `/root` Executive; activated 2026-09-26T10:11:00Z.
GOAL-035/T-287/LIFE01; fixed fifteen-family ABIogenesis 5.0, Product execution/context
calculus; GTL declares, HoG traverses, ABG admits truth, replay projects. Subject is
derived qualification definitions and package bindings, not runtime admission.

Executive accepts CORE44-QUALIFICATION-PRODUCT-BINDING-01 and selects the existing
owner repair as `realization_refactor`. Basis: canonical source commit
`29da18bca003ead4045f5061b495f17e76b5fa33`, ABI immutable STDO v2.5.1-rc.1
manifest 5d306da13994e69aa9f215d4c1cd2d0be96283c1e33a652b58e6e9262d036b64,
with F-END-TO-END-INTERFACE-INTEGRATION, Proof/Install and qualification-owning routes.
GLC remains RC4/dev16. Original Product/Requirements/design are authoritative and
outside mutation scope. Completed native44 is reported by Executive, not acquired
or assessed here; its independent scenario coverage review is separate.

Effects: one existing `generate-qualification-rule-catalog.mjs --stage-authorities`
against the exact current accepted originals and installed release; one necessary
`generate-product-manifest.mjs` invocation. The latter calls the existing
qualification generator internally and uses `npm pack --dry-run --ignore-scripts
--json` solely for member projection; no archive or install is created.

Explicit generator-owned write territory under the TypeScript tenant:
- contracts/qualification/authority-inputs.json and existing sources/ copies;
- contracts/qualification/rule-catalog.json, law-basis.json and coverage.json;
- contracts/schemas/self-conformance.schema.json and existing generator-owned
  schema/public/vocabulary/context assets under contracts/ (exact preimages and
  output paths are recorded in preimages.json); existing public-functions,
  public-operations and schemas/operations trees are regenerated by their owner;
- contracts/capabilities/capability-definition-graph.json and product-toolchain-manifest.json;
- existing build/toolchain copies rewritten by the unchanged manifest generator.
Those additional owner paths were inspected before invocation. Only derivation is
authorized; runtime code/ and build/code/ bytes must remain unchanged. No new
implementation path or authority domain may be introduced.

This new authority-refresh-01 directory owns activation, exact preimage locators,
command receipts, delta, focused correspondence/schema evidence and one freeze.
Existing core44 source/archive/install correspondence provides byte-checked
immutable preimage origins where equal; no package or campaign is copied.
Npm dry-run cache/log writes are confined here. No original Product/Requirements/
design change, code refactor, test-selection or coverage-domain change, broad suite,
native/paid actor, application/Public effect, package/install, Git effect, prior
frozen evidence edit or Reviewer activation is permitted.

Stop on changed accepted original or owner source, new material authority/domain,
missing preimage, generator failure or implementation byte change. Preserve the
first causal diagnostic and return the exact bounded dependency. Freeze one
successor, ready/unbound rows and the ordinary package/rebind relation, then stop.
