# Next ordinary caller relation — dependent, not authorized

First return CORE44-QUALIFICATION-PRODUCT-BINDING-01 to Executive. The existing
qualification-source/package owner must establish an accepted exact relation
between packaged governing Product bytes and the immutable selected source.
The existing explicit source-build owner is `stageQualificationAuthorities` in
`scripts/generate-qualification-rule-catalog.mjs`, exposed by
`node scripts/generate-qualification-rule-catalog.mjs --stage-authorities <repo> <exact-STDO-release>`.
Its paths and exact arguments are recorded in [existing-owner-route.json](existing-owner-route.json).
Ordinary `node scripts/generate-product-manifest.mjs` consumes those frozen inputs;
it does not itself refresh the originals. This preparation grants no generator,
package, source repair or publication change. Preserve core44 source/package acceptance and the real native44 result.
Any changed qualifying Product subject remains governed by SELF008; Executive
selects the lawful existing re-entry and prospective identity.

After that dependency, the current steel-thread disposition and final complete
same-subject qualification selection, reuse the existing observed-source root-C2
recipe. Its `selectedFiles`/`stageSelectedInputs` digest checks and
`constructObservedProducerTask` helper operate only in a separately authorized
new disposable worksite. Existing input manifests here are provisional and must
not be promoted or executed while their original-source relation is refused.
Use ordinary installed `program://abiogenesis/worksite/command-execution@5` /
`graph-function://abiogenesis/worksite/command-execution@5` through Public/native
owners, with the retained 13 commands,14 predicates,one verification subtree,
original budgets and first-unexpected-failure stop. No retry or extra tests.

The actual root-C2 Result, retained recipe, declaration, input,C/J,occurrence and
producer/source/currentness must supply `qualification.verification.executionSelectionRef`.
Existing `resolveQualificationExecutionMaterial`, original-producer acquisition
and native proof handoff keep owner/source/consumer boundaries. Independent
native J/O, full real-tree F11 and seven seeded negatives, complete behavioral
and lifecycle coverage, and sole `C.of(AF-22)` remain dependency ordered.
AF25/publication, installed-RC qualification and actual human acceptance are
separate outputs and decisions. No invented basis, prior Run relabeling or
Hello workspace substitution supplies them.
