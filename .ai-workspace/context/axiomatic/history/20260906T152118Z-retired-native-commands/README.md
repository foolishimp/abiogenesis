# Retired Python-Era Native Commands

These are historical command definitions, not discoverable native commands,
current instructions or work authority. RC6 migration removes their former
routes to the absent Python-era `.genesis` engine from `.claude/commands/`.
Use the current Product Definition, bootstrap and exact installed companions.

The exact pre-migration bytes are preserved at Git commit
`29846fc36c6a455e5aab642a5965a47fbfeb815d` under `.claude/commands/`.
No engine command in this directory is authorized for execution.
