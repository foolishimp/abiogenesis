# T-217 The Consciousness Wave — Higher-Order Regulation As System Content

- id: T-217
- type: feature
- ticket_category: consolidation (supersedes 9 partial/overlapping tickets; multiphasal)
- status: active
- goal: GOAL-033 (the Consciousness Wave — see GOALS.md)
- change_intent: >-
    Build the consciousness layer as SYSTEM CONTENT: the higher-order
    regulator over GTL/ABG/odd_* — same machinery, same F_P worker
    class, instantiated over the system's OWN telemetry, with the
    TICKET as its sole effector. Consolidates the redundant, duplicate,
    and partial backlog tickets that all push toward this build into
    one STDO-governed multiphasal wave; every absorbed acceptance is
    carried forward by name (no silent drops).
- change_class: requirement_reprice (Phase 0), then design_reframe
- re_entry_point: requirements (the FPC family refinements + two new
  families), then design_surface
- intake_source: user directive 2026-07-09 ("consolidate the effort
  into a single logical ticket under STDO governance that captures the
  multiphasal holistic work") + the ratified session laws + the T-032
  campaign as the empirical basis
- triaged_at: 2026-07-09
- created_at: 2026-07-09
- entry_gate: the Foundation wave's release-grade frozen-law proving
  run on 4.5.1 (GOALS Phase 6) remains the OPEN closing gate of the
  prior wave; it is not absorbed here. The tier's own proving (Phase 5
  below) may observe that run when it executes.

## Intake Triage (performed — the upward walk)

1. SUBSTANTIVE? Yes — a new public system capability tier (observer +
   tuner as declared products; witness event kinds; annealing carriers).
2. GOALS: EXIST — the Consciousness Wave is ratified in GOALS.md
   (2026-07-09) with the option-2 adjudication ("build the regulator,
   let it tune the reflexes") and its evidence basis.
3. INTENT/PRODUCT: EXIST — INTENT INT-001; PRODUCT.md carries "the
   reflective boundary" (allocation over scarcity, solve/optimize
   separation, declaration drafts only) and the Phase-1 foundation
   laws. GAP: the three session laws (intent-generation loop; same
   manifold/different observables; scenarios-are-test-fixtures) live in
   ticket prose and GOALS — ticket prose does not outrank live spec =>
   Phase 0 product_reprice.
4. REQUIREMENTS: the spine EXISTS — REQ-R-ABG3-FP-CONSCIOUSNESS
   (FPC-001..011, from T-127): observation snapshots (FPC-003), action
   catalogs from declared truth (FPC-004), gap projection read-only
   (FPC-004C), ConstructionIntentCandidate (FPC-005), priority/affect
   as visible policy (FPC-005A-D), admission before invocation
   (FPC-006), at-most-one selection with "typed block, F_H input
   request, TICKET/REPRICE PROPOSAL, or escalation" (FPC-007), lawful
   mechanics only (FPC-008), replay-sufficient truth (FPC-009),
   progress-vs-stagnation ledger (FPC-010). The session's
   intent-generation loop is FPC-005/-007 sharpened; the manifold
   principle is its implementation stance. GAPS (per the absorbed
   tickets' own walks): (a) NO family owns tuner verbs, draft
   admission, ratification states, annealing equivalence contracts
   (T-206 walk); (b) NO family owns the supervisor-witness event kinds
   (T-215: reprice admission, operator F_H lifecycle, hygiene stamps,
   citability predicate); (c) FPC does not yet name the OBSERVER TIER's
   observable set (replay streams, gap_stops, folds, cost rows,
   constitutional-vs-projected drift). First missing layer =
   REQUIREMENTS => change_class requirement_reprice, then design.
5. EMPIRICAL BASIS (recorded, load-bearing): the T-032 campaign — 12
   law-defects invisible to local evaluators in principle; the lower
   layer HALTS honestly rather than lying (15 runs, zero false
   closures); every monitor act was framework-external and
   replay-invisible. The tier's job is exactly that seat, witnessed
   and governed.
6. RELEASE SCOPE: the wave cuts the 4.6 line; each phase lands by the
   established ritual (gate, cut, artifact verification, installer
   repin).

## Design basis (proposed 2026-07-09, ratification gates realization)

build_tenants/abiogenesis/typescript/design/
M03_CONSCIOUSNESS_TIER_STACK_AND_EXECUTION_MODEL.md — the DMM-governed
stacked hierarchy (L0 reality .. L5 atlas/F_H) and the computational
execution model (ONE step function at every tier; a tier is exactly
three parameters: view coordinates, action catalog, effector
admission). NORMATIVE ZOO AUDIT: no new fundamental particles —
measurement and view disciplines are named over EXISTING carriers;
accepted additions are bounded to T-215 witness event registrations,
the tuner/annealing requirement family, and product-layer node types.
The observer's resolve/drill/escalate algebra is DERIVED from the
consequence() outcome set under a catalog with no constructive actions
(making the T-032 monitor's role-collapse unrepresentable). Phase 0
scope TIGHTENS accordingly: no View or Measurement carrier is minted.

## Holistic ontology & epistemology (ratified design synthesis 2026-07-09)

The exploration converged to one recursive structure. Phase 0 ratifies
this whole synthesis; every particle already exists (zoo audit, design
SS7) — this names the patterns.

ONTOLOGY (what exists):
- ONE MEDIUM: the graph function — typed contract, governed interior.
  Nothing travels except through graph functions; the overlay is their
  published composition (the rails).
- ONE STRUCTURE: the recursive frame. A tier, a view, an episode, a
  drill-down, the operator session — all the same step function
  (render -> dispatch -> admit -> evaluate -> consequence) over a
  bounded chart. Recursion is view restriction opened through published
  refinements/CandidateFamilies.
- ONE RECORD: the append-only measurement record (replay). All models
  (projections) derive from it; there is no second truth store.
- ONE REALITY: the workspace — mutable, concurrent, the referent, never
  a component; reached only by instruments in and admitted actions out.
- THE CLI IS THE ROOT BOUNDARY CONDITION: the abg CLI is a shell that
  hands INITIAL CONDITIONS to the first node. The operator is not
  outside the ontology — the CLI is the transition map between reality
  (operator + workspace) and the root frame; every verb is an admitted
  input to a node, every operator act therefore an event. From the
  first node it is the same recursive structure all the way down. This
  is why T-215's witness problem is structural, not additive: there is
  no operator surface OTHER than admitted inputs to nodes.

EPISTEMOLOGY (how it is known):
- Reality is known only by MEASUREMENT (attributed point-observation,
  stale on arrival); replay is the measurement record, never reality;
  drift is the default, bounded by attribution + single-writer windows
  + scheduled re-measurement.
- TRUTH IS GATED AT TRANSITIONS: admission/mint/fold seams are where
  epistemic commitments are made; defects concentrate there and are
  invisible to any local view in principle (T-032: 12/12). The
  regulation tier is the view whose coordinates ARE the transitions.
- TWO BINDING TIMES OVER ONE ALGEBRA: graph functions compose
  ahead-of-time (the overlay = rails: deterministic, cheap per run,
  ratifiable before execution — accountability's shape) or just-in-time
  (emergent recursion = discovery: ambiguity-tolerant, a judgment per
  step, bounded by the frame contract). Both are optimizations; both
  produce terms in the same typed algebra; they are interchangeable
  where the boundary carries every obligation.
- THE MODE-SELECTION LAW: ambiguity/discovery -> emergent; known flow
  -> rails; per-contract selection is a CandidateFamily choice under
  visible policy, biased toward emergence when confidence is low
  (failure economics: a wrong "rails" guess costs halts; a wrong
  "emergent" guess costs only compute). A rail-break (declared path
  halting on ambiguity) is a MODE SIGNAL before it is a defect.
- CRYSTALLIZATION IS COMPILATION: JIT discovery traces are AOT rail
  candidates. The tuner reads COMPOSITION ENTROPY from replay — low
  entropy (same route every run) -> lay rails (publish overlay
  extension); non-discriminating edges -> pull up. Abstraction (naming
  a proven composition as a new graph function) grows the vocabulary.
  All track changes are declaration drafts behind F_H (SS13.1).
- TYPING IS THE COMPOSITION LAW: functions compose where types unify;
  ill-typed plans are unrepresentable (admission rejects before
  execution). The LLM composes accountability; it cannot compose around
  it. Gap = the typed distance from frontier to exit contract (FPC-004
  C/D); F_D narrows (what's missing, which tools qualify), F_P chooses.

OPEN TENSIONS (named, carried into design):
- ADAPTER LAW: type-mismatch glue must be DECLARED (typed projection or
  small graph function); a missing adapter is a gap, never inline
  improvisation — else mechanism leaks into the joints (the campaign's
  worst-bug locus).
- CATALOG-VISIBILITY ECONOMICS: the view's serialization is the context
  spend and the catalog is part of the view; as abstraction grows the
  vocabulary, curation of what-is-visible-from-where is a tuner surface.

## Constitutional basis (Phase 0 ratifies these into live spec)

- INTENT-GENERATION LOOP (user, 2026-07-09; in T-207 prose today):
  internal model + telemetry -> gap -> intent -> solutioning UP TO
  action; the TICKET is the only action carrier; ticket-first, never
  act-then-ledger.
- SAME MANIFOLD, DIFFERENT OBSERVABLES (user, 2026-07-09; Conant-Ashby
  / requisite variety): the tier is NOT a meta-framework — same
  GTL/ABG machinery, same F_P worker class, pointed at the system's
  own telemetry; recursion terminates at the human F_H seat.
- SCENARIOS ARE TEST FIXTURES (user, 2026-07-09): a proving campaign's
  deliverable is the BUILDER; convergence claims require frozen law.
- OPTION-2 ADJUDICATION (user, 2026-07-09): local-loop tightening
  arrives as the tier's OUTPUT (tuner-derived), never hand-tuning.

## Phases (multiphasal, inside-out; each phase exits by the ritual)

### Phase 0 — Constitutional repricing
(+ CLI-as-control-plane law: the abg CLI is the ONE operator interface
— every operator act a CLI verb, every verb an admitted event, every
tier surface a CLI-rendered projection; requirement families below
include the CLI verb surface. + The crystallization law: graph where
truth must be gated, emergence where truth is claimed at a boundary;
promote/demote as tuner drafts behind F_H. Design SS8-SS9.)
Product_reprice: the three session laws + option-2 adjudication into
PRODUCT.md (reflective boundary section). Requirement_reprice: (a) the
TUNER/ANNEALING family (absorbs T-206's walk: tuner catalog + verbs,
draft admission, ratification states, annealing-proposal carrier with
equivalence contracts per SS13.1); (b) the SUPERVISOR-WITNESS family
(absorbs T-215 rows as requirements: halt-diagnosis projection,
defect-intake record, declaration_reprice_admitted, per-segment
substrate stamps, operator run_resumed/run_stopped F_H events,
workspace hygiene stamps, mechanical citability predicate); (c) FPC
refinement naming the observer-tier observable set. EXIT: live spec
carries every law this wave builds on.

### Phase 1 — Sense organs + trustworthy telemetry
Absorbs T-215 (rows 1-7 realized as kernel events/projections),
T-211's trust items (replay-log authentication — the tier reads replay
as sense data, so the T-195 refinement is now load-bearing; the valibot
installer defect fixed properly, not ritual), T-213 (typed artifact
schemas: one schema rendered into the prompt, enforced at admission,
sole shape authority — the tier must trust worker-payload telemetry),
and T-214 (diff-execution witness: coverage-of-change gates + the
generated-binding export pin rule — earned depth for the builder
itself). EXIT: every observable the tier consumes is typed,
authenticated, and complete; "frozen-law run" is a replay predicate,
not judgment.

### Phase 2 — Clean kernel boundary (the regulated surface)
Absorbs T-209's REMAINDER under its migration declaration (carried
verbatim: declarations-only adoption — worker-loop plumbing into the
standard handler family; D3 report-verification eviction from odd_glc
userland into a kernel surface; D1.4 kernel-witnessed workspace
digests; proxy prohibition; 11.5B execution-authority audit as exit)
and T-208 items 1-2 (admission-support commonization — now 4+ sites;
T-188 fixture family consolidation) as the cleanup riders. EXIT: no
mechanism in userland; 11.5B clean; one authority per recurring seam.

### Phase 3 — The Observer (T-207 realized under FPC law)
The intent-generation observer as a DECLARED product: GTL node types
over the telemetry observables; observation snapshots (FPC-003) from
replay + the Phase-1 sense organs; internal model = the constitutional
surface; gap derivation (FPC-004C/D); ConstructionIntentCandidate
ranking under a visible priority scheme (FPC-005/-005A); output =
TRIAGED TICKET DRAFTS (FPC-007's ticket/reprice proposal) behind an
F_H ratification gate. The monitor seat becomes a framework actor:
every act admitted, gated, replayable. EXIT: from a live replay stream
the observer produces correctly-triaged ticket drafts (owner,
change_class, re-entry point) with zero unlogged supervisor acts —
validated against the T-032 campaign ledger as ground truth (12 known
defects; the observer must independently derive their triage class).

### Phase 4 — The Tuner (T-206 realized) + session economics
The optimisation half: tune report/propose/ratify verbs over replay +
cost read models; annealing proposals (F_P->F_D interiors) ONLY as
declaration drafts with admitted equivalence contracts (SS13.1
ratification); evaluator calibration proposals derived from telemetry
(which stages burn retries, which contracts leak shape variance).
T-110 rides here as the economics substrate: sticky-session agent pool
over the traced call-out interface (session affinity from carrier
identity, local-spawn default preserved) — the cost observables the
tuner optimizes and the affordability of long observer/tuner turns.
EXIT: first annealing proposal ratified end to end; cost ledger live
per turn; local-loop honing tickets generated FROM telemetry (the
option-2 adjudication realized).

### Phase 5 — The tier's proving campaign
The consciousness tier observes a live proving campaign (the 4.5.1
frozen-law run and/or a fresh scenario) with the human in the F_H seat
and the machine tier in the monitor seat. Campaign law applies to the
TIER: its misses are its builder bugs, ledgered and fixed at their
owners. EXIT: a campaign closes where every supervisor act is
replay-visible, halts were diagnosed/triaged/ticketed by the observer,
and the tuner emitted at least one ratified optimisation — with zero
out-of-framework interventions.

### Phase 6 — Release
The 4.6 line Foundation+Consciousness cut: authored note claiming
exactly what Phase 5 demonstrated; artifact verification; installer
repin; release-grade by its own frozen-law proving.

## Supersession table (every absorbed ticket, content carried by phase)

| Superseded | Content carried at | Nothing-lost check |
|---|---|---|
| T-206 tuner/consciousness loop | Phase 0 (requirement family) + Phase 4 | tuner catalog, verbs, SS13.1 drafts, annealing authority, equivalence contracts |
| T-207 intent observer | Phase 0 (laws) + Phase 3 | ratified loop + manifold principle + observer node + ticket effector |
| T-215 supervisor as admitted actor | Phase 0 (witness family) + Phase 1 | rows 1-7 verbatim; row 8 = Phases 3-5 |
| T-213 typed artifact schemas | Phase 1 | one-schema/three-duties law; prompt-rendered + admission-enforced |
| T-214 diff-execution witness | Phase 1 | coverage-of-change gate; binding export pin rule |
| T-211 rc10 survivors | Phase 1 (trust items) | replay-log authentication; valibot installer fix; remaining review-survivor rows re-triaged into Phase 1 or explicitly retired at Phase 0 |
| T-110 sticky-session pool | Phase 4 | session affinity over traced call-out; local-spawn default; T-111 dependency noted |
| T-208 commonization paydown | Phase 2 (riders) | items 1-2 verbatim; item 3's -013 triage note preserved |
| T-209 standard-path adoption | Phase 2 | migration declaration + checklist carried VERBATIM; execution-authority core already proven (history preserved in the superseded file) |

OUT OF SCOPE (not consciousness; remain in backlog untouched):
T-092/T-094/T-095 (paused Python reference line), T-178/T-179
(registry lifecycle semantics), B-010 (conformance induction).

## Non-closure conditions

- Any absorbed acceptance not delivered or not explicitly retired with
  reasons at the phase that owns it (the nothing-lost law).
- The observer ACTING on anything other than ticket drafts behind F_H
  ratification (the loop collapse the T-032 campaign demonstrated).
- Tuner writes beyond declaration drafts (SS13.1 violated).
- A meta-framework: any tier machinery that is not the same GTL/ABG
  substrate declared over telemetry observables.
- Hand-tuned local loops presented as tier output (option-2 inverted).
- Phase claims resting on runs with mid-flight law amendments (frozen-
  law predicate required for capability claims).
