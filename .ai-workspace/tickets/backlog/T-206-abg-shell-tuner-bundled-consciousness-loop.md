---
id: T-206
type: feature
ticket_category: ordinary
status: backlog
opened: 2026-07-06
depends: T-205 (standard plugin + step-2 interpretation + cost projection)
source: T-200 design §17/§17.1
change_intent: bundle the reflective optimisation loop (tune report/propose/ratify) as first-class system content writing declaration drafts only
change_class: requirement_reprice
re_entry_point: requirements (then design)
triaged_at: 2026-07-09
updated_at: 2026-07-09
---

## Intake Triage (2026-07-09)

1. SUBSTANTIVE? Yes — a new public system capability (tuner catalog,
   shell verbs, staged-draft write path).
2. BOUNDARY: product layer + abg/m03 read surfaces; writes are
   declaration drafts only (§13.1).
3. UPWARD WALK: product truth EXISTS (PRODUCT.md "The reflective
   boundary" — allocation over scarcity, solve/optimize separation,
   declarations only). Requirement decomposition DOES NOT — no REQ
   family owns tuner verbs, draft admission, ratification states, or
   annealing equivalence contracts. First missing layer = REQUIREMENTS
   => change_class requirement_reprice, then design, then realization.
4. PRIORITY ELEVATION (2026-07-09 execution-default law): this ticket
   is now the constitutional ANNEALING AUTHORITY — F_D-specific
   traversals are built only through this optimisation inspection
   (typed F_P generics are the default; F_P->F_D annealing requires
   admitted equivalence contracts). Elevate from nice-to-have to the
   named optimisation path; its requirement family must own the
   annealing-proposal carrier.
5. SPAN: requirements -> design -> tuner catalog/verbs -> -012/cost/
   witness read models -> §13.1 draft ratification.

The abg shell tuner: the consciousness loop bundled as first-class
system content. Published gtl://abg/tuner/* catalog module (default
tuning loop; overridable per -016); shell verbs tune report / tune
propose / tune ratify; staged-draft write path enforcing §13.1
(proposes terms as drafts, F_H or declared auto-ratify admits); runs on
the same engine in the same workspace, spine-visible, audited by the
-012 it consumes. Consumes: per-configuration cost projection, temporal
verdicts (T-201), witness rows (T-196), replay. Writes: declaration
drafts only. odd_sdlc T-166 consensus programs compose in as tuner
stages.

Absorbed (board consolidation): T-196 witness-row consumption and T-201
closure-point verdict consumption — the tuner's read surface; realized
here, not as separate waves.

## Design Principle (user, 2026-07-09): same manifold, different observables

"Several billion years has shown that for higher order systems you need
some consciousness regulation with maybe the same sized manifold but
over a different set of observables." (Conant-Ashby: the regulator must
model the system; Ashby: requisite variety.)

CONSEQUENCE FOR THIS TICKET: the consciousness tier is NOT a
meta-framework. It is the SAME machinery — GTL declarations, ABG
traversal/admission/replay, F_P workers behind typed boundaries —
instantiated over a DIFFERENT observable set: the system's own replay
streams, fold outcomes, gap_stops, retry histories, cost rows, and the
drift between the constitutional surface (the internal model) and
projected reality. The T-215 witness events and halt-diagnosis
projections are its sense organs; its requisite variety is the same
F_P worker class that produced the disturbances; its ONLY effector is
the ticket (the ratified intent-generation loop). The recursion
terminates: the regulator-of-the-regulator is the goals/intent surface
with the human in the F_H seat, reading the top layer's replay.

The T-032 campaign is the empirical basis: the monitor seat required no
machinery the framework lacked — it required the same intelligence
pointed at events.jsonl instead of at Scala, plus the declared
observables and the ticket effector it did not yet have.
