# T-213 Typed Artifact Schemas — the Worker Boundary as One Strong Type

- id: T-213
- type: feature
- ticket_category: ordinary
- library_usage: extend
- governing_library: abg/m03 instruction assembly + payload admission (the responseJsonSchema precedent; valibot as the one validation engine)
- status: backlog
- goal: GOAL-032 (post-Foundation hardening; candidate Phase 5 rider)
- change_intent: >-
    Kill the dominant campaign bug class (concept-named, shape-unnamed
    worker contracts): every worker-authored artifact gets ONE declared
    schema serving three duties — rendered into the prompt, enforced at
    admission with typed corrective rejections, and the only shape
    authority. No prose-only shape law at any worker boundary.
- change_class: design_reframe
- re_entry_point: design_surface
- intake_source: T-032 campaign forensics (6 of 12 ledgered defects were
  this class: status/exitStatus, cwd, JAVA_HOME field families, plus the
  T-031 envelope-key lesson recurring)
- triaged_at: 2026-07-09
- created_at: 2026-07-09

## Intake Triage (performed)

1. SUBSTANTIVE? Yes — the worker boundary API becomes typed data
   (constitutional: "agentic workers are incredibly capable, as long as
   their boundary APIs are defined by strong types").
2. UPWARD WALK: the transport's responseJsonSchema realizes this for
   the ASSESSMENT envelope; -035's expectedEvidenceShapeRefs gesture at
   it for evidence; FILE payloads (test-execution-result.json,
   depth-proof-map.json, mutation-outcomes.json) have no schema home —
   first missing layer = DESIGN => design_reframe.
3. SPAN: GTL stage declarations gain artifactSchemas (path -> schema
   ref, declaration data) -> instruction assembly renders the schema
   into the prompt -> kernel admits the file payload against it (typed
   rejection carries the schema back as corrective guidance) ->
   downstream field-family aliases become dead code and are removed.
4. NON-CLOSURE: prose shape instructions surviving alongside a schema
   (two truths); schema at admission but absent from the prompt (the
   worker must SEE the type it is held to).
