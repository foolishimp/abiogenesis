[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "genesis"
version = "3.0.0"
description = "Genesis — GTL-native AI SDLC engine"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["genesis", "gtl"]

[tool.setuptools.package-dir]
"genesis" = "build_tenants/abiogenesis/python/code/genesis"
"gtl" = "build_tenants/abiogenesis/python/code/gtl"
